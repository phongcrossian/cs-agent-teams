---
phase: 01-knowledge-survey-conflict-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md
  - .planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md
  - .planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP.md
  - .planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md
autonomous: true
requirements: [KB-01, KB-02]
must_haves:
  truths:
    - "A reviewer can open SURVEY.md and see a section-keyed inventory whose first entry is the Whimsical workflow source with format (SVG) and last-update cadence"
    - "A reviewer can read GLOSSARY.md and find every internal acronym used in WorkFlow.svg with a plain-English definition and a source, with unconfirmed terms explicitly marked TBD"
    - "A reviewer can read CODE-MAP.md and find every state/template code present in WorkFlow.svg (A1-A9, B1-B11, C1, D1-D9, E1-E13, F1-F22, G1-G13) mapped to its workflow action"
    - "A reviewer can read POLICY-THRESHOLD-INDEX.md and find every numeric/temporal threshold embedded in WorkFlow.svg with its source location in the diagram"
  artifacts:
    - path: ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md"
      provides: "Master section-keyed survey skeleton + Whimsical source entry + coverage backbone"
      contains: "## Source Inventory"
    - path: ".planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md"
      provides: "Internal jargon to plain-English with source per term"
      contains: "CEE"
    - path: ".planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP.md"
      provides: "Workflow state/template code to action mapping"
      contains: "F12"
    - path: ".planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md"
      provides: "Every numeric/temporal threshold with source"
      contains: "1 hour"
  key_links:
    - from: "CODE-MAP.md"
      to: "snapshots/WorkFlow.svg"
      via: "each code cites its workflow node / macro-flow"
      pattern: "WorkFlow"
    - from: "SURVEY.md"
      to: "GLOSSARY.md / CODE-MAP.md / POLICY-THRESHOLD-INDEX.md"
      via: "SURVEY links to the sibling deliverables"
      pattern: "CODE-MAP"
---

<objective>
Take the Whimsical workflow diagram (WorkFlow.svg, already committed to the repo) fully end-to-end as the first vertical slice of the knowledge survey. This proves the survey method on one source before any CS-Lead-gated access is needed, and produces the four reusable, append-friendly deliverable skeletons that later source-survey plans extend.

Purpose: WorkFlow.svg is the process-knowledge backbone (D-05 says use it as the coverage backbone). It embeds the operational thresholds, internal jargon, and state/template codes that every other source must be reconciled against. Extracting structured facts from it first establishes the section-keyed structure, the glossary, the code map, and the threshold index that downstream conflict detection (Plan 04) depends on.

Output: SURVEY.md (master, section-keyed), GLOSSARY.md, CODE-MAP.md, POLICY-THRESHOLD-INDEX.md — all in English, all append-friendly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-knowledge-survey-conflict-inventory/01-CONTEXT.md
@2026-05-28-meeting-note.md
@.planning/phases/01-knowledge-survey-conflict-inventory/snapshots/WorkFlow.svg

<facts_already_extracted>
The following structured facts have already been confirmed present in WorkFlow.svg and the meeting note. The executor must verify and expand them against the SVG, not re-discover them from scratch.

6 macro-flows: (1) CANCELLATION REQUEST, (2) CHANGE REQUEST, (3) PRODUCT COMPLAINT, (4) SHIPPING INQUIRY [sub: Types of inquiries, Test contract, DNR, OOS, RTS, Common scenarios], (5) EMAIL-CALL COLLAB [flow + email-agent side + call-agent side + Request to call team], (6) CEE-SCE COLLAB [flow + CEE side + SCE side + Request types].

State/template codes present in the SVG (confirmed by grep): A1-A9, B1-B3, B5-B7, B9-B11, C1, D1-D9, E1-E13, F1-F22, G1-G13. (Note B4 and B8 may be referenced as ranges "B(5),(6),(7),8" — verify whether each integer exists as a node.)

Thresholds embedded in the SVG (confirmed): cancellation eligible within 1 hour of order placement; change eligible within 1 hour of order placement; warranty within 45 days of purchase date (internal policy: within 14 days from delivery date); aftersale promotion = 40% discount + free shipping; partial-refund / discount cap "up to 20%"; "Offer 50% refund"; shipping inquiry day thresholds (shipping time > 21 days, > 35 days, last tracking update >= 15 days, promise refund on day 40); discount "up to 50%" in shipping common scenarios; operational rule "1 note per request only"; "SCE check on: 11AM - 4PM".

Jargon present in the SVG: CEE, SCE, DO, PO, TA, TO, RTS, OOS, DNR, TC (Test Contract), WOC, WNF, OB, MOQ, FFM, and DO status states (New, Active, In-active, Clear-stock, Write-off, Written-off, Disposed, Active/Disposed). Confirm meaning where the diagram or meeting note makes it explicit; mark anything not explicitly defined as TBD with a note that Plan 03 (Confluence) or a CS-team action item should resolve it.
</facts_already_extracted>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SURVEY.md master skeleton and the Whimsical source-inventory entry</name>
  <read_first>
    - .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/WorkFlow.svg
    - 2026-05-28-meeting-note.md
    - .planning/phases/01-knowledge-survey-conflict-inventory/01-CONTEXT.md
    - .planning/ROADMAP.md (Phase 1 section: the 4 success criteria)
  </read_first>
  <action>
    Create SURVEY.md as the master, append-friendly, section-keyed survey document (D-07). Include these top-level sections, each with a stable heading later plans can append under without reflowing:
    - "## Source Inventory" — a table with columns: Source ID, Source family, Format, Snapshot path, Last-update cadence, Owner/access, Notes. Add the first row for the Whimsical workflow diagram: family = "Whimsical workflow diagram (CEE workspace)", Format = SVG, Snapshot path = snapshots/WorkFlow.svg, Last-update cadence = "TBD — confirm with CS Lead" (it is a living diagram), Owner = CEE workspace via CS Lead. Leave clearly-labeled empty rows / placeholders for the Google Sites Email Templates source (filled by Plan 02) and Confluence SCE guides (filled by Plan 03).
    - "## Coverage Map (backbone)" — list the Freshdesk Level-In categories from the meeting note (Complaint 71% [Return 53, Replace 27, Review 12, Refund partial/full], Change request 16%, Inquiry 9.7% incl. pre-purchase, Chargeback/claim 3%, Other) and map each of the 6 WorkFlow macro-flows to the Level-In category it serves. State explicitly that per-section tagging and evidence-sample validation are completed in Plan 04; this section is the top-down backbone only (D-05).
    - "## Conflict Findings" — empty placeholder section with a note that Plan 04 populates it.
    - "## Linked Deliverables" — links to GLOSSARY.md, CODE-MAP.md, POLICY-THRESHOLD-INDEX.md (created in Tasks 2-3) and forward-references to COVERAGE-MAP.csv, CONFLICT-INVENTORY.md, ACTION-ITEMS.md (Plan 04).
    Map each section to which ROADMAP Phase 1 success criterion it serves in a short "## Traceability" note (criteria 1=Source Inventory, 2=Coverage Map, 3=Conflict Findings, 4=Action Items forward-ref). Do NOT paste full SVG content into SURVEY.md; reference the snapshot path.
  </action>
  <verify>
    <automated>test -f ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md" && grep -q "## Source Inventory" ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md" && grep -q "## Coverage Map" ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md" && grep -q "## Conflict Findings" ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md" && grep -qi "WorkFlow.svg" ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md"</automated>
  </verify>
  <acceptance_criteria>
    - SURVEY.md exists with the four stable section headings (Source Inventory, Coverage Map, Conflict Findings, Linked Deliverables) plus Traceability.
    - Source Inventory table has a Whimsical row referencing snapshots/WorkFlow.svg and explicit placeholder rows for Email Templates and Confluence.
    - Coverage Map backbone lists all five Level-In categories from the meeting note and maps all 6 macro-flows.
    - No raw SVG markup pasted into SURVEY.md.
  </acceptance_criteria>
  <done>SURVEY.md is the section-keyed master that later plans append to without restructuring; criterion 1 (inventory) and the coverage backbone (criterion 2) are seeded.</done>
</task>

<task type="auto">
  <name>Task 2: Build GLOSSARY.md and POLICY-THRESHOLD-INDEX.md from WorkFlow.svg</name>
  <read_first>
    - .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/WorkFlow.svg
    - 2026-05-28-meeting-note.md
    - .planning/phases/01-knowledge-survey-conflict-inventory/01-CONTEXT.md (Specifics: jargon list + threshold list)
  </read_first>
  <action>
    Create GLOSSARY.md: a table with columns Term, Plain-English meaning, Source (workflow macro-flow / node label, or meeting note), Confidence (Confirmed / TBD). Cover at minimum every acronym appearing in WorkFlow.svg: CEE, SCE, DO, PO, TA, TO, RTS, OOS, DNR, TC (Test Contract), WOC, WNF, OB, MOQ, FFM, plus the DO product/status states (New, Active, In-active, Clear-stock, Write-off, Written-off, Disposed). Where the diagram defines a term explicitly (e.g. DNR appears as "Delivered not received" in flow 4.3; OOS as "Out of stock"; RTS as "Returned to sender"), mark Confidence = Confirmed and cite the macro-flow. Where meaning is not explicit in any in-repo source (e.g. TA/TO, WOC/WNF, OB, MOQ, FFM if unconfirmed), give the best-supported reading and mark Confidence = TBD with a note "resolve via Confluence (Plan 03) or CS-team action item (Plan 04)".

    Create POLICY-THRESHOLD-INDEX.md: a table with columns Threshold ID, Description, Value, Source (WorkFlow macro-flow + node text), Cross-source status. Capture every numeric/temporal threshold confirmed in the SVG: 1-hour cancellation window, 1-hour change window, 45-day warranty (purchase date), 14-day internal warranty (delivery date), 40% aftersale promo + free shipping, "up to 20%" partial-refund/discount cap, "50% refund" offers, shipping-time thresholds (>21 days, >35 days, last tracking >=15 days, promise full refund on day 40), "up to 50%" discount in shipping common scenarios, "1 note per request only", "SCE check 11AM-4PM". Set Cross-source status = "Whimsical only — pending cross-check (Plan 04)" for each, so Plan 04 can fill the Confluence/Email-Template columns later (D-06 mandatory threshold axis).
  </action>
  <verify>
    <automated>test -f ".planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md" && test -f ".planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md" && grep -q "CEE" ".planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md" && grep -q "SCE" ".planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md" && grep -q "DNR" ".planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md" && grep -qi "45" ".planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md" && grep -qi "1 hour" ".planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md"</automated>
  </verify>
  <acceptance_criteria>
    - GLOSSARY.md contains a row for each of CEE, SCE, DO, PO, TA, TO, RTS, OOS, DNR, TC, WOC, WNF, OB, MOQ, FFM and the DO status states; each row has a Source and a Confidence (Confirmed or TBD) value.
    - Every term whose meaning is not explicit in an in-repo source is marked TBD (not silently guessed as Confirmed).
    - POLICY-THRESHOLD-INDEX.md lists at least the six mandatory thresholds from CONTEXT.md (1h cancel, 1h change, 45d warranty, 40% promo, 20% cap, "1 note per request") plus the shipping-day thresholds, each with a Source citing the WorkFlow macro-flow and a Cross-source status placeholder.
  </acceptance_criteria>
  <done>Glossary and threshold index exist as the canonical reference for jargon and numeric policy; the threshold cross-source axis is staged for Plan 04.</done>
</task>

<task type="auto">
  <name>Task 3: Build CODE-MAP.md covering every workflow state/template code</name>
  <read_first>
    - .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/WorkFlow.svg
    - .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/templates.md
    - .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/product complaint-within guarantee-template1.md
    - .planning/phases/01-knowledge-survey-conflict-inventory/01-CONTEXT.md
  </read_first>
  <action>
    Create CODE-MAP.md mapping every workflow state/template code in WorkFlow.svg to its action. Use a table with columns: Code, Macro-flow, Described action (from the diagram node text), Linked email template (filled in Plan 02), Notes. Enumerate by code family using the codes confirmed present in the SVG:
    - A-codes (A1-A9): product-complaint within-guarantee replacement/return/refund offers.
    - B-codes (B1-B3, B5-B7, B9-B11): product-complaint return/refund + change-request branches. Note explicitly whether B4 and B8 exist as nodes or are only referenced inside ranges like "B(5),(6),(7),8" — if a code is referenced but has no own node, record it as "referenced, no dedicated node — verify in Plan 03/04".
    - C1: out-of-warranty "40% for next purchase".
    - D1-D9: product-complaint resolution / refund-excluding-fees / evidence requests.
    - E1-E13: change-request actions (address, size, color, billing, add-items, validate-address).
    - F1-F22: cancellation-request actions (cancel DO/PO, refunds, express shipping, postpone).
    - G1-G13: shipping-inquiry actions (DNR/OOS/RTS/common-scenario offers).
    Leave the "Linked email template" column as "TBD — Plan 02" for now (Plan 02 owns wiring codes to the Email Templates). Add a short "## Coverage note" listing any code family where the diagram action text was ambiguous, so Plan 04 can surface it. Do NOT paste raw SVG markup; transcribe the human-readable node text only.
  </action>
  <verify>
    <automated>F=".planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP.md"; test -f "$F" && for c in A1 A9 C1 D9 E13 F12 F22 G1 G13; do grep -q "$c" "$F" || { echo "MISSING $c"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CODE-MAP.md exists and contains a row for every code confirmed in the SVG across all six families (spot-checked: A1, A9, C1, D9, E13, F12, F22, G1, G13 all present).
    - Each code row names its macro-flow and a described action transcribed from the diagram.
    - The "Linked email template" column is present for every row (value may be "TBD — Plan 02").
    - Codes referenced only inside ranges (potential B4/B8) are explicitly recorded as referenced-without-node rather than silently omitted.
  </acceptance_criteria>
  <done>Every workflow code is mapped to an action; the code-to-template wiring is staged for Plan 02 so Phase 3 ingest can later resolve cross-references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkFlow.svg content -> committed deliverables | Diagram text is internal operational knowledge, not customer PII. Low sensitivity. |
| Future LLM-assisted steps (Plan 04) -> repo | Out of scope for this plan; flagged here for continuity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Deliverable files (SURVEY/GLOSSARY/CODE-MAP/THRESHOLD) | accept | Source is the internal workflow diagram already committed to the repo; contains operational process knowledge and internal jargon, no customer PII. No new disclosure beyond the existing snapshot. |
| T-01-02 | Information Disclosure | Customer ticket samples | mitigate | This plan deliberately introduces NO ticket samples. Evidence sampling is deferred to Plan 04, where PII redaction is mandatory. This plan must not paste any customer data. |
| T-01-SC | Tampering | Package installs | accept | Docs-only phase. No npm/pip/cargo installs in this plan; no supply-chain surface. |
</threat_model>

<verification>
- All four deliverable files exist under the phase directory.
- SURVEY.md is section-keyed with the four stable headings and links to the sibling deliverables.
- GLOSSARY.md, CODE-MAP.md, POLICY-THRESHOLD-INDEX.md derive every entry from WorkFlow.svg / the meeting note and cite their source.
- No customer PII present anywhere in this plan's output.
</verification>

<success_criteria>
- The Whimsical source is fully surveyed: inventoried, snapshot referenced, coverage-backbone tagged, jargon glossed, codes mapped, thresholds indexed.
- The survey method is proven on one source and the append-friendly skeletons exist for Plans 02-04 to extend.
- ROADMAP Phase 1 criteria 1 and 2 are seeded; criteria 3 and 4 are staged with placeholders.
</success_criteria>

<output>
Create `.planning/phases/01-knowledge-survey-conflict-inventory/01-01-SUMMARY.md` when done.
</output>
