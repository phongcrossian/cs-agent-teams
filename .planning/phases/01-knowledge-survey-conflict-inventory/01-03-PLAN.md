---
phase: 01-knowledge-survey-conflict-inventory
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/phases/01-knowledge-survey-conflict-inventory/SURVEY-confluence.md
autonomous: false
requirements: [KB-01]
user_setup:
  - service: Confluence (SCE root-cause guides)
    why: "Access is read-only viewer via CS Lead only; no API tokens, no scripted enumeration (D-01). The specific space/page list is TBD and must be granted before the survey can enumerate it."
    dashboard_config:
      - task: "Grant read-only viewer access to the Confluence SCE root-cause classification space and confirm the root page(s)"
        location: "Confluence — SCE space (URL TBD, provided by CS Lead)"
      - task: "Export each SCE guide page (and anything it links to) to PDF (D-04) and drop into snapshots/confluence/"
        location: "repo: .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/confluence/"
must_haves:
  truths:
    - "A reviewer can read SURVEY-confluence.md and see every surveyed Confluence SCE page inventoried with format (PDF), snapshot path, and last-update cadence"
    - "A reviewer can see which root-cause classification categories the SCE guides define and how they relate to the workflow macro-flows / Level-In categories"
    - "A reviewer can see any thresholds or jargon definitions found in Confluence cross-referenced back to POLICY-THRESHOLD-INDEX.md and GLOSSARY.md (resolving Plan 01 TBDs where possible)"
  artifacts:
    - path: ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY-confluence.md"
      provides: "Confluence SCE source inventory + root-cause taxonomy + threshold/jargon cross-refs"
      contains: "## Confluence SCE Inventory"
  key_links:
    - from: "SURVEY-confluence.md"
      to: "snapshots/confluence/*.pdf"
      via: "each inventoried page cites its PDF snapshot path"
      pattern: "snapshots/confluence"
    - from: "SURVEY-confluence.md"
      to: "POLICY-THRESHOLD-INDEX.md / GLOSSARY.md"
      via: "Confluence findings annotate threshold cross-source status and resolve glossary TBDs"
      pattern: "POLICY-THRESHOLD-INDEX|GLOSSARY"
---

<objective>
Survey the Confluence SCE root-cause classification guides — the source CS agents use at step B4 to tag root cause. This applies the proven survey method to the third source family and captures the root-cause taxonomy plus any policy thresholds/jargon definitions that resolve the TBDs Plan 01 left open.

Purpose: SCE Confluence guides define the root-cause logic that downstream classification and grounding depend on. They are also the most likely place to find authoritative definitions for jargon marked TBD in GLOSSARY.md and threshold values to cross-check against the Whimsical diagram (the D-06 threshold axis). Because access is viewer-only via CS Lead and the page list is unknown, this plan is gated on a human checkpoint that grants access and provides PDF exports.

Output: SURVEY-confluence.md + Confluence PDF snapshots under snapshots/confluence/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-knowledge-survey-conflict-inventory/01-CONTEXT.md
@.planning/phases/01-knowledge-survey-conflict-inventory/SURVEY.md
@.planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md
@.planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md
@2026-05-28-meeting-note.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Nothing yet — this plan cannot begin until Confluence access and PDF exports are provided. Access is read-only viewer via CS Lead with no API/scripted enumeration (D-01), and the SCE space/page list is currently TBD.
  </what-built>
  <how-to-verify>
    1. Grant the project read-only viewer access to the Confluence SCE root-cause classification space and confirm the root page(s) URL.
    2. Walk the SCE guide(s) page-by-page in the Confluence UI, following any pages they link to (D-02 scope = the guides plus anything they link to).
    3. Export each page to PDF (D-04 — PDF preserves structure + visuals) and place the files under .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/confluence/ (create the folder).
    4. Reply "confluence provided" and list the PDF files added and the source page URLs, OR "confluence unavailable" if access cannot be granted this phase (the survey will then record Confluence as an un-surveyed source gap for Plan 04).
  </how-to-verify>
  <resume-signal>Type "confluence provided" (list PDFs + URLs) or "confluence unavailable"</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Inventory the Confluence SCE guides and extract the root-cause taxonomy</name>
  <read_first>
    - every PDF under .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/confluence/ (list the folder first)
    - .planning/phases/01-knowledge-survey-conflict-inventory/GLOSSARY.md
    - .planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md
    - 2026-05-28-meeting-note.md (B4: tag root-cause per SCE Confluence guide)
  </read_first>
  <action>
    Create SURVEY-confluence.md with "## Confluence SCE Inventory": a table (Page title, Source URL, Format=PDF, Snapshot path, Last-update cadence, Notes) listing every Confluence page provided. Add "## Root-cause taxonomy": transcribe the root-cause classification categories the SCE guides define and, for each, note which workflow macro-flow / Level-In category it relates to (link back to the Coverage Map backbone in SURVEY.md). Add "## Threshold & jargon cross-references": for any numeric/temporal threshold found in Confluence, note its value and whether it agrees with or contradicts the matching row in POLICY-THRESHOLD-INDEX.md (record agreement/contradiction as a finding for Plan 04 — do not edit POLICY-THRESHOLD-INDEX.md, which Plan 01 owns; record the cross-reference here). For any GLOSSARY.md term marked TBD that Confluence defines authoritatively, record the resolved definition + source here so Plan 04 / the reviewer can update GLOSSARY.md.

    If the human replied "confluence unavailable": instead create SURVEY-confluence.md recording the Confluence SCE source as NOT surveyed this phase, with the reason, and flag it as a high-priority source gap and CS-team action item for Plan 04. This is a legitimate, explicit gap — do not fabricate taxonomy.
  </action>
  <verify>
    <automated>test -f ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY-confluence.md" && grep -qi "Confluence SCE Inventory\|NOT surveyed\|not surveyed\|unavailable" ".planning/phases/01-knowledge-survey-conflict-inventory/SURVEY-confluence.md"</automated>
  </verify>
  <acceptance_criteria>
    - If guides were provided: SURVEY-confluence.md inventories every Confluence PDF with format PDF and snapshot path; a root-cause taxonomy section maps categories to macro-flows/Level-In; threshold and jargon cross-references record agreement/contradiction vs Plan 01's index/glossary.
    - If unavailable: SURVEY-confluence.md explicitly records Confluence as an un-surveyed source gap with a CS-team action item, and no taxonomy is fabricated.
    - The file does not edit Plan 01-owned files; it records cross-reference findings for Plan 04 to reconcile.
  </acceptance_criteria>
  <done>The Confluence SCE source is either fully inventoried with its root-cause taxonomy and threshold/jargon cross-refs, or explicitly recorded as a source gap — never silently absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Confluence SCE pages -> repo PDFs | SCE guides are internal classification logic. They may contain example tickets or screenshots with customer data. |
| Exported PDFs -> git | PDFs are committed to the repo; any embedded customer PII would be persisted in version control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-PII | Information Disclosure | Confluence PDF snapshots committed to repo | mitigate | SCE guides may embed real example tickets/screenshots with customer names, emails, addresses, order IDs. Before committing any Confluence PDF, the human/executor must confirm it contains policy/taxonomy content only; redact or exclude pages containing real customer data (aligns with the project Presidio-redaction constraint). Prefer exporting policy pages over example-laden ones. |
| T-01-03-ACC | Tampering / accuracy | Root-cause taxonomy transcription | accept | Taxonomy is transcribed from the PDFs; ambiguous items are flagged as findings, not guessed. |
| T-01-03-SC | Tampering | Package installs | accept | Docs-only; no installs. |
</threat_model>

<verification>
- SURVEY-confluence.md exists and either inventories provided PDFs or records an explicit source gap.
- Threshold/jargon cross-references are recorded for Plan 04 without editing Plan 01-owned files.
- No customer PII committed in Confluence PDF snapshots (policy/taxonomy pages only).
</verification>

<success_criteria>
- The Confluence SCE source family is inventoried (criterion 1) with its root-cause taxonomy captured, OR recorded as an explicit, action-itemized source gap (criterion 4).
- Threshold and jargon findings are staged for Plan 04's cross-source conflict detection and glossary resolution.
</success_criteria>

<output>
Create `.planning/phases/01-knowledge-survey-conflict-inventory/01-03-SUMMARY.md` when done.
</output>
