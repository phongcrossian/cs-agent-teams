---
phase: 02-freshdesk-i-o-layer-pipeline-backbone
plan: 06
type: execute
wave: 3
depends_on: ["02-04", "02-05"]
files_modified:
  - src/queue/dead_letter.py
  - src/queue/worker.py
  - src/observability.py
  - src/main.py
  - tests/test_queue.py
  - tests/test_e2e_sandbox.py
autonomous: false
requirements: [REP-05]

must_haves:
  truths:
    - "Lỗi transient (429/5xx/timeout) retry với bounded backoff (~5 lần) honor Retry-After; cạn lượt → đẩy dead_letter + fire alert (crit #3)"
    - "Lỗi fatal (404/403/401/400) đi thẳng dead_letter, không retry vô ích"
    - "main.py khởi động đồng thời webhook server + poller + sequential worker thành một process chạy được"
    - "Sandbox e2e demo: post canned reply thật vào ticket sandbox (live-send) và re-run KHÔNG tạo reply thứ 2 (D-03, REP-05 crit#2)"
  artifacts:
    - path: "src/queue/dead_letter.py"
      provides: "to_dead_letter(conn, row, error) + alert emit; should_dead_letter classify"
      contains: "dead_letter"
    - path: "src/main.py"
      provides: "Entry point: webhook (uvicorn) + poller_loop + worker_loop concurrent"
      contains: "worker_loop"
    - path: "src/observability.py"
      provides: "structlog setup + metric counters (processed, suppressed, dead_lettered, retries)"
      contains: "structlog"
    - path: "tests/test_e2e_sandbox.py"
      provides: "Sandbox smoke: real reply + retry-no-double-send (marker sandbox)"
      contains: "sandbox"
  key_links:
    - from: "src/queue/worker.py"
      to: "src/queue/dead_letter.py"
      via: "exhausted/fatal → to_dead_letter + alert"
      pattern: "dead_letter|to_dead_letter"
    - from: "src/main.py"
      to: "src/queue/worker.py + src/poller/reconcile.py + src/webhook/receiver.py"
      via: "asyncio gather webhook server + poller + worker"
      pattern: "worker_loop|poller_loop"
---

<objective>
Hoàn thiện hardening và chứng minh giá trị end-to-end — slice cuối ghép retry/dead-letter (D-10) vào worker, gom mọi thành phần thành một entry point chạy được (main.py), rồi chạy demo thật trên Freshdesk sandbox (D-03) chứng minh post + exactly-once trên Freshdesk thật. Đây là slice "value-proving" của cả phase.

Mục đích: Hiện thực criterion #3 (rate-limit honor + dead-letter cho human thay vì drop âm thầm) qua D-10 (bounded backoff + jitter, Retry-After, dead-letter + alert; transient vs fatal classification). Wire observability tối thiểu (D-12 structured logs + metrics, không Langfuse). Chứng minh REP-05 crit #2 trên sandbox thật (D-03).

Output: dead_letter (classify + alert), worker retry/DLQ wiring, observability, main.py, sandbox e2e demo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-VALIDATION.md
@src/config.py

<interfaces>
<!-- Từ plan 02/03/04/05. Dùng trực tiếp. -->
src/queue/claim.py: claim_one, finalize_done, finalize_retry(conn,row_id,token,error,backoff_seconds), recover_stale_claims.
src/queue/worker.py: process_queue_row(pool, client, row, settings), worker_loop(pool, client, settings).
src/queue/send.py: send_reply(client, conn, ticket_id, inbound_msg_id, body, mode).
src/poller/reconcile.py: poller_loop(client, pool, settings).
src/webhook/receiver.py: app (FastAPI).
src/freshdesk_io/errors.py: FreshdeskRateLimitError(retry_after), FreshdeskTransientError, FreshdeskFatalError.
src/freshdesk_io/rate_limit.py: classify_status(status_code) -> "transient"|"fatal".
Bảng dead_letter (từ 02-01): idempotency_key, ticket_id, inbound_msg_id, payload, attempts, last_error, alerted.

Error classification (RESEARCH § Error Classification): 429/500/502/503/504/timeout → transient (retry); 401/403/404/400 → fatal (dead-letter ngay). max_attempts=5 (Settings).
Backoff: exponential base 1s cap 60s + jitter, honor Retry-After khi RateLimitError.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dead-letter + alert + retry classification wired vào worker (D-10)</name>
  <files>src/queue/dead_letter.py, src/queue/worker.py, src/observability.py, tests/test_queue.py</files>
  <behavior>
    - test_transient_retries_then_dead_letter: send raise FreshdeskTransientError 5 lần → row retry với backoff tăng dần rồi đẩy dead_letter sau cạn max_attempts; alert flag set.
    - test_fatal_straight_to_dead_letter: send raise FreshdeskFatalError (404) → row vào dead_letter NGAY (attempts không tăng tới 5).
    - test_retry_after_honored: FreshdeskRateLimitError(retry_after=3) → next_attempt_at ~= NOW()+3s (không exponential mù).
    - test_suppressed_not_dead_letter: suppressed inbound (từ plan 04) KHÔNG bao giờ vào dead_letter (D-08 vs crit#3 phân biệt).
  </behavior>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ httpx + tenacity Error Classification; § Postgres Queue Pattern — Exhaustion → dead-letter SQL; Pitfall 6 Retry-After)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-10 bounded backoff + alert, honor Retry-After, transient vs fatal; D-08 suppressed KHÔNG dead-letter; D-12 structured logs+metrics)
    - src/queue/claim.py, src/queue/worker.py (interfaces ở trên)
    - tests/test_queue.py
  </read_first>
  <action>
    Tạo observability.py: cấu hình structlog JSON renderer; metric counters đơn giản (in-memory dict hoặc structlog event): processed_total, suppressed_total, dead_lettered_total, retries_total. Hàm `emit_alert(event: str, **kw)` log ở level warning/error (alert qua log/metric — đủ cho Phase 2; notify/dashboard ở Phase 6). KHÔNG log PII (D-12).
    Tạo dead_letter.py:
      - `should_dead_letter(exc) -> bool`: True nếu FreshdeskFatalError (đi thẳng); False nếu transient (để retry).
      - `to_dead_letter(conn, row, error: str) -> None`: INSERT vào dead_letter (SELECT từ ticket_queue), UPDATE ticket_queue status='dead_lettered'; set alerted=true; gọi emit_alert("dead_letter", ticket_id=row.ticket_id, attempts=row.attempts) — error đã redact, không PII.
    Sửa worker.process_queue_row try/except (thay hook tạm ở plan 04 bằng wiring thật):
      - FreshdeskFatalError hoặc should_dead_letter → to_dead_letter ngay.
      - FreshdeskRateLimitError → finalize_retry với backoff = retry_after (honor Retry-After, Pitfall 6).
      - FreshdeskTransientError/timeout → finalize_retry với exponential backoff+jitter (base 1s cap 60s); nếu attempts+1 >= max_attempts → to_dead_letter + alert.
      - Increment metric counters tương ứng.
    Biến 4 test RED→GREEN.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -m pytest "tests/test_queue.py::test_transient_retries_then_dead_letter" "tests/test_queue.py::test_fatal_straight_to_dead_letter" "tests/test_queue.py::test_retry_after_honored" "tests/test_queue.py::test_suppressed_not_dead_letter" -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Transient error retry bounded (~max_attempts) rồi dead_letter + alert (crit #3 — không drop âm thầm).
    - Fatal error (404/403/401/400) → dead_letter ngay, không retry phí.
    - Retry-After được honor (next_attempt_at ~= NOW()+retry_after).
    - Suppressed inbound KHÔNG vào dead_letter (D-08); dead_letter chỉ cho processing failure thật (crit #3).
    - emit_alert + metric counters không log PII raw.
  </acceptance_criteria>
  <done>4 dead-letter/retry test xanh; `pytest tests/test_queue.py -x -q` xanh toàn bộ.</done>
</task>

<task type="auto">
  <name>Task 2: main.py entry point — webhook + poller + worker concurrent + stale-recovery scheduler</name>
  <files>src/main.py, tests/test_queue.py</files>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Recommended Project Structure — main.py: start webhook server + poller + worker; § Architecture Patterns diagram)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-09 webhook+poller, D-11 single sequential worker, D-05 send-mode từ config)
    - src/webhook/receiver.py, src/poller/reconcile.py, src/queue/worker.py, src/queue/claim.py (recover_stale_claims)
  </read_first>
  <action>
    Tạo main.py: tạo asyncpg pool + FreshdeskClient từ Settings; chạy đồng thời (asyncio.gather hoặc uvicorn programmatic + background tasks):
      - uvicorn serve webhook.app (FastAPI) trên host/port từ Settings.
      - poller_loop(client, pool, settings) (cadence band 5–15 min).
      - worker_loop(pool, client, settings) (single sequential — D-11).
      - scheduler định kỳ gọi recover_stale_claims(conn) (~mỗi 10 phút).
    Graceful shutdown (uvicorn lifespan / signal handler) đóng pool + client. Log startup ghi send_mode hiện tại (dry_run/live) để vận hành thấy rõ — KHÔNG log secret.
    Thêm test_main_wires_components (smoke, không network): import main, assert build_app/assemble trả về các coroutine/đối tượng mong đợi (webhook app, worker_loop callable) mà không thực sự chạy server.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -c "import src.main as m; assert hasattr(m, 'main') or hasattr(m, 'run'), 'main entry missing'; print('main importable')" && python -m pytest "tests/test_queue.py::test_main_wires_components" -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - main.py import được; expose entry point (main/run) khởi động webhook + poller + worker + stale-recovery.
    - Startup log ghi send_mode hiện tại; không log secret/PII.
    - test_main_wires_components xanh (smoke, không bind port thật).
  </acceptance_criteria>
  <done>main importable + test_main_wires_components xanh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Sandbox e2e demo (D-03) — real post + retry-không-double-send</name>
  <what-built>
    Toàn bộ rails đã sẵn sàng: webhook → queue → worker → FreshdeskClient.post_reply, với loop-guard, idempotency, retry/dead-letter, send-mode switch. tests/test_e2e_sandbox.py chứa 2 test (marked `sandbox`, skip trong CI) gọi Freshdesk sandbox thật: test_sandbox_real_reply (post canned reply vào ticket test) + test_sandbox_retry_no_double_send (chạy lại cùng inbound → KHÔNG post lần 2).
    Các test sandbox đã được hoàn thiện (assertions thật) trong task này, dùng FreshdeskClient + queue thật trỏ vào sandbox account + test DB.
  </what-built>
  <how-to-verify>
    1. Đảm bảo .env có FRESHDESK_DOMAIN + FRESHDESK_API_KEY của SANDBOX account (không phải production) và DATABASE_URL của test/dev DB. Tạo/ghi nhận một ticket test trên sandbox (ghi ticket_id).
    2. Flip send-mode sang live: set SEND_MODE=live trong .env (D-05 — mặc định dry_run để an toàn dev).
    3. Chạy: `RUN_SANDBOX=1 pytest tests/test_e2e_sandbox.py -m sandbox -x -q` (hoặc lệnh tương đương trong README).
    4. Mở ticket test trên Freshdesk sandbox UI: xác nhận canned reply public xuất hiện đúng trong conversation history của ĐÚNG ticket.
    5. Xác nhận test_sandbox_retry_no_double_send pass: chạy lại cùng inbound (cùng idempotency_key) KHÔNG tạo reply thứ 2 trên ticket (kiểm conversation count không tăng).
    6. Trả send-mode về dry_run (SEND_MODE=dry_run) sau demo để tránh live-send ngoài ý muốn.
  </how-to-verify>
  <resume-signal>Gõ "sandbox demo OK" (kèm ticket_id đã test + xác nhận không có reply trùng) HOẶC mô tả lỗi gặp phải.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Freshdesk (live-send) | Lần đầu gọi Freshdesk thật; send-mode mặc định dry_run chống live-send ngoài ý |
| dead_letter → human | Lỗi xử lý không drop âm thầm; alert tới human |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | DoS (runaway/stuck queue) | retry/dead-letter | mitigate | Bounded retries (max_attempts) + Retry-After honor; cạn lượt → dead_letter, không infinite loop (D-10, crit#3) |
| T-02-20 | Tampering (accidental live-send) | send-mode | mitigate | SEND_MODE mặc định dry_run; demo phải chủ động flip live rồi trả về dry_run (D-05) |
| T-02-21 | Tampering (duplicate-send) | sandbox e2e | mitigate | test_sandbox_retry_no_double_send chứng minh idempotency trên Freshdesk thật (REP-05 crit#2, D-03) |
| T-02-22 | Information Disclosure | dead_letter.last_error / alert | mitigate | error redact trước persist/alert; không log raw ticket body/PII (D-12) |
| T-02-SC | Tampering | dependency state | mitigate | Không cài package mới ở plan này; toàn bộ deps đã audit [OK] ở plan 01 |
</threat_model>

<verification>
- `pytest tests/ -x --ignore=tests/test_e2e_sandbox.py -q` xanh (full CI suite).
- Transient retry bounded → dead_letter + alert; fatal → dead_letter ngay; Retry-After honored; suppressed không dead_letter.
- main.py import + wires webhook/poller/worker.
- Checkpoint Task 3: sandbox demo post thật + retry-không-double-send xác nhận thủ công (D-03).
</verification>

<success_criteria>
- Criterion #3: rate-limit honored (Retry-After), repeated failures → dead-letter cho human, không drop âm thầm.
- D-10: bounded backoff+jitter, transient vs fatal classification, dead-letter + alert on exhaustion.
- main.py chạy webhook + poller + sequential worker + stale-recovery thành một process (D-09/D-11).
- D-03: sandbox e2e demo chứng minh real post + exactly-once (REP-05 crit#2) trên Freshdesk thật.
- Observability tối thiểu (structlog + metrics, không PII) — D-12.
</success_criteria>

<output>
Create `.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-06-SUMMARY.md` when done
</output>
