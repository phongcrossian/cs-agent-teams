---
phase: 02-freshdesk-i-o-layer-pipeline-backbone
plan: 04
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - src/guards/__init__.py
  - src/guards/loop_guard.py
  - src/guards/pii.py
  - src/work_queue/worker.py
  - src/work_queue/send.py
  - src/work_queue/dead_letter_sink.py
  - tests/test_loop_guard.py
  - tests/test_queue.py
autonomous: false
requirements: [REP-05]

must_haves:
  truths:
    - "Auto-reply mail (Auto-Submitted/Precedence/List-*), no-reply/mailer-daemon sender, agent/AI reply (incoming=false), và Selless-sync update đều bị loop-guard chặn → không bao giờ trigger send (crit #4)"
    - "should_suppress là single source of truth: resolve step (02-05) và worker re-check gọi CÙNG hàm; worker re-check suppress một row đã enqueue hợp lệ → status='stale_inbound' + alert (observable), KHÔNG silent drop (fix #4)"
    - "Per-ticket reply throttle độc lập: không gửi quá N AI reply tới cùng ticket trong T phút (loop-breaker phòng khi RFC 3834 header layer inert — fix #4)"
    - "Inbound bị suppress được mark processed (không re-pick) + log/metric, KHÔNG vào dead-letter (D-08)"
    - "PII trong ticket body bị Presidio redact TRƯỚC mọi log/persist (D-12)"
    - "Worker ghi send-intent transactional (sent_at + freshdesk_reply_id) khi POST 200; re-claim một row đã có sent_at → SKIP post, đi thẳng finalize_done (exactly-once across crash — fix #1, REP-05)"
    - "Pre-send existence guard: trước live post_reply, worker scan recent conversations cho system marker của inbound này → nếu đã post thì SKIP (đóng residual window giữa POST success và sent_at write — fix #1)"
  artifacts:
    - path: "src/guards/loop_guard.py"
      provides: "should_suppress(conv, headers, from_email) qua 4 signal layer (D-06) — single source of truth"
      contains: "Auto-Submitted"
    - path: "src/guards/pii.py"
      provides: "redact_text(text) dùng Presidio trước log/persist"
      contains: "redact"
    - path: "src/work_queue/send.py"
      provides: "send_reply honor SendMode: dry_run → dry_run_log, live → pre-send guard + client.post_reply + persist sent_at/freshdesk_reply_id (D-05, fix #1)"
      contains: "dry_run"
    - path: "src/work_queue/dead_letter_sink.py"
      provides: "DeadLetterSink protocol + RetryOnlyDeadLetterSink default impl (fix #7)"
      contains: "DeadLetterSink"
    - path: "src/work_queue/worker.py"
      provides: "process_queue_row: skip-if-sent → fetch conversations → should_suppress → throttle → redact → send → persist sent_at → finalize"
      contains: "claim_one"
  key_links:
    - from: "src/work_queue/worker.py"
      to: "src/guards/loop_guard.py"
      via: "should_suppress check (single source of truth); re-check suppress của row hợp lệ → stale_inbound + alert"
      pattern: "should_suppress|stale_inbound"
    - from: "src/work_queue/worker.py"
      to: "src/guards/pii.py"
      via: "redact ticket body trước persist/log"
      pattern: "redact"
    - from: "src/work_queue/send.py"
      to: "src/freshdesk_io/client.py"
      via: "live-send gọi FreshdeskClient.post_reply sau pre-send guard; persist sent_at + freshdesk_reply_id"
      pattern: "post_reply|sent_at"
---

<objective>
Ghép post-path thành một worker hoàn chỉnh và bọc nó bằng các guard an toàn — slice biến "client + queue rời rạc" thành "inbound → xử lý → post (hoặc suppress) exactly-once". Đây là nơi 4 layer loop-guard (D-06/D-07), PII redaction (D-12), send-mode switch (D-05), và **send-intent transactional + pre-send guard (fix review #1 — true exactly-once across crash)** hợp nhất quanh client (plan 02) + queue (plan 03).

Mục đích: Hiện thực criterion #4 (auto-reply/no-reply/sync-echo không bao giờ gửi) + D-08 (suppress = skip+log, không dead-letter) + D-12 (redact trước log/persist) + seam D-05 dry-run/live + **đóng crash-window giữa POST 200 và finalize_done** (fix #1). Loop-guard hợp nhất về MỘT điểm đánh giá (should_suppress) + observable stale_inbound + per-ticket throttle (fix #4). Plan này có một checkpoint thủ công để verify D-07 trên sandbox (research đánh dấu LOW confidence).

MODULE: queue logic ở `src/work_queue/` (KHÔNG `src/queue/` — fix #10). SQL tham chiếu schema `queue`.

Output: loop_guard (4 layer, single source of truth), pii.redact_text, send.send_reply (mode-aware + send-intent), dead_letter_sink (protocol), worker.process_queue_row ghép tất cả.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md
@src/config.py

<interfaces>
<!-- Từ plan 02 (freshdesk_io) + plan 03 (work_queue). Dùng trực tiếp. -->
src/freshdesk_io/client.py: FreshdeskClient.post_reply(ticket_id, body)->ReplyResult, post_note(...), get_conversations(ticket_id)->list[Conversation], list_updated_tickets(since)->list[Ticket].
src/freshdesk_io/models.py: Conversation(id, incoming, private, user_id, from_email, source, body_text), ReplyResult(id, ticket_id).
src/work_queue/claim.py: claim_one(conn, worker_id), finalize_done(conn, row_id, token), finalize_retry(conn, row_id, token, error, backoff_seconds).
src/work_queue/enqueue.py: enqueue_ticket(conn, ticket_id, inbound_msg_id, redacted_payload).
src/config.py: Settings.send_mode (SendMode.DRY_RUN|LIVE), selless_sync_user_ids: set[int], per_ticket_reply_throttle_n, per_ticket_reply_throttle_window_minutes.
queue.ticket_queue có cột sent_at TIMESTAMPTZ NULL + freshdesk_reply_id BIGINT NULL (02-01) — send-intent transactional.

Loop-guard signals (RESEARCH § Loop/Auto-Reply Guard D-06):
  Layer1 headers: Auto-Submitted!=no, Precedence in {bulk,list,junk,auto_reply}, X-Auto-Response-Suppress, List-* headers, empty Return-Path.
  Layer2 sender regex: ^(no[._-]?reply|noreply|mailer-daemon|postmaster|bounce[+-]|do-not-reply)@
  Layer3 source/actor: incoming=false → skip (agent/AI/system); private=true → skip.
  Layer4 Selless sync: conv.user_id in selless_sync_user_ids → skip.
Presidio (RESEARCH § PII Redaction): AnalyzerEngine + AnonymizerEngine, REDACT_ENTITIES (PERSON,EMAIL_ADDRESS,PHONE_NUMBER,CREDIT_CARD,US_SSN,LOCATION,IP_ADDRESS,URL), language="en".
SendMode switch (RESEARCH Code Example Pattern 3): DRY_RUN → INSERT queue.dry_run_log; LIVE → pre-send guard + client.post_reply.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loop-guard 4 signal layers (D-06, single source of truth) + per-ticket throttle + Presidio PII redaction (D-12)</name>
  <files>src/guards/loop_guard.py, src/guards/pii.py, src/guards/__init__.py, tests/test_loop_guard.py</files>
  <behavior>
    - test_rfc3834_headers: header {Auto-Submitted: auto-replied} → suppress; {Precedence: bulk} → suppress; List-Id present → suppress; normal headers → không suppress.
    - test_sender_pattern_noreply: from_email "noreply@shop.com"/"mailer-daemon@x" → suppress; "jane@gmail.com" → không.
    - test_source_actor_incoming_false: conv incoming=false → suppress; private=true → suppress; incoming=true & public → không.
    - test_selless_sync_user_id: conv.user_id in selless_sync_user_ids → suppress (D-07 primary path).
    - test_resolve_uses_should_suppress: helper resolve dùng để quyết định "real inbound customer message" gọi CÙNG should_suppress (single source of truth) — không inline điều kiện riêng (fix #4; đảm bảo resolve + worker không drift).
    - test_loop_guard_throttle: should_throttle_ticket(conn, ticket_id, n, window_minutes) trả True khi đã có >= N sent reply (sent_at trong window) cho ticket → loop-breaker độc lập (fix #4).
    - test_redact_pii: redact_text("email me at jane@x.com or 555-123-4567") không còn chứa "jane@x.com" và số phone raw.
  </behavior>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Loop/Auto-Reply Guard — 4 layer code; § PII Redaction với Presidio; Pitfall 5 spaCy model; Pitfall 3 incoming=false; § Assumptions Log A1,A2,A4)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-06 4 signal layers, D-07 source/actor + fallback marker/tag, D-08 suppress=skip+log không dead-letter, D-12 redact trước log/persist)
    - CLAUDE.md (§ "What NOT to Use" — never log raw ticket text; Presidio PII rule)
    - tests/test_loop_guard.py (scaffold từ 02-01)
  </read_first>
  <action>
    Tạo loop_guard.py: `should_suppress(conv: Conversation, headers: dict | None = None, from_email: str | None = None, selless_sync_user_ids: set[int] = frozenset()) -> tuple[bool, str]` trả (suppress, reason). Đây là SINGLE SOURCE OF TRUTH (fix #4): cả resolve step (02-05) và worker re-check gọi hàm này — KHÔNG nơi nào inline điều kiện `incoming/private` riêng. Áp dụng 4 layer theo thứ tự, trả reason layer đầu khớp:
      Layer1 `is_auto_reply_by_headers` (Auto-Submitted!=no, Precedence in {bulk,list,junk,auto_reply}, X-Auto-Response-Suppress chứa DR/AUTOREPLY/ALL, bất kỳ List-* header, Return-Path rỗng/<>); chỉ áp dụng nếu headers có sẵn (A4: raw headers có thể không expose — degrade gracefully).
      Layer2 `is_auto_reply_by_sender` regex NO_REPLY_PATTERN.
      Layer3 incoming==false hoặc private==true → suppress (chỉ process customer public reply, per Pitfall 3 scope).
      Layer4 conv.user_id in selless_sync_user_ids → suppress (D-07 primary). Để chỗ rõ ràng (comment + hàm `is_selless_sync`) cho fallback marker/tag nếu checkpoint Task 3 phát hiện source/actor không phân biệt được.
    THÊM `async should_throttle_ticket(conn, ticket_id: int, n: int, window_minutes: int) -> bool` (fix #4 — loop-breaker độc lập): COUNT các row queue.ticket_queue có cùng ticket_id AND sent_at >= NOW() - window → True nếu >= n. Đây là loop-breaker KHÔNG phụ thuộc sender classification, phòng khi RFC 3834 header layer inert (RESEARCH A4); bảo vệ criterion #4 ngay cả khi D-06 layer 1 không hoạt động.
    Tạo pii.py: khởi tạo AnalyzerEngine + AnonymizerEngine lazy (singleton, vì load spaCy model tốn thời gian); `redact_text(text: str) -> str` analyze REDACT_ENTITIES language="en" → anonymize → trả text với PII thay bằng entity tag; text rỗng → trả nguyên.
    Biến 7 test (4 loop-guard + resolve-uses-should-suppress + throttle + redact_pii) RED→GREEN.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -m pytest tests/test_loop_guard.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - should_suppress trả (True, reason) cho cả 4 layer; (False, "") cho customer public reply hợp lệ; là single source of truth (resolve + worker cùng gọi — test_resolve_uses_should_suppress).
    - should_throttle_ticket trả True khi >= N reply đã sent trong window (loop-breaker độc lập — fix #4).
    - Layer1 degrade gracefully khi headers=None (không crash; dựa layer 2-4 — A4).
    - redact_text loại bỏ email + phone raw khỏi output (Presidio chạy với en_core_web_lg).
    - is_selless_sync tách riêng để dễ swap sang marker/tag fallback (D-07).
  </acceptance_criteria>
  <done>`pytest tests/test_loop_guard.py -x -q` xanh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: send.send_reply (mode-aware D-05 + send-intent fix #1) + DeadLetterSink protocol (fix #7) + worker.process_queue_row ghép toàn slice</name>
  <files>src/work_queue/send.py, src/work_queue/dead_letter_sink.py, src/work_queue/worker.py, tests/test_queue.py</files>
  <behavior>
    - test_send_dry_run: send_mode=DRY_RUN → ghi 1 row vào queue.dry_run_log (gồm inbound_msg_id + action='reply'), KHÔNG gọi client (respx call count 0).
    - test_send_live: send_mode=LIVE → pre-send guard (scan conversations, chưa có marker) → gọi client.post_reply (respx mock 200) → persist sent_at + freshdesk_reply_id, trả ReplyResult.
    - test_worker_suppressed_path: conv loop-guard suppress (non-customer enqueue hiếm) → row mark 'suppressed' + KHÔNG vào dead_letter + KHÔNG gọi send (D-08).
    - test_worker_recheck_routes_stale_inbound: row enqueue hợp lệ nhưng tại worker re-check should_suppress=True (state đổi) → status='stale_inbound' + emit_alert, KHÔNG silent 'suppressed', KHÔNG dead_letter (fix #4 — dropped reply observable).
    - test_worker_happy_path_exactly_once: claim → fetch conversations (mock latest incoming) → redact → send (dry-run) → finalize done; re-run cùng inbound KHÔNG tạo send thứ 2.
    - test_worker_crash_after_post_does_not_resend: live send POST 200 thành công + persist sent_at, NHƯNG finalize_done raise (mô phỏng crash) → row vẫn 'claimed' → recover_stale_claims → re-claim → process_queue_row thấy sent_at IS NOT NULL → SKIP post (respx POST count vẫn == 1) → đi thẳng finalize_done (fix #1, REP-05 exactly-once).
  </behavior>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Code Examples Pattern 2 worker loop, Pattern 3 send-mode; Pitfall 1 conversation body phải fetch; Pitfall 2 race — đã giải quyết tại resolve step của 02-05)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-05 dry-run default + seam Phase6/7, D-08 suppress action, D-11 single sequential worker, D-12 redact trước persist)
    - src/work_queue/claim.py, src/work_queue/enqueue.py, src/freshdesk_io/client.py (interfaces ở trên)
    - tests/test_queue.py (đã có dedup/claim test từ plan 03; thêm worker test ở đây)
  </read_first>
  <action>
    DOC-DRIFT NOTE (fix review #10): dry_run_log insert PHẢI theo schema 02-01 (ticket_id, inbound_msg_id, action, body) — KHÔNG theo snippet RESEARCH cũ chỉ có (ticket_id, body, created_at). Executor follow plan, không follow snippet.

    Tạo dead_letter_sink.py (fix review #7): định nghĩa `DeadLetterSink` Protocol (typing.Protocol) với method `async def to_dead_letter(conn, row, error: str) -> None`. Cung cấp default impl `RetryOnlyDeadLetterSink` (no-op cho fatal → để worker finalize_retry tới max trong Wave 2 nếu chưa có sink thật) VÀ một `RaisingDeadLetterSink` tùy chọn. Worker NHẬN sink qua dependency injection (tham số), KHÔNG dùng "if to_dead_letter exists" stringly-typed hook. Plan 06 sẽ inject impl thật (PostgresDeadLetterSink). Đảm bảo fatal-404 path có nơi đi rõ ràng ngay từ Wave 2 (qua sink được inject).

    Tạo send.py: `async send_reply(client, conn, ticket_id, inbound_msg_id, body, mode: SendMode, row_id, claim_token) -> dict`.
      - DRY_RUN → INSERT queue.dry_run_log (ticket_id, inbound_msg_id, action='reply', body=redact_text(body)), trả {"dry_run": True}.
      - LIVE (fix #1 — send-intent + pre-send guard):
        1. PRE-SEND GUARD: await client.get_conversations(ticket_id); nếu đã tồn tại reply do hệ thống post cho inbound này (phát hiện qua system marker — tái dùng ý tưởng D-07 marker: stamp một marker cố định vào body outbound, scan marker + tham chiếu inbound_msg_id) → coi như đã gửi, SKIP post, trả {"skipped": "already_sent"}. Đóng residual window giữa POST success và sent_at write.
        2. result = await client.post_reply(ticket_id, body_with_marker).
        3. Ngay sau POST 200: UPDATE queue.ticket_queue SET sent_at=NOW(), freshdesk_reply_id=result.id WHERE id=row_id AND claim_token=claim_token (transactional send-intent). Trả result.
      Hàm này LÀ seam cho Phase 6 kill-switch / Phase 7 rollout (comment ghi rõ — không phải dev flag tạm).
    Tạo worker.py: `async process_queue_row(pool, client, row, settings, dead_letter_sink)`:
      1. SKIP-IF-SENT (fix #1): nếu row.sent_at IS NOT NULL → đã post thành công ở claim trước (crash trước finalize) → đi thẳng finalize_done(conn, row.id, row.claim_token), return (KHÔNG post lại).
      2. await client.get_conversations(row.ticket_id) để lấy conversation body (Pitfall 1) và re-check loop-guard. KHÔNG re-key: row.inbound_msg_id ĐÃ là id thật của latest inbound (resolve-then-enqueue tại 02-05 — D-02). Chọn conversation có id == row.inbound_msg_id (hoặc latest incoming nếu cần body).
      3. should_suppress(conv, headers, from_email, settings.selless_sync_user_ids) — CÙNG hàm như resolve (single source of truth). Nếu suppress: vì row này đã enqueue như một inbound hợp lệ tại resolve → state đã đổi giữa enqueue và process → UPDATE status='stale_inbound' + emit_alert("stale_inbound_dropped", ticket_id=...) + metric, finalize KHÔNG dead-letter (D-08 distinction giữ nguyên: suppressed≠dead_letter; stale_inbound là trạng thái observable riêng — fix #4), return. (Nếu một non-customer lọt qua resolve thì cùng path; điểm mấu chốt là KHÔNG silent drop.)
      4. THROTTLE (fix #4): nếu await should_throttle_ticket(conn, row.ticket_id, settings.per_ticket_reply_throttle_n, settings.per_ticket_reply_throttle_window_minutes) → suppress với reason='throttled' → status='stale_inbound' (hoặc 'suppressed' với reason throttled) + alert, return (loop-breaker).
      5. redact body (D-12) trước bất kỳ log/persist.
      6. canned_body = placeholder reply (Phase 4 thay bằng draft thật — seam rõ ràng).
      7. await send_reply(..., mode=settings.send_mode, row_id=row.id, claim_token=row.claim_token); finalize_done(conn, row.id, row.claim_token).
      Bọc try/except: transient error → finalize_retry với backoff; fatal (FreshdeskFatalError, gồm 409) → dead_letter_sink.to_dead_letter(conn, row, redacted_error) (wiring impl thật ở plan 06; ở đây dùng sink được inject — fix #7).
    Cung cấp `worker_loop(pool, client, settings, dead_letter_sink)` sequential (D-11): claim_one → process_queue_row → sleep khi idle.
    Biến 6 test RED→GREEN (dùng respx cho client, db_pool cho queue).
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -m pytest "tests/test_queue.py::test_send_dry_run" "tests/test_queue.py::test_send_live" "tests/test_queue.py::test_worker_suppressed_path" "tests/test_queue.py::test_worker_recheck_routes_stale_inbound" "tests/test_queue.py::test_worker_happy_path_exactly_once" "tests/test_queue.py::test_worker_crash_after_post_does_not_resend" -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DRY_RUN không gọi Freshdesk (respx 0 call) + persist queue.dry_run_log với inbound_msg_id + action + body đã redact.
    - LIVE: pre-send guard scan marker trước post; sau POST 200 persist sent_at + freshdesk_reply_id (fix #1).
    - test_worker_crash_after_post_does_not_resend: re-claim row có sent_at → SKIP post (respx POST count == 1) → finalize_done (fix #1, true exactly-once across crash).
    - test_worker_recheck_routes_stale_inbound: worker re-check suppress row hợp lệ → status='stale_inbound' + alert, KHÔNG silent suppressed, KHÔNG dead_letter (fix #4 — dropped reply observable).
    - Throttle: >= N reply trong window → loop-breaker chặn send thứ N+1 (fix #4).
    - Suppressed (non-customer) inbound → status='suppressed', không dead_letter, không send (D-08).
    - Happy path post exactly-once; body redact (D-12) trước persist.
    - DeadLetterSink inject qua tham số (fix #7); fatal path gọi sink.to_dead_letter, không stringly-typed hook.
  </acceptance_criteria>
  <done>6 worker/send test xanh; `pytest tests/test_queue.py -x -q` (các test thuộc plan này) xanh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify D-07 trên Freshdesk sandbox — source/actor có phân biệt Selless-sync update không + raw header exposure + 409 semantic</name>
  <wave_note>
    **KHÔNG hard-block Wave 3.** Code path source/actor (is_selless_sync user_id whitelist) ĐÃ ship làm DEFAULT trong Task 1 và là layer 4 hoạt động đầy đủ ngay cả khi checkpoint chưa chạy. Per-ticket throttle (fix #4) là loop-breaker độc lập đã ship, nên ngay cả khi raw RFC 3834 header không expose (A4) loop-guard vẫn có backstop. Checkpoint này CONFIRM giả định (A2/A4, LOW confidence) trên sandbox thật + nạp user_id thực vào config. Nếu human response chậm, 02-06 (Wave 3) vẫn execute được; marker/tag fallback (deferred CONTEXT idea) là FOLLOW-UP qua gap-closure — KHÔNG phải prerequisite của Wave 3.
  </wave_note>
  <what-built>
    Loop-guard layer 4 (is_selless_sync) dựa trên giả định (RESEARCH LOW confidence, A2): Selless-sync-originated update mang một Freshdesk user_id cố định có thể whitelist. Code để hàm is_selless_sync tách riêng + comment seam cho fallback marker/tag. Layer 4 + per-ticket throttle là DEFAULT đang chạy; checkpoint confirm + nạp user_id thật, đồng thời confirm raw-header exposure (A4) và 409 semantic (fix #5).
  </what-built>
  <how-to-verify>
    1. Trên Freshdesk sandbox: kích hoạt một Selless→Freshdesk sync update lên một ticket test (theo cách Selless tạo update — hỏi user nếu cần thao tác).
    2. Gọi `GET /api/v2/tickets/{id}/conversations` cho ticket đó (có thể chạy: `RUN_SANDBOX=1 python -c "..."` dùng FreshdeskClient.get_conversations, hoặc curl với API key).
    3. Ghi lại `user_id`, `from_email`, `incoming`, `source` của conversation entry do sync tạo ra; ĐỒNG THỜI ghi lại liệu raw RFC 3834 headers (Auto-Submitted/Precedence/List-*) có expose qua API không (A4 — quyết định layer 1 thật hay inert).
    4. (fix #5) Trên sandbox, thử tái tạo một 409 từ `POST /reply` nếu có thể; ghi lại body/semantic. Tới khi observe được semantic an toàn, 409 vẫn classify fatal→dead-letter (đã set ở 02-02).
    5. Quyết định:
       - NẾU sync update có user_id cố định/phân biệt được → thêm user_id vào config SELLESS_SYNC_USER_IDS; layer 4 hoạt động như thiết kế. Xác nhận "D-07 primary path OK".
       - NẾU KHÔNG phân biệt được → kích hoạt fallback marker/tag (deferred idea CONTEXT): stamp private marker/tag lên outbound do hệ thống tạo, check ở layer 4. Báo "D-07 fallback activated" để plan 06 hoặc gap-closure xử lý.
  </how-to-verify>
  <resume-signal>Gõ "D-07 primary OK" (kèm user_id + ghi chú raw-header exposure + 409 observation) HOẶC "D-07 fallback marker/tag" (kèm mô tả marker đã chọn).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body (attacker-controllable email) → worker | Nội dung email là dữ liệu, không bao giờ là lệnh; redact PII trước log/persist |
| Selless-sync update → loop-guard | Sync echo có thể giả dạng customer reply → phải phân biệt (D-07) để tránh reply loop |
| POST success → DB finalize (dual-write) | Crash giữa POST 200 và finalize_done → send-intent (sent_at) + pre-send guard chống resend (fix #1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | DoS (auto-reply loop) | loop_guard | mitigate | 4 signal layer (RFC3834 + sender + incoming/private + Selless-sync) + per-ticket reply throttle độc lập (fix #4) chặn không gửi cho non-customer + chặn runaway loop ngay cả khi layer 1 inert (crit #4) |
| T-02-12 | Information Disclosure | worker log/persist | mitigate | Presidio redact_text trước MỌI log/persist (D-12); dry_run_log lưu body đã redact |
| T-02-13 | Spoofing (sync echo) | loop_guard layer 4 | mitigate | is_selless_sync user_id whitelist; fallback marker/tag nếu sandbox verify (Task 3) cho thấy không phân biệt được |
| T-02-14 | Tampering (reply injection) | worker | mitigate | ticket body chỉ được redact + dùng làm dữ liệu; Phase 2 chỉ post canned reply, không eval nội dung |
| T-02-23 | Tampering (duplicate-send across crash) | send-intent / pre-send guard | mitigate | sent_at + freshdesk_reply_id persist transactional; re-claim row đã sent → SKIP post; pre-send marker scan đóng residual window (fix #1, REP-05) |
| T-02-24 | Information Disclosure (silent drop) | worker re-check suppress | mitigate | Row hợp lệ bị suppress ở worker → status='stale_inbound' + alert (observable), không silent drop một reply khách hàng thật (fix #4) |
</threat_model>

<verification>
- `pytest tests/test_loop_guard.py tests/test_queue.py -x -q` (các test thuộc plan này) xanh.
- Loop-guard chặn cả 4 layer (single source of truth) + throttle loop-breaker; suppressed không vào dead-letter; stale_inbound observable.
- redact_text loại PII; dry-run không gọi Freshdesk; happy path exactly-once; crash-after-post KHÔNG resend (respx POST count==1).
- Checkpoint Task 3: D-07 + raw-header + 409 verify trên sandbox.
</verification>

<success_criteria>
- Criterion #4 thoả: auto-reply/no-reply/sync-echo không bao giờ trigger send; per-ticket throttle là loop-breaker độc lập.
- D-08: suppress = mark processed + log/metric, KHÔNG dead-letter; stale_inbound là trạng thái observable riêng cho dropped-valid-reply (fix #4).
- D-12: PII redact trước log/persist.
- D-05: send-mode dry-run (default) persist would-be action; live gọi Freshdesk — seam cho Phase 6/7.
- fix #1: send-intent transactional (sent_at/freshdesk_reply_id) + pre-send guard → true exactly-once across crash (REP-05).
- fix #7: DeadLetterSink protocol inject → fatal path có nơi đi rõ ràng từ Wave 2.
- D-07 verified trên sandbox (primary user_id whitelist hoặc fallback marker/tag).
</success_criteria>

<output>
Create `.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-04-SUMMARY.md` when done
</output>
