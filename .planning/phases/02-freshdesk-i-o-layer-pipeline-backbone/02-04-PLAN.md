---
phase: 02-freshdesk-i-o-layer-pipeline-backbone
plan: 04
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - src/guards/__init__.py
  - src/guards/loop_guard.py
  - src/guards/pii.py
  - src/queue/worker.py
  - src/queue/send.py
  - tests/test_loop_guard.py
autonomous: false
requirements: [REP-05]

must_haves:
  truths:
    - "Auto-reply mail (Auto-Submitted/Precedence/List-*), no-reply/mailer-daemon sender, agent/AI reply (incoming=false), và Selless-sync update đều bị loop-guard chặn → không bao giờ trigger send (crit #4)"
    - "Inbound bị suppress được mark processed (không re-pick) + log/metric, KHÔNG vào dead-letter (D-08)"
    - "PII trong ticket body bị Presidio redact TRƯỚC mọi log/persist (D-12)"
    - "Worker ghép client + queue + guard + send-mode: dry-run persist would-be action, live-send gọi Freshdesk; canned reply posted exactly-once"
  artifacts:
    - path: "src/guards/loop_guard.py"
      provides: "should_suppress(conv, headers, from_email) qua 4 signal layer (D-06)"
      contains: "Auto-Submitted"
    - path: "src/guards/pii.py"
      provides: "redact_text(text) dùng Presidio trước log/persist"
      contains: "redact"
    - path: "src/queue/send.py"
      provides: "send_reply honor SendMode: dry_run → dry_run_log, live → client.post_reply (D-05)"
      contains: "dry_run"
    - path: "src/queue/worker.py"
      provides: "process_queue_row: fetch conversations → loop-guard → redact → send → finalize"
      contains: "claim_one"
  key_links:
    - from: "src/queue/worker.py"
      to: "src/guards/loop_guard.py"
      via: "suppress check trước khi send; suppressed → mark processed + metric"
      pattern: "should_suppress|suppress"
    - from: "src/queue/worker.py"
      to: "src/guards/pii.py"
      via: "redact ticket body trước persist/log"
      pattern: "redact"
    - from: "src/queue/send.py"
      to: "src/freshdesk_io/client.py"
      via: "live-send gọi FreshdeskClient.post_reply"
      pattern: "post_reply"
---

<objective>
Ghép post-path thành một worker hoàn chỉnh và bọc nó bằng các guard an toàn — slice biến "client + queue rời rạc" thành "inbound → xử lý → post (hoặc suppress) exactly-once". Đây là nơi 4 layer loop-guard (D-06/D-07), PII redaction (D-12), và send-mode switch (D-05) hợp nhất quanh client (plan 02) + queue (plan 03).

Mục đích: Hiện thực criterion #4 (auto-reply/no-reply/sync-echo không bao giờ gửi) + D-08 (suppress = skip+log, không dead-letter) + D-12 (redact trước log/persist) + seam D-05 dry-run/live. Plan này có một checkpoint thủ công để verify D-07 trên sandbox (research đánh dấu LOW confidence).

Output: loop_guard (4 layer), pii.redact_text, send.send_reply (mode-aware), worker.process_queue_row ghép tất cả.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md
@src/config.py

<interfaces>
<!-- Từ plan 02 (freshdesk_io) + plan 03 (queue). Dùng trực tiếp. -->
src/freshdesk_io/client.py: FreshdeskClient.post_reply(ticket_id, body)->ReplyResult, post_note(...), get_conversations(ticket_id)->list[Conversation].
src/freshdesk_io/models.py: Conversation(id, incoming, private, user_id, from_email, source, body_text).
src/queue/claim.py: claim_one(conn, worker_id), finalize_done(conn, row_id, token), finalize_retry(conn, row_id, token, error, backoff_seconds).
src/queue/enqueue.py: enqueue_ticket(conn, ticket_id, inbound_msg_id, redacted_payload).
src/config.py: Settings.send_mode (SendMode.DRY_RUN|LIVE), selless_sync_user_ids: set[int].

Loop-guard signals (RESEARCH § Loop/Auto-Reply Guard D-06):
  Layer1 headers: Auto-Submitted!=no, Precedence in {bulk,list,junk,auto_reply}, X-Auto-Response-Suppress, List-* headers, empty Return-Path.
  Layer2 sender regex: ^(no[._-]?reply|noreply|mailer-daemon|postmaster|bounce[+-]|do-not-reply)@
  Layer3 source/actor: incoming=false → skip (agent/AI/system); private=true → skip.
  Layer4 Selless sync: conv.user_id in selless_sync_user_ids → skip.
Presidio (RESEARCH § PII Redaction): AnalyzerEngine + AnonymizerEngine, REDACT_ENTITIES (PERSON,EMAIL_ADDRESS,PHONE_NUMBER,CREDIT_CARD,US_SSN,LOCATION,IP_ADDRESS,URL), language="en".
SendMode switch (RESEARCH Code Example Pattern 3): DRY_RUN → INSERT dry_run_log; LIVE → client.post_reply.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loop-guard 4 signal layers (D-06) + Presidio PII redaction (D-12)</name>
  <files>src/guards/loop_guard.py, src/guards/pii.py, src/guards/__init__.py, tests/test_loop_guard.py</files>
  <behavior>
    - test_rfc3834_headers: header {Auto-Submitted: auto-replied} → suppress; {Precedence: bulk} → suppress; List-Id present → suppress; normal headers → không suppress.
    - test_sender_pattern_noreply: from_email "noreply@shop.com"/"mailer-daemon@x" → suppress; "jane@gmail.com" → không.
    - test_source_actor_incoming_false: conv incoming=false → suppress; private=true → suppress; incoming=true & public → không.
    - test_selless_sync_user_id: conv.user_id in selless_sync_user_ids → suppress (D-07 primary path).
    - test_redact_pii: redact_text("email me at jane@x.com or 555-123-4567") không còn chứa "jane@x.com" và số phone raw.
  </behavior>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Loop/Auto-Reply Guard — 4 layer code; § PII Redaction với Presidio; Pitfall 5 spaCy model; Pitfall 3 incoming=false; § Assumptions Log A1,A2,A4)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-06 4 signal layers, D-07 source/actor + fallback marker/tag, D-08 suppress=skip+log không dead-letter, D-12 redact trước log/persist)
    - CLAUDE.md (§ "What NOT to Use" — never log raw ticket text; Presidio PII rule)
    - tests/test_loop_guard.py (scaffold từ 02-01)
  </read_first>
  <action>
    Tạo loop_guard.py: `should_suppress(conv: Conversation, headers: dict | None = None, from_email: str | None = None, selless_sync_user_ids: set[int] = frozenset()) -> tuple[bool, str]` trả (suppress, reason). Áp dụng 4 layer theo thứ tự, trả reason layer đầu khớp:
      Layer1 `is_auto_reply_by_headers` (Auto-Submitted!=no, Precedence in {bulk,list,junk,auto_reply}, X-Auto-Response-Suppress chứa DR/AUTOREPLY/ALL, bất kỳ List-* header, Return-Path rỗng/<>); chỉ áp dụng nếu headers có sẵn (A4: raw headers có thể không expose — degrade gracefully).
      Layer2 `is_auto_reply_by_sender` regex NO_REPLY_PATTERN.
      Layer3 incoming==false hoặc private==true → suppress (chỉ process customer public reply, per Pitfall 3 scope).
      Layer4 conv.user_id in selless_sync_user_ids → suppress (D-07 primary). Để chỗ rõ ràng (comment + hàm `is_selless_sync`) cho fallback marker/tag nếu checkpoint Task 3 phát hiện source/actor không phân biệt được.
    Tạo pii.py: khởi tạo AnalyzerEngine + AnonymizerEngine lazy (singleton, vì load spaCy model tốn thời gian); `redact_text(text: str) -> str` analyze REDACT_ENTITIES language="en" → anonymize → trả text với PII thay bằng entity tag; text rỗng → trả nguyên.
    Biến 5 test (4 loop-guard + redact_pii) RED→GREEN.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -m pytest tests/test_loop_guard.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - should_suppress trả (True, reason) cho cả 4 layer; (False, "") cho customer public reply hợp lệ.
    - Layer1 degrade gracefully khi headers=None (không crash; dựa layer 2-4 — A4).
    - redact_text loại bỏ email + phone raw khỏi output (Presidio chạy với en_core_web_lg).
    - is_selless_sync tách riêng để dễ swap sang marker/tag fallback (D-07).
  </acceptance_criteria>
  <done>`pytest tests/test_loop_guard.py -x -q` xanh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: send.send_reply (mode-aware D-05) + worker.process_queue_row ghép toàn slice</name>
  <files>src/queue/send.py, src/queue/worker.py, tests/test_queue.py</files>
  <behavior>
    - test_send_dry_run: send_mode=DRY_RUN → ghi 1 row vào dry_run_log, KHÔNG gọi client (respx call count 0).
    - test_send_live: send_mode=LIVE → gọi client.post_reply (respx mock 200), trả ReplyResult.
    - test_worker_suppressed_path: conv loop-guard suppress → row mark 'suppressed' + KHÔNG vào dead_letter + KHÔNG gọi send (D-08).
    - test_worker_happy_path_exactly_once: claim → fetch conversations (mock latest incoming) → redact → send (dry-run) → finalize done; re-run cùng inbound KHÔNG tạo send thứ 2.
  </behavior>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Code Examples Pattern 2 worker loop, Pattern 3 send-mode; § FastAPI Webhook — two-step idempotency key: worker fetch conversations → latest incoming → compute key; Pitfall 1 webhook không có body; Pitfall 2 race)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-05 dry-run default + seam Phase6/7, D-08 suppress action, D-11 single sequential worker, D-12 redact trước persist)
    - src/queue/claim.py, src/queue/enqueue.py, src/freshdesk_io/client.py (interfaces ở trên)
    - tests/test_queue.py (đã có dedup/claim test từ plan 03; thêm worker test ở đây)
  </read_first>
  <action>
    Tạo send.py: `async send_reply(client, conn, ticket_id, inbound_msg_id, body, mode: SendMode) -> dict`. DRY_RUN → INSERT dry_run_log (ticket_id, inbound_msg_id, action='reply', body=redact_text(body)) , trả {"dry_run": True}. LIVE → await client.post_reply(ticket_id, body). Hàm này LÀ seam cho Phase 6 kill-switch / Phase 7 rollout (comment ghi rõ — không phải dev flag tạm).
    Tạo worker.py: `async process_queue_row(pool, client, row, settings)`:
      1. await client.get_conversations(row.ticket_id); chọn latest conversation có incoming=true (two-step idempotency key — Pitfall 2). Nếu inbound_msg_id của row khác latest → dùng row.inbound_msg_id (key đã cố định lúc enqueue).
      2. should_suppress(conv, headers, from_email, settings.selless_sync_user_ids) → nếu suppress: UPDATE row status='suppressed', log + metric (suppression reason), finalize KHÔNG dead-letter (D-08), return.
      3. redact body (D-12) trước bất kỳ log/persist.
      4. canned_body = placeholder reply (Phase 4 thay bằng draft thật — để seam rõ ràng).
      5. await send_reply(..., mode=settings.send_mode); finalize_done(conn, row.id, row.claim_token).
      Bọc try/except: transient error → finalize_retry với backoff; fatal → đẩy dead-letter (wiring đầy đủ ở plan 06; ở đây gọi hook `to_dead_letter` nếu đã tồn tại, else finalize_retry tới max).
    Cung cấp `worker_loop(pool, client, settings)` sequential (D-11): claim_one → process_queue_row → sleep khi idle.
    Biến 4 test RED→GREEN (dùng respx cho client, db_pool cho queue).
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -m pytest "tests/test_queue.py::test_send_dry_run" "tests/test_queue.py::test_send_live" "tests/test_queue.py::test_worker_suppressed_path" "tests/test_queue.py::test_worker_happy_path_exactly_once" -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DRY_RUN không gọi Freshdesk (respx 0 call) + persist dry_run_log với body đã redact.
    - LIVE gọi client.post_reply.
    - Suppressed inbound → status='suppressed', không dead_letter, không send (D-08).
    - Happy path post exactly-once; re-run cùng inbound không tạo send thứ 2 (REP-05).
    - body redact (D-12) trước persist vào dry_run_log / log.
  </acceptance_criteria>
  <done>4 worker/send test xanh; `pytest tests/test_queue.py -x -q` xanh toàn bộ.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify D-07 trên Freshdesk sandbox — source/actor có phân biệt Selless-sync update không</name>
  <what-built>
    Loop-guard layer 4 (is_selless_sync) hiện dựa trên giả định (RESEARCH LOW confidence, A2): Selless-sync-originated update mang một Freshdesk user_id cố định có thể whitelist. Code đã để hàm is_selless_sync tách riêng + comment seam cho fallback marker/tag.
  </what-built>
  <how-to-verify>
    1. Trên Freshdesk sandbox: kích hoạt một Selless→Freshdesk sync update lên một ticket test (theo cách Selless tạo update — hỏi user nếu cần thao tác).
    2. Gọi `GET /api/v2/tickets/{id}/conversations` cho ticket đó (có thể chạy: `RUN_SANDBOX=1 python -c "..."` dùng FreshdeskClient.get_conversations, hoặc curl với API key).
    3. Ghi lại `user_id`, `from_email`, `incoming`, `source` của conversation entry do sync tạo ra.
    4. Quyết định:
       - NẾU sync update có user_id cố định/phân biệt được → thêm user_id đó vào config SELLESS_SYNC_USER_IDS; layer 4 hoạt động như thiết kế. Xác nhận "D-07 primary path OK".
       - NẾU KHÔNG phân biệt được (sync tạo incoming=true không có actor riêng) → kích hoạt fallback marker/tag (deferred idea trong CONTEXT): stamp một private marker/tag lên outbound do hệ thống tạo và check nó ở layer 4. Báo "D-07 fallback activated" để plan 06 hoặc gap-closure xử lý.
  </how-to-verify>
  <resume-signal>Gõ "D-07 primary OK" (kèm user_id để thêm vào config) HOẶC "D-07 fallback marker/tag" (kèm mô tả marker đã chọn).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body (attacker-controllable email) → worker | Nội dung email là dữ liệu, không bao giờ là lệnh; redact PII trước log/persist |
| Selless-sync update → loop-guard | Sync echo có thể giả dạng customer reply → phải phân biệt (D-07) để tránh reply loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | DoS (auto-reply loop) | loop_guard | mitigate | 4 signal layer (RFC3834 + sender + incoming/private + Selless-sync) chặn không gửi cho non-customer (crit #4) |
| T-02-12 | Information Disclosure | worker log/persist | mitigate | Presidio redact_text trước MỌI log/persist (D-12); dry_run_log lưu body đã redact |
| T-02-13 | Spoofing (sync echo) | loop_guard layer 4 | mitigate | is_selless_sync user_id whitelist; fallback marker/tag nếu sandbox verify (Task 3) cho thấy không phân biệt được |
| T-02-14 | Tampering (reply injection) | worker | mitigate | ticket body chỉ được redact + dùng làm dữ liệu; Phase 2 chỉ post canned reply, không eval nội dung |
</threat_model>

<verification>
- `pytest tests/test_loop_guard.py tests/test_queue.py -x -q` xanh.
- Loop-guard chặn cả 4 layer; suppressed không vào dead-letter.
- redact_text loại PII; dry-run không gọi Freshdesk; happy path exactly-once.
- Checkpoint Task 3: D-07 đã verify trên sandbox (primary hoặc fallback).
</verification>

<success_criteria>
- Criterion #4 thoả: auto-reply/no-reply/sync-echo không bao giờ trigger send.
- D-08: suppress = mark processed + log/metric, KHÔNG dead-letter.
- D-12: PII redact trước log/persist.
- D-05: send-mode dry-run (default) persist would-be action; live gọi Freshdesk — seam cho Phase 6/7.
- D-07 verified trên sandbox (primary user_id whitelist hoặc fallback marker/tag).
</success_criteria>

<output>
Create `.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-04-SUMMARY.md` when done
</output>
