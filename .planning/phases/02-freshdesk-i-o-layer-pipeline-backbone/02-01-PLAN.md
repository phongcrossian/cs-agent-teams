---
phase: 02-freshdesk-i-o-layer-pipeline-backbone
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - docker-compose.yml
  - .env.example
  - alembic.ini
  - migrations/env.py
  - migrations/versions/0001_initial_queue_schema.py
  - src/__init__.py
  - src/config.py
  - tests/conftest.py
  - tests/test_client.py
  - tests/test_queue.py
  - tests/test_webhook.py
  - tests/test_loop_guard.py
  - tests/test_poller.py
  - tests/test_e2e_sandbox.py
  - README.md
autonomous: true
requirements: [REP-05]

must_haves:
  truths:
    - "Chạy `pytest tests/ -q` thực thi được (test scaffolds tồn tại, fail có chủ đích chứ không phải import error)"
    - "Postgres 16 chạy được qua docker-compose và Alembic migrate tạo (trong schema `queue`) bảng ticket_queue + dead_letter + dry_run_log + poller_checkpoint"
    - "ticket_queue có cột sent_at TIMESTAMPTZ + freshdesk_reply_id BIGINT (send-intent transactional cho exactly-once REP-05)"
    - "Presidio AnalyzerEngine khởi tạo được (spaCy en_core_web_lg đã cài)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Khai báo dependency + cấu hình pytest (markers: sandbox)"
      contains: "presidio-analyzer"
    - path: "docker-compose.yml"
      provides: "Postgres 16 pgvector-ready cho dev/test"
      contains: "postgres"
    - path: "migrations/versions/0001_initial_queue_schema.py"
      provides: "Schema `queue`: ticket_queue (+sent_at,+freshdesk_reply_id), dead_letter, dry_run_log, poller_checkpoint"
      contains: "poller_checkpoint"
    - path: "tests/conftest.py"
      provides: "asyncpg pool fixtures + respx mock fixtures + test DB"
      contains: "asyncpg"
    - path: "src/config.py"
      provides: "Settings: send_mode (default dry_run), DB URL, Freshdesk API key, webhook secret"
      contains: "dry_run"
  key_links:
    - from: "migrations/env.py"
      to: "src/config.py"
      via: "đọc DATABASE_URL từ Settings"
      pattern: "config|DATABASE_URL"
---

<objective>
Bootstrap dự án Python greenfield và toàn bộ hạ tầng test cho Phase 2. Đây là Wave 0 bắt buộc: repo hiện chỉ có planning docs, chưa có code nào.

Mục đích: Tạo nền tảng (uv project, Postgres qua Docker, Alembic schema cho queue/dead-letter/poller-checkpoint, test scaffolding với respx + asyncpg, Presidio backend) để các plan feature (Wave 1+) chạy được ngay. Mọi test file được tạo dưới dạng scaffold fail-có-chủ-đích (không phải import error) để các plan sau biến RED → GREEN.

Output: pyproject.toml, docker-compose.yml, Alembic migration đầu tiên, src/config.py, và 7 test file scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv project scaffold + dependency declarations + pytest config</name>
  <files>pyproject.toml, .python-version, .env.example, README.md, src/__init__.py</files>
  <read_first>
    - CLAUDE.md (§ "Recommended Stack" core technologies + § "What NOT to Use" — MCP SDK v2 cấm)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Standard Stack — danh sách version đã verify trên PyPI; § Package Legitimacy Audit — 10 package [OK]; § Environment Availability — uv chưa có trong PATH, fallback pip+venv)
  </read_first>
  <action>
    Khởi tạo Python project bằng `uv`. Nếu `uv` không có trong PATH (xem RESEARCH § Environment Availability), cài qua `curl -LsSf https://astral.sh/uv/install.sh | sh` hoặc fallback `pip install uv`; ghi cách đã dùng vào README.
    Tạo pyproject.toml khai báo runtime deps đúng version đã verify: fastapi==0.136.3, uvicorn==0.48.0, httpx==0.28.1, tenacity==9.1.4, asyncpg==0.31.0, sqlalchemy==2.0.50, alembic==1.18.4, pydantic==2.13.4, pydantic-settings (cho Settings), presidio-analyzer==2.2.359, presidio-anonymizer==2.2.362, python-dotenv==1.2.2, structlog==25.5.0. Dev deps: pytest==9.0.3, pytest-asyncio, respx==0.23.1.
    Trong pyproject.toml thêm `[tool.pytest.ini_options]` với `asyncio_mode = "auto"` và đăng ký marker `sandbox` (dùng cho test e2e thật) qua `markers = ["sandbox: real Freshdesk sandbox smoke tests (skipped in CI)"]`.
    Set `.python-version` = 3.14. Tạo .env.example liệt kê key (không có giá trị thật): DATABASE_URL, FRESHDESK_DOMAIN, FRESHDESK_API_KEY, WEBHOOK_SECRET, SEND_MODE, SELLESS_SYNC_USER_IDS, PER_TICKET_REPLY_THROTTLE_N (default 1), PER_TICKET_REPLY_THROTTLE_WINDOW_MINUTES (default 30). Tạo src/__init__.py rỗng. README ghi lệnh setup + lệnh test nhanh `pytest tests/ -x --ignore=tests/test_e2e_sandbox.py -q`.
    Cài spaCy model Presidio cần: `python -m spacy download en_core_web_lg` (per RESEARCH Pitfall 5). Ghi lệnh này vào README setup.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=str(d); assert 'presidio-analyzer' in deps and 'tenacity' in deps and 'fastapi' in deps, 'missing core deps'; print('pyproject OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml parse được và chứa tất cả 12 runtime deps + 3 dev deps đúng version RESEARCH § Standard Stack.
    - `[tool.pytest.ini_options]` đăng ký marker `sandbox` và set `asyncio_mode = "auto"`.
    - .env.example liệt kê đủ các key (bao gồm PER_TICKET_REPLY_THROTTLE_N + window); KHÔNG chứa giá trị secret thật (CLAUDE.md: API key không commit).
    - README ghi rõ lệnh `python -m spacy download en_core_web_lg`.
  </acceptance_criteria>
  <done>pyproject.toml + .env.example + README tồn tại; lệnh verify in "pyproject OK".</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose Postgres 16 + Alembic schema (schema `queue`: ticket_queue, dead_letter, dry_run_log, poller_checkpoint)</name>
  <files>docker-compose.yml, alembic.ini, migrations/env.py, migrations/versions/0001_initial_queue_schema.py, src/config.py</files>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Postgres Queue Pattern — schema thiết kế đầy đủ: ticket_queue, dead_letter, indexes; § Recommended Project Structure)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-01 Postgres queue, D-05 dry-run, D-10 dead-letter)
    - CLAUDE.md (§ Stack — Postgres 16/17 + pgvector 0.8.x; Phase 3 mở rộng cùng DB này)
  </read_first>
  <action>
    Tạo docker-compose.yml định nghĩa service `postgres` dùng image `pgvector/pgvector:pg16` (pgvector-ready cho Phase 3, per CONTEXT D-01 integration point), expose 5432, env POSTGRES_USER/PASSWORD/DB, named volume cho persistence, healthcheck `pg_isready`. Vì Docker chưa có trong PATH (RESEARCH § Environment Availability), README hướng dẫn cài Docker Desktop hoặc dùng Postgres local; docker-compose.yml vẫn là nguồn chuẩn.
    Tạo src/config.py dùng pydantic-settings `Settings` đọc env: database_url, freshdesk_domain, freshdesk_api_key, webhook_secret, send_mode (Enum SendMode {DRY_RUN="dry_run", LIVE="live"}, default DRY_RUN — per D-05), selless_sync_user_ids (set[int], parse từ CSV env), retry max_attempts (default 5), poller_interval_seconds, per_ticket_reply_throttle_n (default 1), per_ticket_reply_throttle_window_minutes (default 30). KHÔNG log giá trị api_key/webhook_secret (CLAUDE.md PII/secret rule).
    Khởi tạo Alembic (`alembic init migrations` rồi sửa). migrations/env.py đọc DATABASE_URL từ src/config.Settings (không hardcode). Viết migration 0001 — TẤT CẢ bảng đặt trong schema `queue` (fix review #6 — Phase-3 Postgres co-location: queue tables tách namespace khỏi `public` nơi Phase 3 pgvector sẽ sống; tạo `CREATE SCHEMA IF NOT EXISTS queue` ở đầu migration). Theo RESEARCH § Postgres Queue Pattern + các cột bổ sung dưới đây:
      - queue.ticket_queue (id BIGSERIAL PK, idempotency_key TEXT, ticket_id INT, inbound_msg_id INT, payload JSONB, status TEXT default 'pending' — domain: pending|claimed|done|suppressed|stale_inbound|dead_lettered, claimed_at, claimed_by, claim_token UUID, attempts INT default 0, max_attempts INT default 5, next_attempt_at TIMESTAMPTZ default NOW(), last_error, last_error_at, **sent_at TIMESTAMPTZ NULL, freshdesk_reply_id BIGINT NULL**, created_at, updated_at) + UNIQUE INDEX trên idempotency_key + partial index (status,next_attempt_at) WHERE status='pending'. Cột `sent_at`/`freshdesk_reply_id` (fix review #1) ghi send-intent transactional: re-claim một row đã có sent_at IS NOT NULL → SKIP post, đi thẳng finalize_done (idempotent completion → đóng crash-window giữa POST 200 và finalize_done).
      - queue.dead_letter (id, idempotency_key, ticket_id, inbound_msg_id, payload JSONB, attempts, last_error, created_at, alerted BOOLEAN default false).
      - queue.dry_run_log (id BIGSERIAL PK, ticket_id INT, inbound_msg_id INT, action TEXT, body TEXT, created_at TIMESTAMPTZ default NOW()) — nơi dry-run persist would-be action per D-05. (LƯU Ý cho executor: insert luôn gồm inbound_msg_id + action — KHÔNG theo snippet RESEARCH cũ chỉ có ticket_id/body; xem 02-04 doc-drift note.)
      - queue.poller_checkpoint (id INT PK CHECK (id=1) — single-row state, last_since TIMESTAMPTZ, updated_at TIMESTAMPTZ default NOW()) — fix review #3: poller window durable; persist last_since sau mỗi reconcile_once, resume khi restart với safety overlap (last_since − một window). Seed một row id=1 với last_since = NOW() − safety_window khi migrate (hoặc để poller upsert lần đầu).
    Ghi chú design (fix review #6) vào docstring migration HOẶC README: Phase 3 pgvector sẽ dùng schema `public` (mặc định); quyết định chia sẻ pool hay tách pool/`statement_timeout` riêng được đẩy sang Phase 3 — schema `queue` riêng biến lock-in "một schema" ngầm thành lựa chọn có chủ đích trong khuôn khổ D-01.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -c "import ast,sys; src=open('src/config.py').read(); t=ast.parse(src); assert 'dry_run' in src and 'send_mode' in src, 'config missing send_mode/dry_run'; m=open('migrations/versions/0001_initial_queue_schema.py').read(); assert 'ticket_queue' in m and 'dead_letter' in m and 'dry_run_log' in m and 'poller_checkpoint' in m and 'idempotency_key' in m and 'sent_at' in m and 'freshdesk_reply_id' in m and 'queue' in m, 'migration missing tables/columns/schema'; print('schema+config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml dùng image pgvector/pgvector:pg16, có healthcheck pg_isready và named volume.
    - src/config.py định nghĩa SendMode Enum với default DRY_RUN; selless_sync_user_ids parse được từ env; có per_ticket_reply_throttle_n + window.
    - Migration 0001 tạo schema `queue` + cả 4 bảng (ticket_queue, dead_letter, dry_run_log, poller_checkpoint) với UNIQUE INDEX trên ticket_queue.idempotency_key (dedup at insert — D-02).
    - ticket_queue có cột sent_at TIMESTAMPTZ NULL + freshdesk_reply_id BIGINT NULL (send-intent transactional — REP-05 exactly-once, fix #1).
    - poller_checkpoint là single-row state lưu last_since (fix #3); migrations/env.py đọc DATABASE_URL từ Settings, không hardcode.
  </acceptance_criteria>
  <done>Lệnh verify in "schema+config OK"; 4 bảng trong schema `queue` với unique idempotency index + sent_at/freshdesk_reply_id + poller_checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Test scaffolding — conftest + 7 test files (RED-có-chủ-đích, không import error)</name>
  <files>tests/conftest.py, tests/test_client.py, tests/test_queue.py, tests/test_webhook.py, tests/test_poller.py, tests/test_loop_guard.py, tests/test_e2e_sandbox.py</files>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-VALIDATION.md (§ Wave 0 Requirements — danh sách test file + nội dung; § Per-Task Verification Map; § Manual-Only Verifications — sandbox + D-07)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ Validation Architecture — Phase Requirements → Test Map; Code Examples cho enqueue/worker/send-mode)
  </read_first>
  <action>
    Tạo tests/conftest.py: fixture `db_pool` (asyncpg.create_pool tới DATABASE_URL test), fixture `clean_db` (truncate queue.ticket_queue/queue.dead_letter/queue.dry_run_log + reset queue.poller_checkpoint giữa các test), fixture `respx_mock` cho mock httpx tới Freshdesk base_url. Dùng pytest-asyncio. Thêm `pytest_collection_modifyitems` skip test marker `sandbox` trừ khi env `RUN_SANDBOX=1` (để CI bỏ qua, per VALIDATION § Manual-Only).
    Tạo 7 test file dưới dạng scaffold đúng theo VALIDATION § Per-Task Verification Map. MỖI test phải IMPORT được (không ImportError) nhưng fail-có-chủ-đích bằng `pytest.fail("Wave 1: implement <X>")` hoặc assert chống stub — KHÔNG dùng `pytest.skip`. Tạo module placeholder tối thiểu mà test import (ví dụ `src/freshdesk_io/__init__.py`, `src/work_queue/__init__.py`, `src/webhook/__init__.py`, `src/poller/__init__.py`, `src/guards/__init__.py`) chỉ chứa stub signature `raise NotImplementedError` để import thành công (LƯU Ý: module là `src/work_queue/` — KHÔNG `src/queue/` — tránh shadow stdlib `queue`, fix review #10):
      - test_client.py: test_post_reply_success, test_post_note, test_retry_after (REP-05 rate limit), test_fatal_404_no_retry, **test_list_updated_tickets** (fix #2 — poller dependency: list_updated_tickets(since) trả list[Ticket]).
      - test_queue.py: test_enqueue_dedup, test_idempotency (retry không double-send — REP-05 crit#2), test_skip_locked_claim, test_dead_letter_on_exhaustion, test_stale_claim_recovery, **test_worker_crash_after_post_does_not_resend** (fix #1 — crash giữa POST 200 và finalize_done → re-claim KHÔNG post lần 2; respx POST count == 1), **test_loop_guard_throttle** (fix #4 — per-ticket reply throttle: không gửi quá N reply tới cùng ticket trong T phút), **test_sweep_exhausted_unlettered** (fix #9 — row status='pending' AND attempts>=max_attempts được sweeper đẩy dead_letter).
      - test_webhook.py: test_hmac_verify_valid, test_hmac_verify_rejects_bad_sig, test_enqueue_on_webhook.
      - test_poller.py: test_poller_enqueues_updated, test_poller_dedup_with_webhook (cùng resolved key → exactly-once), test_poller_advances_window (D-09 reconciliation), **test_poller_window_persists_across_restart** (fix #3 — last_since persist vào queue.poller_checkpoint, restart resume từ đó với safety overlap).
      - test_loop_guard.py: test_rfc3834_headers, test_sender_pattern_noreply, test_source_actor_incoming_false, test_selless_sync_user_id (D-06/D-07), **test_resolve_uses_should_suppress** (fix #4 — resolve step gọi CÙNG should_suppress, single source of truth), **test_worker_recheck_routes_stale_inbound** (fix #4 — worker re-check suppress một row đã enqueue hợp lệ → status='stale_inbound' + alert, KHÔNG silent suppressed).
      - test_e2e_sandbox.py: test_sandbox_real_reply + test_sandbox_retry_no_double_send, cả hai `@pytest.mark.sandbox` (D-03).
    Đảm bảo `pytest tests/ -q` CHẠY tới giai đoạn run (collect thành công, không lỗi collection).
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -m pytest tests/ --collect-only -q 2>&1 | grep -v '^#' | grep -Eq "test_worker_crash_after_post_does_not_resend" && python -m pytest tests/ --collect-only -q 2>&1 | grep -v '^#' | grep -Eq "test_poller_window_persists_across_restart" && python -m pytest tests/ --collect-only -q 2>&1 | grep -v '^#' | grep -Eq "test_list_updated_tickets" && echo "scaffolds collect OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ --collect-only` thành công (không ImportError / collection error) và liệt kê test_worker_crash_after_post_does_not_resend, test_poller_window_persists_across_restart, test_list_updated_tickets (3 RED test bắt buộc của revision).
    - Mọi test fail bằng pytest.fail / NotImplementedError assert — KHÔNG dùng pytest.skip (trừ marker sandbox được skip qua conftest khi RUN_SANDBOX chưa set).
    - conftest.py có fixture asyncpg pool + respx mock + skip-sandbox-unless-RUN_SANDBOX; clean_db reset cả poller_checkpoint.
    - Module placeholder (freshdesk_io, work_queue, webhook, poller, guards) import được; module là src/work_queue/ KHÔNG src/queue/.
  </acceptance_criteria>
  <done>Lệnh verify in "scaffolds collect OK"; 7 test file + conftest tồn tại, collect không lỗi, gồm 3 RED test mới của revision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → Postgres container | Local credentials; không expose ra ngoài |
| env/.env → process | Secrets (API key, webhook secret) chỉ qua env, không commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .env / config.py | mitigate | API key + webhook secret chỉ trong .env (gitignored); .env.example chỉ chứa key rỗng; config.py không log secret values |
| T-02-02 | Tampering | DB schema/migration | mitigate | Alembic migration versioned; unique index idempotency_key chặn duplicate row ở DB level; schema `queue` cô lập khỏi `public` (Phase 3) |
| T-02-SC | Tampering | uv/pip installs | mitigate | Tất cả 10 package core đã qua slopcheck [OK] trong RESEARCH § Package Legitimacy Audit; không có [ASSUMED]/[SUS]/[SLOP] → không cần checkpoint |
</threat_model>

<verification>
- `pytest tests/ --collect-only -q` thành công, không collection error; gồm 3 RED test mới (crash-after-post, poller-window-persist, list_updated_tickets).
- pyproject.toml parse được với đủ deps.
- Migration 0001 tạo schema `queue` + 4 bảng + unique idempotency index + sent_at/freshdesk_reply_id + poller_checkpoint.
- src/config.py có SendMode default dry_run + per-ticket throttle config.
</verification>

<success_criteria>
- Dự án Python init được qua uv (hoặc fallback pip), deps khai báo đúng version đã verify.
- Postgres 16 (pgvector-ready) khởi động được qua docker-compose; Alembic migrate tạo schema `queue` với ticket_queue (+sent_at,+freshdesk_reply_id) + dead_letter + dry_run_log + poller_checkpoint.
- 7 test file scaffold + conftest collect thành công, fail-có-chủ-đích sẵn sàng cho Wave 1 biến RED→GREEN (gồm 3 RED test revision).
- Presidio backend (spaCy en_core_web_lg) cài được.
</success_criteria>

<output>
Create `.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-01-SUMMARY.md` when done
</output>
