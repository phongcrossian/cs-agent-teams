---
phase: 02-freshdesk-i-o-layer-pipeline-backbone
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/freshdesk_io/__init__.py
  - src/freshdesk_io/client.py
  - src/freshdesk_io/models.py
  - src/freshdesk_io/rate_limit.py
  - src/freshdesk_io/errors.py
  - tests/test_client.py
autonomous: true
requirements: [REP-05]

must_haves:
  truths:
    - "Client có thể post một public reply vào đúng ticket_id qua POST /api/v2/tickets/{id}/reply"
    - "Client có thể post một private note qua POST /api/v2/tickets/{id}/notes"
    - "Khi gặp 429, client honor Retry-After header; 5xx/timeout retry với backoff+jitter; 401/403/404/400/409 fail ngay (không retry)"
    - "Client có thể GET /api/v2/tickets/{id}/conversations để lấy danh sách conversation"
    - "Client có thể list_updated_tickets(since) -> list[Ticket] qua GET /api/v2/tickets?updated_since=...&per_page=100 (có pagination) — poller (02-05) phụ thuộc vào đây"
  artifacts:
    - path: "src/freshdesk_io/client.py"
      provides: "FreshdeskClient: post_reply, post_note, get_conversations, get_ticket, list_updated_tickets"
      exports: ["FreshdeskClient"]
    - path: "src/freshdesk_io/errors.py"
      provides: "FreshdeskRateLimitError(retry_after), FreshdeskFatalError, FreshdeskTransientError"
      contains: "retry_after"
    - path: "src/freshdesk_io/models.py"
      provides: "Pydantic models: Conversation (incoming, private, user_id, from_email, source), Ticket (id, updated_at)"
      contains: "incoming"
  key_links:
    - from: "src/freshdesk_io/client.py"
      to: "src/freshdesk_io/rate_limit.py"
      via: "tenacity wait function honors Retry-After"
      pattern: "Retry-After|retry_after"
    - from: "src/freshdesk_io/client.py"
      to: "src/config.py"
      via: "đọc freshdesk_domain + freshdesk_api_key (Basic Auth, key as username)"
      pattern: "api_key|freshdesk_domain"
---

<objective>
Xây dựng module I/O duy nhất được phép gọi Freshdesk API — slice mỏng nhất chứng minh "hệ thống có thể post vào ticket". Đây là vertical slice đầu tiên có chức năng thật: sau plan này, một caller có thể post reply/note vào ticket sandbox (qua live-send) với retry/rate-limit an toàn, VÀ list các ticket updated (poller dependency).

Mục đích: Centralize toàn bộ outbound/inbound Freshdesk call (D-04 expose cả reply public + note private từ đầu; thêm list_updated_tickets cho poller D-09), với httpx + tenacity xử lý rate-limit/backoff (D-10 error classification). Module này là biên giới kiến trúc: không nơi nào khác trong codebase được gọi Freshdesk trực tiếp.

Output: FreshdeskClient với post_reply, post_note, get_conversations, get_ticket, list_updated_tickets; Ticket + Conversation model; phân loại lỗi transient vs fatal (409 → fatal/dead-letter cho tới khi verify sandbox); honor Retry-After.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md
@.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md
@src/config.py

<interfaces>
<!-- Từ 02-01 (Wave 0). Executor dùng trực tiếp, không cần khám phá codebase. -->
src/config.py exports `Settings` với: freshdesk_domain, freshdesk_api_key, send_mode (SendMode.DRY_RUN|LIVE), max_attempts (default 5).
src/config.py exports `SendMode` Enum: DRY_RUN="dry_run", LIVE="live".

Freshdesk auth (RESEARCH § Authentication): Basic Auth, API key = username, "X" = password:
  httpx.AsyncClient(auth=(api_key, "X"), base_url=f"https://{domain}.freshdesk.com")

Endpoints (RESEARCH § Endpoints chính):
  POST /api/v2/tickets/{id}/reply   body: {"body": "<html>"}
  POST /api/v2/tickets/{id}/notes   body: {"body": "<html>", "private": true}
  GET  /api/v2/tickets/{id}/conversations
  GET  /api/v2/tickets/{id}
  GET  /api/v2/tickets?updated_since={iso}&per_page=100   (list updated, có pagination qua Link header / page param)

Conversation fields (RESEARCH § Conversation Object Fields):
  incoming: bool (true=customer), private: bool, user_id: int, from_email: str, source: int, body_text: str
Ticket fields (RESEARCH § Endpoints — List tickets updated): id: int, updated_at: datetime (tối thiểu; cho phép extra field).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Models (Conversation + Ticket) + error taxonomy + rate-limit helper</name>
  <files>src/freshdesk_io/models.py, src/freshdesk_io/errors.py, src/freshdesk_io/rate_limit.py</files>
  <behavior>
    - errors.py: FreshdeskRateLimitError lưu retry_after (int seconds); FreshdeskFatalError cho 401/403/404/400/409; FreshdeskTransientError cho 5xx.
    - rate_limit.py: hàm parse_retry_after(headers) trả về int seconds từ header "Retry-After" (default 60 nếu thiếu); hàm classify_status(status_code) trả về "transient"|"fatal" theo bảng RESEARCH § Error Classification, VỚI 409 → "fatal" (fix review #5 — KHÔNG coi 409 là success; semantic chưa verify trên sandbox).
    - models.py: Conversation parse từ dict Freshdesk có incoming/private/user_id/from_email/source/body_text; Ticket parse từ dict có id + updated_at (cho phép extra); ReplyResult/NoteResult chứa conversation id trả về.
  </behavior>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ httpx + tenacity — Error Classification table: 429/5xx/timeout=transient, 401/403/404/400=fatal, 409=context; § Conversation Object Fields; § Endpoints — List tickets updated)
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-CONTEXT.md (D-10 retry/dead-letter; Claude's Discretion — error taxonomy)
    - tests/test_client.py (scaffold từ 02-01: test_retry_after, test_fatal_404_no_retry, test_list_updated_tickets — biến RED→GREEN)
  </read_first>
  <action>
    Tạo errors.py với 3 exception class: FreshdeskRateLimitError(retry_after: int), FreshdeskTransientError (5xx/timeout), FreshdeskFatalError (401/403/404/400/409 — không retry). XỬ LÝ 409 (fix review #5): treat 409 là FATAL → dead-letter cho human inspection. KHÔNG coi 409 là "success/duplicate" (semantic Freshdesk chưa verify; treating-as-success có thể nuốt lỗi thật và KHÔNG được dùng làm bất kỳ phần nào của exactly-once story). Verify 409 semantic thật trên sandbox là một checkpoint item (02-06 Task 3) — tới khi đó 409 luôn dead-letter.
    Tạo rate_limit.py: `parse_retry_after(headers: dict) -> int` đọc "Retry-After" (default 60); `classify_status(status_code: int) -> Literal["transient","fatal"]` theo bảng RESEARCH (429,500,502,503,504 → transient; 401,403,404,400,409 → fatal).
    Tạo models.py: Pydantic `Conversation` (id, incoming, private, user_id, from_email, source, body_text — dùng alias nếu Freshdesk field khác), `Ticket` (id: int, updated_at: datetime — cho phép extra field từ API), `ReplyResult` (id, ticket_id), `NoteResult` (id, ticket_id). Cho phép extra field từ API (model_config extra="ignore").
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -c "from src.freshdesk_io.errors import FreshdeskRateLimitError, FreshdeskFatalError; from src.freshdesk_io.rate_limit import parse_retry_after, classify_status; assert parse_retry_after({'Retry-After':'30'})==30; assert classify_status(429)=='transient'; assert classify_status(404)=='fatal'; assert classify_status(409)=='fatal'; from src.freshdesk_io.models import Conversation, Ticket; c=Conversation(**{'id':1,'incoming':True,'private':False,'user_id':9,'from_email':'a@b.com','source':1,'body_text':'hi'}); assert c.incoming; t=Ticket(**{'id':5,'updated_at':'2026-06-01T00:00:00Z'}); assert t.id==5; print('models+errors OK')"</automated>
  </verify>
  <acceptance_criteria>
    - parse_retry_after({'Retry-After':'30'}) == 30; thiếu header → 60.
    - classify_status(429)=='transient', classify_status(404)=='fatal', classify_status(409)=='fatal' (fix #5 — 409 không còn là success).
    - Conversation model parse dict với incoming/private/user_id/from_email/source và bỏ qua field thừa.
    - Ticket model parse dict {id, updated_at} (poller dependency, fix #2).
  </acceptance_criteria>
  <done>Lệnh verify in "models+errors OK".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FreshdeskClient (post_reply/post_note/get_conversations/list_updated_tickets) + tenacity retry honor Retry-After</name>
  <files>src/freshdesk_io/client.py, src/freshdesk_io/__init__.py, tests/test_client.py</files>
  <behavior>
    - test_post_reply_success: respx mock 200 cho POST /api/v2/tickets/123/reply → trả ReplyResult với conversation id.
    - test_post_note: respx mock 201 cho POST /api/v2/tickets/123/notes với private=true.
    - test_retry_after: respx mock trả 429 (Retry-After: 1) lần đầu rồi 200 → client retry đúng 1 lần, tổng wait ~= retry_after, cuối cùng success.
    - test_fatal_404_no_retry: respx mock 404 → raise FreshdeskFatalError NGAY, KHÔNG retry (respx call count == 1).
    - test_get_conversations: respx mock trả list conversation → parse thành list[Conversation].
    - test_list_updated_tickets: respx mock GET /api/v2/tickets?updated_since=...&per_page=100 trả ≥1 trang ticket → list_updated_tickets(since) parse thành list[Ticket]; nếu mock trả 2 trang (Link header / page param) → client gom đủ cả 2 trang (pagination).
  </behavior>
  <read_first>
    - .planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-RESEARCH.md (§ httpx + tenacity — Tenacity pattern với Retry-After: freshdesk_wait, is_transient, @retry stop_after_attempt(5); § Endpoints chính — request bodies + List tickets updated_since pagination)
    - CLAUDE.md (§ Supporting Libraries — httpx + tenacity, respect Freshdesk rate limits, backoff on 429)
    - tests/test_client.py (scaffold từ 02-01)
    - src/config.py (Settings: freshdesk_domain, freshdesk_api_key, max_attempts)
  </read_first>
  <action>
    Tạo FreshdeskClient nhận httpx.AsyncClient (auth=(api_key,"X"), base_url từ Settings) hoặc tự build từ Settings. Methods async: `post_reply(ticket_id, body) -> ReplyResult`, `post_note(ticket_id, body, private=True) -> NoteResult`, `get_conversations(ticket_id) -> list[Conversation]`, `get_ticket(ticket_id) -> dict`, `list_updated_tickets(since: datetime) -> list[Ticket]`.
    `list_updated_tickets` (fix review #2 — poller D-09 dependency): GET `/api/v2/tickets?updated_since={since.isoformat()}&per_page=100`; xử lý pagination (đọc `Link` header `rel="next"` HOẶC tăng `page=` cho tới khi trang rỗng); parse mỗi item thành Ticket; trả list[Ticket]. LƯU Ý sub-limit của List endpoint (RESEARCH §298: Growth ~20 calls/min) — tenacity retry honor Retry-After đã bao phủ 429.
    Bọc mỗi outbound/inbound call bằng tenacity `@retry(stop=stop_after_attempt(settings.max_attempts), wait=freshdesk_wait, retry=retry_if_exception_type((FreshdeskRateLimitError, FreshdeskTransientError, httpx.TransportError)), reraise=True)`. Trong call: nếu status 429 → đọc parse_retry_after → raise FreshdeskRateLimitError(retry_after); nếu classify_status=='fatal' (gồm 409) → raise FreshdeskFatalError (KHÔNG nằm trong retry set → fail ngay); nếu 5xx → FreshdeskTransientError. freshdesk_wait honor retry_after khi là RateLimitError, else exponential backoff + jitter (base 1s, cap 60s) per RESEARCH.
    Export FreshdeskClient từ __init__.py. Biến 6 test trong test_client.py từ RED → GREEN dùng respx mock (không network).
    LƯU Ý log: không log raw body/PII (CLAUDE.md) — nếu log, chỉ log ticket_id + status + conversation id.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -m pytest tests/test_client.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 6 test trong test_client.py PASS (post_reply_success, post_note, retry_after, fatal_404_no_retry, get_conversations, list_updated_tickets).
    - test_fatal_404_no_retry chứng minh respx call count == 1 (404 không retry).
    - test_retry_after chứng minh client honor Retry-After (retry sau đúng khoảng từ header, không exponential mù).
    - test_list_updated_tickets chứng minh list[Ticket] parse được + pagination gom đủ trang (fix #2).
    - FreshdeskClient export từ src.freshdesk_io; không log raw ticket body.
  </acceptance_criteria>
  <done>`pytest tests/test_client.py -x -q` xanh hoàn toàn (6 test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → Freshdesk API | Outbound HTTPS với API key; mọi call qua module duy nhất này |
| Freshdesk response → pipeline | Response body là dữ liệu ngoài (untrusted), chỉ parse, không eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | client.py logging | mitigate | Không log raw ticket body / from_email; chỉ ticket_id + status (CLAUDE.md PII rule; PII redaction đầy đủ ở plan 04) |
| T-02-04 | Information Disclosure | API key trong auth | mitigate | API key đọc từ Settings (env), không hardcode, không log; HTTPS-only base_url |
| T-02-05 | Denial of Service | retry loop | mitigate | stop_after_attempt(max_attempts); fatal errors (401/403/404/400/409) fail ngay không retry → không runaway |
| T-02-06 | Tampering | Freshdesk response parse | mitigate | Pydantic model với extra="ignore"; không eval/execute nội dung response |
</threat_model>

<verification>
- `pytest tests/test_client.py -x -q` xanh (6 test gồm list_updated_tickets).
- FreshdeskClient.post_reply / post_note / get_conversations / list_updated_tickets import + gọi được (respx mock).
- 404 + 409 → FreshdeskFatalError không retry; 429 → honor Retry-After.
</verification>

<success_criteria>
- Module freshdesk_io là điểm duy nhất gọi Freshdesk API (reply public + note private — D-04; list_updated_tickets cho poller D-09).
- Retry/backoff honor Retry-After trên 429; transient (5xx/timeout) retry; fatal (401/403/404/400/409) fail ngay (D-10; 409 fix #5).
- Toàn bộ test_client.py xanh dùng respx (network-free).
</success_criteria>

<output>
Create `.planning/phases/02-freshdesk-i-o-layer-pipeline-backbone/02-02-SUMMARY.md` when done
</output>
