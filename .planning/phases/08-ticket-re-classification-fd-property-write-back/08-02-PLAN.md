---
phase: 08-ticket-re-classification-fd-property-write-back
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - scripts/test_tickets_run.py
  - scripts/test_test_tickets_run.py
  - .gitignore
autonomous: true
requirements: [REP-06]

must_haves:
  truths:
    - "Each processed ticket carries an fd_property_update block (one verbatim enum value per owned field) assembled from the AI classification, advisory/additive to the always-draft verdict"
    - "The xlsx shows AI-defined FD properties vs CS gold fd_props side-by-side with a per-field match metric and a *_valid (or status) flag per field"
    - "The run asserts DRY_RUN: no live PUT /tickets/{id} occurs; the only Freshdesk calls remain the existing read-only GETs"
  artifacts:
    - path: "scripts/test_tickets_run.py"
      provides: "fd_property_update assembled in _process_row + rendered side-by-side in build_xlsx with per-field match + valid flags"
      contains: "build_fd_property_update"
    - path: "scripts/test_test_tickets_run.py"
      provides: "Offline tests: assembly merged into record, per-field match metric, no live PUT"
    - path: ".gitignore"
      provides: "test-tickets.xlsx ignored (PII-bearing output)"
      contains: "test-tickets.xlsx"
  key_links:
    - from: "scripts/test_tickets_run.py::_process_row"
      to: "src/file_store/fd_classification.build_fd_property_update"
      via: "assemble fd_property_update from ai['properties'] and attach to the record"
      pattern: "build_fd_property_update"
    - from: "scripts/test_tickets_run.py::build_xlsx"
      to: "rec fd_property_update + rec fd_props"
      via: "side-by-side AI-vs-CS-gold rows with per-field match + valid flag"
      pattern: "fd_property_update"
---

<objective>
Wire the offline enum loader + validator/assembler (Plan 08-01) into the existing validation harness:
assemble an `fd_property_update` block per ticket from the AI's classification, attach it to the
record (advisory/additive to the always-draft verdict), and render AI-defined FD properties vs CS gold
`fd_props` side-by-side in the xlsx with a per-field match metric and per-field validity flags.

Purpose: REP-06 Success Criteria #2 (emit the block), #3 (validate, flag out-of-enum), #4 (side-by-side
vs CS gold with per-field match), #5 (DRY_RUN — no live write). This plan delivers the observable
pipeline output and the human-readable comparison.

Output: updated `scripts/test_tickets_run.py` (assembly in `_process_row`, rendering in `build_xlsx`),
extended offline tests in `scripts/test_test_tickets_run.py`, and a `.gitignore` entry for the
PII-bearing `test-tickets.xlsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-ticket-re-classification-fd-property-write-back/08-CONTEXT.md
@.planning/phases/08-ticket-re-classification-fd-property-write-back/08-01-SUMMARY.md
@./.claude/CLAUDE.md

# The harness this plan extends (read _process_row, build_xlsx, _extract_fd_props, run_ai_team):
@scripts/test_tickets_run.py
@scripts/test_test_tickets_run.py

<interfaces>
<!-- Contracts from Plan 08-01 (src/file_store) and the existing harness. -->

From src/file_store/fd_classification.py (built in 08-01):
```python
OWNED_FIELDS = ("Level_in", "Customer_Request", "Rootcause", "Flow", "Section_Flow")
def validate_field(field: str, value: str, allowed: list[str] | None = None) -> dict
    # -> {"field","value","status": "valid"|"invalid"|"unverifiable"|"missing","allowed"}
def build_fd_property_update(ai_props: dict) -> dict
    # -> {"fields": {<owned>: <validate_field result>}, "all_valid": bool, "advisory": True}
```

From scripts/test_tickets_run.py (existing, the integration points):
```python
# _process_row(...) builds the per-ticket record and returns the dict that collect()/run() persist.
#   rec keys today: category_file, ticket_id, cs_props, fd_props, customer_msg, cs_reply,
#                   fetch_error, selless_order, ai_properties, ai_verdict
#   fd_props = CS GOLD from FD custom_fields (Level_in, Customer_Request, Rootcause, Flow,
#              Section_Flow, ... ) via _extract_fd_props — already PII-redacted.
#   ai["properties"] carries: category, customer_request, order_ref, flow, step, rootcause, ...
# build_xlsx() renders per-ticket sheets. The "— FD ticket properties —" section already lists
#   rec["fd_props"] (CS gold). The "— AI Team output —" block lists ai_properties.
# Mirror the existing template_valid / allowed_codes rendering pattern for the *_valid flags.
```

NOTE on AI property keys: the AI emits `customer_request` and `category` (complaint|change_request|
inquiry|other) but NOT a literal `Level_in`. Derive Level_in from category using the snapshot's macro
keys: complaint->Complaint, change_request->Change_Request, inquiry->Inquiry (the loader's
level_in_choices() are the authoritative macro labels). Pass the derived Level_in into
build_fd_property_update so nested integrity (Customer_Request must be a child of Level_in) is checked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble fd_property_update into the record + per-field match</name>
  <files>scripts/test_tickets_run.py, scripts/test_test_tickets_run.py</files>
  <behavior>
    Add offline tests (pure dict in/out; NO claude/Freshdesk/Selless calls — same offline contract as
    the existing _extract_fd_props tests):
    - A new pure helper `_assemble_fd_property_update(ai_props)` (thin adapter that derives Level_in
      from category, then calls src.file_store.fd_classification.build_fd_property_update) returns a
      block with owned fields only; Customer_Request="Return" under category="complaint" -> status
      "valid"; an invented Customer_Request -> status "invalid".
    - A new pure helper `_fd_field_match(fd_update, fd_props)` returns a per-owned-field match dict
      {field: {ai_value, cs_gold, match: bool, status}} comparing the AI verbatim value to the CS gold
      fd_props value (case-insensitive exact match on the enum label); a field absent from CS gold ->
      match recorded as None/"no_gold" (not a false match).
    - Empty-enum field (Rootcause/Flow/Section_Flow currently empty in snapshot) -> status
      "unverifiable" and match still computed against CS gold if gold present.
    - Out-of-scope FD fields present in fd_props (e.g. Package_status) are NOT added to the match dict
      (owned-field scope preserved).
  </behavior>
  <action>
    In scripts/test_tickets_run.py import build_fd_property_update (and validate_field if needed) from
    src.file_store.fd_classification and level_in_choices from src.file_store.ticket_fields_store. Add
    two pure helpers near _extract_fd_props: `_assemble_fd_property_update(ai_props)` (derive Level_in
    from ai_props["category"] via the macro map {complaint:Complaint, change_request:Change_Request,
    inquiry:Inquiry}; pass a normalized dict into build_fd_property_update mapping
    customer_request/rootcause/flow/section_flow|step) and `_fd_field_match(fd_update, fd_props)`
    (per-owned-field comparison vs CS gold). In `_process_row`, after `ai = await run_ai_team(...)`,
    compute `fd_update = _assemble_fd_property_update(ai["properties"])` and
    `fd_match = _fd_field_match(fd_update, fd_props)` and add both to the returned record dict
    (keys: "fd_property_update", "fd_field_match"). This is purely additive — do NOT change the
    verdict, the draft, or the submit_reply path (D-33 advisory; per 08-CONTEXT write-back is additive).
    Keep the change read-only: no new Freshdesk call. Extend scripts/test_test_tickets_run.py with the
    behavior tests above, reusing the existing fake-payload / pure-function offline style (no new
    network imports).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest scripts/test_test_tickets_run.py -q</automated>
  </verify>
  <done>_process_row records carry fd_property_update + fd_field_match; owned-field-only; out-of-enum
  flagged "invalid"; empty-enum "unverifiable"; per-field match vs CS gold computed; all tests offline GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Render side-by-side in build_xlsx + DRY_RUN assertion + gitignore</name>
  <files>scripts/test_tickets_run.py, scripts/test_test_tickets_run.py, .gitignore</files>
  <action>
    In build_xlsx(), add a new section per sheet "— FD re-classification (AI vs CS gold) —" that, for
    each OWNED field (Level_in, Customer_Request, Rootcause, Flow, Section_Flow), renders a row with:
    column B = CS gold value (from rec["fd_props"]), column C = AI value (from
    rec["fd_property_update"]["fields"][field]["value"]), plus an adjacent cell showing the per-field
    status flag ("✓ valid" / "✗ INVALID (not in enum)" / "unverifiable (enum empty)" / "missing") and
    the match result ("match" / "differ" / "no gold") from rec["fd_field_match"] — mirror the existing
    template_valid rendering style (the ✓/✗ + allowed_codes pattern already in build_xlsx). Add a
    summary row "FD per-field match: N/M owned fields match CS gold". Guard for records that predate
    this block (missing keys -> render gracefully, no crash; reuse the str()-coercion discipline from
    the existing int-props regression). Then assert the DRY_RUN posture: the harness already asserts
    `settings.dry_run` in run()/run_ai_team — add an explicit offline test that constructs a record and
    runs build_xlsx with the new section to a tmp path (mirror test_build_xlsx_handles_int_props),
    and a test/grep-gate proving no `PUT`/`tickets/{id}` write path was introduced (the only FD verbs
    in the file remain GET). Finally add `test-tickets.xlsx` to .gitignore (PII-bearing output;
    08-CONTEXT/REP-06 want outputs gitignored — only .test-tickets-data.jsonl is ignored today).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest scripts/test_test_tickets_run.py -q && grep -q "test-tickets.xlsx" .gitignore && test -z "$(grep -nE 'PUT|\.put\(|/tickets/[^\"]*\", *json=|api/v2/tickets/.*reply' scripts/test_tickets_run.py | grep -v '^[[:space:]]*#')" && echo NO_LIVE_WRITE_OK    </automated>
  </verify>
  <done>build_xlsx renders the AI-vs-CS-gold owned-field comparison with per-field status + match
  and a N/M summary; test-tickets.xlsx is gitignored; no live PUT/reply write path exists (only
  read-only GETs remain); offline tests GREEN; DRY_RUN posture asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> Freshdesk | This phase must add NO new write path; write-back is a NEW FD surface beyond submit_reply and stays DRY_RUN-gated (08-CONTEXT locked) |
| AI classification -> would-be FD property | An invented enum value must surface as flagged, never as a clean would-be update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | new FD write path | mitigate | No PUT /tickets/{id} added; assert settings.dry_run on the run path; grep-gate proves only read-only GETs remain (08-CONTEXT DRY_RUN-only lock) |
| T-08-06 | Tampering | fd_property_update rendering | mitigate | Render the validate_field status verbatim; an "invalid"/out-of-enum value is shown as ✗ INVALID, never as a clean value |
| T-08-07 | Information Disclosure | test-tickets.xlsx | mitigate | xlsx carries redacted ticket/CS/AI content; add it to .gitignore so PII-bearing output is never committed (D-04 posture) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest scripts/test_test_tickets_run.py -q` is GREEN.
- `grep -q "test-tickets.xlsx" .gitignore` (PII-bearing output ignored).
- No live write: `grep -nE "PUT|\.put\(|api/v2/tickets/.*reply" scripts/test_tickets_run.py | grep -v "^[[:space:]]*#"` is empty (only read-only GETs remain).
- A built workbook contains the "— FD re-classification (AI vs CS gold) —" section with per-field status + match + N/M summary.
</verification>

<success_criteria>
- Pipeline output carries an fd_property_update block (verbatim enum per owned field), advisory/additive to the always-draft verdict (Phase 8 Success Criterion #2).
- Out-of-enum values are flagged in the rendered output, never silently accepted (Phase 8 Success Criterion #3).
- The xlsx shows AI-defined properties vs CS gold fd_props side-by-side with a per-field match metric (Phase 8 Success Criterion #4).
- DRY_RUN only — no live PUT /tickets/{id}; only the existing read-only GETs remain (Phase 8 Success Criterion #5).
</success_criteria>

<output>
Create `.planning/phases/08-ticket-re-classification-fd-property-write-back/08-02-SUMMARY.md` when done
</output>
