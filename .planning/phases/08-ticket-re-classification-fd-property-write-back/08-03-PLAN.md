---
phase: 08-ticket-re-classification-fd-property-write-back
plan: 03
type: execute
wave: 1
depends_on: ["08-01", "08-02"]
files_modified:
  - scripts/test_tickets_run.py
  - scripts/test_test_tickets_run.py
autonomous: true
gap_closure: true
requirements: [REP-06]

must_haves:
  truths:
    - "The classifier prompt (_build_classify_prompt) instructs the AI to pick a VERBATIM value from the allowed Rootcause / Flow / Section_Flow enum, sourced from field_choices() at build time"
    - "The drafter prompt (_build_draft_prompt2) carries the same verbatim-enum instruction + allowed values for Rootcause / Flow / Section_Flow"
    - "The injected enum values are read deterministically from field_choices() (loader-sourced), not hard-coded literals in the prompt string — so they stay in sync with the snapshot"
    - "Re-running the phase verifier moves SC#2 PARTIAL -> VERIFIED: a live --id smoke shows AI Rootcause/Flow/Section_Flow values are validate_field() 'valid' (enum-verbatim), not free-text 'invalid'"
  artifacts:
    - path: "scripts/test_tickets_run.py"
      provides: "Enum-injection block in _build_classify_prompt and _build_draft_prompt2 for Rootcause/Flow/Section_Flow, sourced from field_choices()"
      contains: "field_choices"
    - path: "scripts/test_test_tickets_run.py"
      provides: "Offline tests asserting both prompts contain the injected enum values for the 3 owned free-text fields"
      contains: "field_choices"
  key_links:
    - from: "scripts/test_tickets_run.py::_build_classify_prompt"
      to: "src/file_store/ticket_fields_store.field_choices"
      via: "field_choices('Rootcause'|'Flow'|'Section_Flow') at prompt-build time"
      pattern: "field_choices\\((\"|')(Rootcause|Flow|Section_Flow)"
    - from: "scripts/test_tickets_run.py::_build_draft_prompt2"
      to: "src/file_store/ticket_fields_store.field_choices"
      via: "shared enum-block helper injected into the draft prompt"
      pattern: "field_choices\\((\"|')(Rootcause|Flow|Section_Flow)"
---

<objective>
Close the SC#2 PARTIAL gap from 08-VERIFICATION.md: the fd_property_update block is emitted and validated, but the AI emits FREE-TEXT for Rootcause / Flow / Section_Flow because the harness prompt builders give open-ended slots ("rootcause": "<root cause if determinable>") without injecting the allowed enum values. validate_field() then correctly flags those as "invalid".

Fix: inject the allowed enum values for Rootcause, Flow, and Section_Flow into both prompt builders — `_build_classify_prompt` and `_build_draft_prompt2` in `scripts/test_tickets_run.py` — mirroring how ALLOWED TEMPLATE CODES BY SUB-TYPE are already injected for `template_code` (run_ticket prompt, lines 332-363). The allowed lists are pulled deterministically from `field_choices()` at prompt-build time (NOT hard-coded), with an explicit "choose VERBATIM from this list, else leave empty" instruction per field. This honors 08-CONTEXT.md locked decisions: core fields only, DRY_RUN, verbatim-enum discipline (NEVER free-pick).

Purpose: make SC#2 (REP-06) move from PARTIAL -> VERIFIED — the AI produces enum-verbatim values for the 3 owned free-text fields so validate_field() returns "valid".
Output: enum-injection block in two prompt builders + offline tests proving the built prompts contain the injected values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-ticket-re-classification-fd-property-write-back/08-VERIFICATION.md
@.planning/phases/08-ticket-re-classification-fd-property-write-back/08-CONTEXT.md

<interfaces>
<!-- Enum source (already built, Phase 8-01). Use field_choices() directly; do NOT re-implement. -->
From src/file_store/ticket_fields_store.py:
```python
def field_choices(field: str, *, snapshot_path: Path | None = None) -> list[str]:
    """Verbatim allowed values for a named FD field. Fail-soft -> [] for unknown/empty.
    Confirmed populated from the real snapshot:
      field_choices("Rootcause")     -> 14 values  (e.g. Shipping, Fulfillment, Product, ...)
      field_choices("Flow")          -> 7 values   (e.g. REPLACEMENT_DEFINE, CANCEL_IN_POLICY, ...)
      field_choices("Section_Flow")  -> 5 values   (e.g. BRA_SIZING, DRESS_SIZING, ...)
    """
```

<!-- The validator that consumes the AI output (already built, Phase 8-01). Do NOT modify. -->
From src/file_store/fd_classification.py:
```python
OWNED_FIELDS: tuple[str, ...]  # Level_in, Customer_Request, Rootcause, Flow, Section_Flow
def validate_field(field, value) -> {...}  # value in non-empty allowed -> "valid"; not in -> "invalid"
```

<!-- The pattern to MIRROR — ALLOWED TEMPLATE CODES injection already in scripts/test_tickets_run.py -->
The run_ticket prompt (lines 332-363) builds `codes_map` from _SUBTYPE_TEMPLATES and injects:
  "ALLOWED TEMPLATE CODES BY SUB-TYPE:\n" + codes_map + ...
with a HARD CONSTRAINT: "template_code MUST be exactly one code from the ALLOWED list ... verbatim ... NEVER invent."
Replicate this shape for Rootcause / Flow / Section_Flow, sourced from field_choices().

<!-- The two builders to modify (scripts/test_tickets_run.py) -->
def _build_classify_prompt(ticket: dict, selless: dict | None) -> str   # ~line 495 — pass-1 classify
def _build_draft_prompt2(ticket, selless, templates_text, allowed_codes, subtype) -> str  # ~line 521 — pass-2 draft
Both currently emit open slots: '"rootcause": "<root cause if determinable, else empty>"', '"flow": "<workflow/flow name if determinable, else empty>"'.

<!-- The test file imports PURE helpers and asserts on returned strings — no LLM call. -->
scripts/test_test_tickets_run.py imports from scripts.test_tickets_run (e.g. _assemble_fd_property_update, _allowed_codes_for_subtype). Add `_build_classify_prompt`, `_build_draft_prompt2` to that import block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — offline tests assert both prompts inject the Rootcause/Flow/Section_Flow enum verbatim from field_choices()</name>
  <files>scripts/test_test_tickets_run.py</files>
  <behavior>
    - Test A (_build_classify_prompt enum injection): build the classify prompt with a minimal fake ticket and selless=None; assert the returned string contains (1) every value of field_choices("Rootcause"), (2) every value of field_choices("Flow"), (3) every value of field_choices("Section_Flow"), and (4) a verbatim-discipline instruction token (e.g. the word "VERBATIM" or "verbatim" alongside "Rootcause"/"Flow"/"Section_Flow"). Use field_choices() in the test itself (loader-sourced) — do NOT hard-code the enum literals, so the test stays in sync with the snapshot.
    - Test B (_build_draft_prompt2 enum injection): build the draft prompt with a fake ticket, selless=None, templates_text="", allowed_codes=["G2"], subtype="Ask_About_Order"; assert the same three enum lists and the verbatim instruction appear in the returned string.
    - Test C (deterministic / loader-sourced): assert that for at least one sampled value from field_choices("Rootcause") the prompt contains that exact value — proving the injected text is sourced from the loader, not a stale literal. (Guards the must_have "stays in sync with the snapshot".)
    - These run with NO subprocess / NO claude CLI call — pure string assertions on the built prompt (mirrors the existing import-pure-helpers discipline at the top of the file).
  </behavior>
  <action>
    Add `_build_classify_prompt` and `_build_draft_prompt2` to the existing `from scripts.test_tickets_run import (...)` block (line 27-35). Also import `field_choices` from `src.file_store.ticket_fields_store` at module top.

    Add three test functions (Test A/B/C above). In each, call the builder, then loop over `field_choices("Rootcause")`, `field_choices("Flow")`, `field_choices("Section_Flow")` asserting each value is `in prompt`. Assert a verbatim-discipline marker is present.

    Run the new tests and confirm they FAIL (RED) against the current builders (which do not inject the enums). Commit: `test(08-03): add failing tests for Rootcause/Flow/Section_Flow enum injection`.

    Do NOT touch _SUBTYPE_TEMPLATES, template-selection logic, or the template_code allowed-set injection — this plan is additive prompt text for the FD classification fields only (blocking anti-pattern guardrail from 08-CONTEXT).
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python3 -m pytest scripts/test_test_tickets_run.py -k "enum_injection or verbatim_enum" -q 2>&1 | tail -15</automated>
  </verify>
  <done>3 new offline tests exist and FAIL against the current (un-fixed) builders, proving the assertions are real. No LLM/subprocess call in the tests. Committed as the RED step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — inject allowed Rootcause/Flow/Section_Flow enum (from field_choices) into both prompt builders</name>
  <files>scripts/test_tickets_run.py</files>
  <behavior>
    - After the fix, `_build_classify_prompt(...)` and `_build_draft_prompt2(...)` each contain an explicit allowed-enum block listing every field_choices("Rootcause") / field_choices("Flow") / field_choices("Section_Flow") value, with a "choose VERBATIM from this list for that field, else leave empty — NEVER invent a value" instruction (mirroring the ALLOWED TEMPLATE CODES constraint).
    - The enum lists are built at call time from field_choices() (loader-sourced), so editing the snapshot changes the prompt with no code change.
    - The three RED tests from Task 1 now PASS.
    - The existing 27 harness tests still pass (no regression to template-code injection, xlsx rendering, no-PUT assertion).
  </behavior>
  <action>
    Add a small module-level helper (e.g. `_fd_enum_constraint_block()`) in scripts/test_tickets_run.py that builds the injected text deterministically:
      - For each of "Rootcause", "Flow", "Section_Flow", read `field_choices(field)` and render a line like
        `  {field}: {comma-joined verbatim values}` (or `(no enum values available)` when the list is empty — fail-soft, matching the loader contract).
      - Prefix with a HARD-CONSTRAINT instruction: the value for each of these fields MUST be chosen VERBATIM from its allowed list below; if none fits, leave the field empty; NEVER invent or paraphrase a value (same discipline as the ALLOWED TEMPLATE CODES block). Reference the verbatim-enum lock from 08-CONTEXT.

    Inject `_fd_enum_constraint_block()` into BOTH builders:
      - `_build_classify_prompt`: append the block (and tighten the rootcause-relevant guidance) before the `<ticket_metadata>` section. The classify prompt's JSON currently omits rootcause/flow/section_flow — keep its existing minimal classify JSON, but add the allowed-enum block so PASS-1 is enum-aware. (PASS-2 backfills; both passes must carry the constraint per the verifier's fix.)
      - `_build_draft_prompt2`: replace the open slots `"flow":"..."`, `"rootcause":"..."` guidance by appending the enum-constraint block and pointing the JSON `flow`/`rootcause`/`section_flow` slots at it ("pick VERBATIM from the ALLOWED <field> list above, else empty"). Add a `section_flow` slot if absent so the model can fill it (the assembler already falls back to `step`, but an explicit enum-bound slot is better). Do NOT remove or alter the template_code/allowed_codes logic.

    Keep `field_choices` imported at module top (it is already used via fd_classification; import it directly from src.file_store.ticket_fields_store if not already in scope). Do NOT hard-code the enum literals in the prompt string.

    Run the full harness test file; confirm Task-1 tests PASS and the prior 27 tests still PASS. Commit: `feat(08-03): inject Rootcause/Flow/Section_Flow allowed enum into classify+draft prompts`.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python3 -m pytest scripts/test_test_tickets_run.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>Both builders inject the allowed Rootcause/Flow/Section_Flow enum sourced from field_choices() with a verbatim-only instruction. All Task-1 tests pass; the prior 27 harness tests still pass (no regression). No _SUBTYPE_TEMPLATES / template-selection change. No live PUT path introduced.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest scripts/test_test_tickets_run.py -q` — all tests pass (prior 27 + 3 new).
- `python3 -m pytest tests/test_ticket_fields_store.py tests/test_fd_classification.py -q` — 72 prior offline tests still green (loader/validator untouched).
- `grep -nE "PUT|\.put\(|api/v2/tickets/.*reply" scripts/test_tickets_run.py` returns nothing — DRY_RUN posture preserved (SC#5).
- Grep gate: `grep -v '^#' scripts/test_tickets_run.py | grep -c 'field_choices'` > 0 in both builder regions — confirms loader-sourced injection, not literals.

Live smoke (MANUAL — not in CI, requires .env.prd + claude CLI): run `python3 scripts/test_tickets_run.py collect --id <one prod ticket>` (DRY_RUN), open test-tickets.xlsx FD re-classification section, confirm AI Rootcause/Flow/Section_Flow now render with status ✓ ("valid"), not ✗ ("invalid") free-text. This is the final confirmation that SC#2 moves PARTIAL -> VERIFIED.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body -> prompt builders | Untrusted attacker-controlled email body is interpolated into the prompt (existing boundary; D-14). |
| snapshot file -> field_choices() -> prompt | Committed static snapshot is the enum source; no runtime/ticket input controls which values are injected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | enum source for injected lists | mitigate | Values come ONLY from `field_choices()` (repo-root-anchored static snapshot, never built from ticket/runtime input) — mirrors loader T-04-00-01 discipline; the injected block contains no ticket-derived text. |
| T-08-03-02 | Information Disclosure | prompt PII | accept | Builders already run `redact_text()` on subject/body/order_ref (D-04); the enum block adds only static field names + snapshot values (no PII). No new exposure. |
| T-08-03-03 | Elevation of Privilege | new FD write path | mitigate | This plan adds prompt text only — no PUT/write path. `assert settings.dry_run` at run/run_ai_team entry remains (SC#5). Verified by grep gate. |
| T-08-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — pure-Python edits to existing files; no new dependency. |
</threat_model>

<success_criteria>
- SC#2 (REP-06): both `_build_classify_prompt` and `_build_draft_prompt2` inject the allowed Rootcause/Flow/Section_Flow enum, sourced verbatim from `field_choices()`, with an explicit "choose VERBATIM, else empty, NEVER invent" instruction.
- The injected lists are loader-sourced (deterministic, snapshot-synced) — no hard-coded enum literals in the prompt strings.
- 3 new offline tests prove the built prompts contain the injected values; they fail before the fix (RED) and pass after (GREEN).
- No regression: prior 27 harness tests + 72 loader/validator tests still green.
- DRY_RUN preserved: no PUT/live-write path added.
- _SUBTYPE_TEMPLATES / template-selection logic untouched (blocking anti-pattern respected).
- A manual live --id smoke confirms AI Rootcause/Flow/Section_Flow now validate as "valid" (enum-verbatim), moving the phase verifier's SC#2 from PARTIAL -> VERIFIED.
</success_criteria>

<output>
Create `.planning/phases/08-ticket-re-classification-fd-property-write-back/08-03-SUMMARY.md` when done.
</output>
