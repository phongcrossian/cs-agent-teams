---
phase: 03-grounding-layer-selless-mcp-knowledge-rag-mcp
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/knowledge_mcp/models.py
  - src/knowledge_mcp/retrieval.py
  - src/knowledge_mcp/exact.py
  - src/knowledge_mcp/conflict.py
  - src/knowledge_mcp/server.py
  - tests/knowledge_mcp/test_semantic.py
  - tests/knowledge_mcp/test_exact.py
  - tests/knowledge_mcp/test_conflict.py
  - tests/knowledge_mcp/test_override.py
autonomous: true
requirements: [KB-05]

must_haves:
  truths:
    - "semantic_search returns cited passages each carrying source, authority_rank, recency_flag, and snapshot_version"
    - "When conflicting passages are retrieved, the result carries conflict=True and surfaces ALL of them (no self-arbitration)"
    - "When a policy_resolution override row exists for a conflict, resolved_by_override=True and the winning passage is ordered first"
    - "lookup_threshold returns the exact stored value for a threshold_id; get_template returns a scaffold by code"
  artifacts:
    - path: "src/knowledge_mcp/server.py"
      provides: "FastMCP server with semantic_search / lookup_threshold / lookup_code / get_template tools (readOnlyHint)"
      contains: "FastMCP"
    - path: "src/knowledge_mcp/retrieval.py"
      provides: "RRF-fused hybrid search (vector ANN + FTS) + citation assembly"
      contains: "rrf"
    - path: "src/knowledge_mcp/conflict.py"
      provides: "D-13 conflict flag + D-14 override resolution"
      min_lines: 20
    - path: "src/knowledge_mcp/models.py"
      provides: "Citation, SemanticSearchResult, ThresholdResult, TemplateResult Pydantic models"
      contains: "class Citation"
  key_links:
    - from: "src/knowledge_mcp/server.py"
      to: "src/knowledge_mcp/retrieval.py"
      via: "semantic_search calls hybrid_search + assemble_citations"
      pattern: "hybrid_search"
    - from: "src/knowledge_mcp/retrieval.py"
      to: "knowledge.kb_chunk"
      via: "vector + FTS parameterized queries"
      pattern: "embedding <=>"
    - from: "src/knowledge_mcp/conflict.py"
      to: "knowledge.policy_resolution"
      via: "override lookup by conflict_id"
      pattern: "policy_resolution"
---

<objective>
Second vertical slice: the Knowledge MCP query surface (KB-05). Exposes a cited semantic-search tool over the ingested RAG store plus exact-lookup tools (threshold, code-map, template), with conflict-aware behavior (D-13 surface-all-and-flag, D-14 override resolution, D-15 stale downrank/flag) and the D-12 authority hierarchy in citation metadata.

Purpose: Proves success criterion #1 — the Knowledge MCP answers a semantic query and returns passages with source citations carrying source/recency/authority metadata. The conflict flag is the hook the Phase-4 escalation layer reads; the exact tools are the D-10 anti-hallucination path for numbers.
Output: `src/knowledge_mcp/` query modules (models, retrieval, exact, conflict, server) and the four KB-05/D-13/D-14 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-PATTERNS.md

<interfaces>
<!-- Contracts from Plan 00/01 + research patterns. Build against these; do not re-explore. -->

knowledge.kb_chunk: id, content_hash, source, source_type, authority_rank INT (D-12: WorkFlow=3>Templates=2>Confluence=1), recency_flag TEXT ("stale"|NULL, D-15), body, body_tsv (FTS), embedding VECTOR(1024), metadata JSONB, snapshot_version.
knowledge.policy_threshold: threshold_id PK, label, value TEXT, source, authority_rank, conflict_id (e.g. CONTRA-01), snapshot_version.
knowledge.code_map: code PK, action, template_code, source, snapshot_version.
knowledge.template_library: code PK, scenario, subject_template, body_template, source, authority_rank, snapshot_version.
knowledge.policy_resolution: conflict_id PK, winning_source, resolved_value, resolved_by, resolved_at, notes (D-14 override).

src/knowledge_mcp/embeddings.py (Plan 01): async embed_query(text)->list[float] (1024-dim); async embed_documents(texts).

Pydantic model contracts to create (03-PATTERNS.md §models.py):
- Citation{text, source, source_type, authority_rank:int, recency_flag:str|None, snapshot_version:str, score:float}
- SemanticSearchResult{citations:list[Citation], conflict:bool, resolved_by_override:bool}
- ThresholdResult{threshold_id, value:str, source, conflict_id:str|None, override_resolution:str|None}
- TemplateResult{code, scenario, subject_template, body_template, source, authority_rank:int}

FastMCP tool pattern (03-RESEARCH.md §Pattern 1/2): @mcp.tool(annotations=ToolAnnotations(readOnlyHint=True)) on async funcs; FastMCP auto-generates JSON schema from Pydantic return types.

src/freshdesk_io/client.py: async parameterized asyncpg query pattern (lines 211-249) — mirror for retrieval/exact (always $N params, never f-string SQL).
tests/conftest.py (Plan 00): db_pool (pgvector codec), stub_embedder, clean_knowledge_db.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Models + hybrid retrieval + exact-lookup tools</name>
  <read_first>
    - src/freshdesk_io/client.py (async asyncpg fetch/fetchrow parameterized pattern, lines 211-249)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-PATTERNS.md (§models.py, §retrieval.py, §exact.py — full signatures)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md (§Pattern 4 hybrid RRF k=60, hnsw.ef_search; §Code Examples exact threshold lookup)
    - tests/conftest.py (db_pool, stub_embedder, clean_knowledge_db)
  </read_first>
  <behavior>
    - Test 1 (KB-05 semantic): after seeding kb_chunk rows, semantic_search(query) returns a SemanticSearchResult whose citations each have non-empty source, int authority_rank, snapshot_version, float score.
    - Test 2 (KB-05 exact / D-10): lookup_threshold("THR-03") returns ThresholdResult with value containing "45 days" and source set; unknown id raises KeyError.
    - Test 3 (D-15): a kb_chunk seeded with recency_flag="stale" surfaces in citations with recency_flag="stale".
  </behavior>
  <action>
    Create `src/knowledge_mcp/models.py` with the four Pydantic models above (Citation, SemanticSearchResult, ThresholdResult, TemplateResult), exactly the field shapes in 03-PATTERNS.md §models.py.
    Create `src/knowledge_mcp/retrieval.py`: `async def hybrid_search(query, top_k=5)` acquires the shared pool, calls `embed_query`, runs a vector ANN query (`ORDER BY embedding <=> $1::vector LIMIT 20`) and an FTS query (`ts_rank(body_tsv, plainto_tsquery('english',$1))`), fuses with Reciprocal Rank Fusion `1/(60+rank)` (k=60) in Python, returns top_k candidate rows. `assemble_citations(rows)` -> list[Citation] carrying source/source_type/authority_rank/recency_flag/snapshot_version/score (D-12/D-15 metadata). All SQL parameterized ($N), never f-string. Set `hnsw.ef_search` per session if needed.
    Create `src/knowledge_mcp/exact.py`: `async def lookup_threshold_row(threshold_id)` -> ThresholdResult via `SELECT * FROM knowledge.policy_threshold WHERE threshold_id=$1` (raise KeyError if None); `async def lookup_code_row(code)` -> dict from code_map; `async def fetch_template_row(code)` -> TemplateResult from template_library. D-10: values come straight from exact rows, no LLM interpretation.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && pytest tests/knowledge_mcp/test_semantic.py tests/knowledge_mcp/test_exact.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/knowledge_mcp/test_exact.py` GREEN: lookup_threshold("THR-03") value contains "45 days"; unknown id raises KeyError (KB-05/D-10).
    - `tests/knowledge_mcp/test_semantic.py` GREEN: citations carry source + int authority_rank + snapshot_version + float score (KB-05).
    - stale-flagged chunk surfaces with recency_flag="stale" in its Citation (D-15 behavior).
    - retrieval.py and exact.py SQL use `$N` params and the `embedding <=>` operator (source assertion); no f-string SQL.
    - tests use stub_embedder (no live Voyage call).
  </acceptance_criteria>
  <done>models, retrieval (RRF hybrid + citation assembly), exact-lookup created; semantic + exact tests GREEN; SQL parameterized; D-15 stale flag carried.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conflict flag + override resolution + FastMCP server wiring</name>
  <read_first>
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-PATTERNS.md (§conflict.py D-13/D-14, §server.py KnowledgeMCP tool defs + boundary docstring)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md (§Pattern 1/2 FastMCP tools; §Anti-Patterns "self-arbitrating conflicts")
    - src/knowledge_mcp/models.py (created Task 1)
    - tests/conftest.py (db_pool, clean_knowledge_db, stub_embedder)
  </read_first>
  <behavior>
    - Test 1 (D-13): when retrieval returns passages from conflicting sources / a known conflict_id, the result has conflict=True and ALL conflicting citations are present (none dropped).
    - Test 2 (D-14): with a policy_resolution row for that conflict_id, resolved_by_override=True and the winning_source citation is ordered first.
    - Test 3 (D-13 no-arbitration): without an override row, conflict=True and resolved_by_override=False (MCP never picks a winner on its own).
  </behavior>
  <action>
    Create `src/knowledge_mcp/conflict.py`: `apply_conflict_flag(citations)` -> ConflictResult{has_conflict, resolved} detecting conflicting passages via known conflict_id / source disagreement / stale flag (D-13 — surface all, never drop). `async def apply_override(citations)` -> reordered citations: query `knowledge.policy_resolution` by the conflict_ids present (parameterized), and if a winning row exists put the winning_source citation first and mark resolved=True (D-14). MCP NEVER self-arbitrates — it only reorders when a human-populated override row exists.
    Create `src/knowledge_mcp/server.py`: boundary docstring ("KnowledgeMCP — Phase-4 calls these tools; never reads raw Confluence/Sheets directly" per CLAUDE.md); `mcp = FastMCP(name="KnowledgeMCP", on_duplicate_tools="error")`; tools (all readOnlyHint=True): `semantic_search(query, top_k=5)` -> hybrid_search -> assemble_citations -> apply_override (D-14) -> apply_conflict_flag (D-13) -> SemanticSearchResult{citations, conflict, resolved_by_override}; `lookup_threshold(threshold_id)` -> exact.lookup_threshold_row; `lookup_code(code)` -> exact.lookup_code_row; `get_template(code)` -> exact.fetch_template_row (D-11 keyed, not semantic). Expose the tool callables for direct unit-test import.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && pytest tests/knowledge_mcp/test_conflict.py tests/knowledge_mcp/test_override.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/knowledge_mcp/test_conflict.py` GREEN: conflicting passages -> conflict=True and ALL surfaced (D-13 no-drop, source assertion on citation count).
    - `tests/knowledge_mcp/test_override.py` GREEN: with a policy_resolution row, resolved_by_override=True and winning_source citation is first (D-14).
    - without an override row, resolved_by_override=False while conflict=True (D-13 no self-arbitration).
    - `server.py` declares semantic_search / lookup_threshold / lookup_code / get_template all with readOnlyHint=True (source assertion).
    - conflict.py override query is parameterized ($N) against knowledge.policy_resolution (no f-string SQL).
  </acceptance_criteria>
  <done>conflict flag + override resolution + FastMCP server with 4 read-only tools created; conflict + override tests GREEN; D-13 surface-all and D-14 override-wins both proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-4 caller -> Knowledge MCP | query string is untrusted free text reaching SQL FTS + embedding |
| Knowledge MCP -> Postgres | retrieval/exact/override SQL; injection risk if string-formatted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-T | Tampering | retrieval/exact/conflict SQL | mitigate | asyncpg `$N` parameterized queries incl. plainto_tsquery($1); never f-string SQL |
| T-03-02-HALL | Tampering (data integrity) | numeric thresholds | mitigate | D-10: lookup_threshold returns exact stored value, never inferred from prose |
| T-03-02-ARB | Repudiation/integrity | conflict resolution | mitigate | D-13 MCP never self-arbitrates; surfaces all + flag; only a human policy_resolution row reorders (D-14) |
| T-03-02-PI | Tampering | retrieved KB content (prompt-injection downstream) | accept | Out of Phase-3 scope; Phase-4 guards. Phase 3 returns cited+flagged passages so Phase-4 can react |
</threat_model>

<verification>
- `pytest tests/knowledge_mcp/ -x -q` green (KB-05, D-13, D-14, D-15).
- semantic_search citations carry source/authority/recency/snapshot metadata.
- lookup_threshold returns exact value (D-10); conflicts surface all + flag (D-13); override reorders (D-14).
- All four tools annotated readOnlyHint=True; all SQL parameterized.
</verification>

<success_criteria>
- KB-05: cited semantic search over the ingested store with source/recency/authority metadata.
- D-10 exact-lookup path for thresholds/code-map/templates.
- D-13/D-14/D-15 conflict-aware behavior wired and tested.
</success_criteria>

<output>
Create `.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-02-SUMMARY.md` when done.
</output>
