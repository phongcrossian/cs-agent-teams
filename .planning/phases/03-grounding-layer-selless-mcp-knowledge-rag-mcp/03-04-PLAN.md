---
phase: 03-grounding-layer-selless-mcp-knowledge-rag-mcp
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - tests/smoke/test_grounding_demo.py
  - src/selless_mcp/client.py
  - README-grounding.md
autonomous: false
requirements: [KB-03, KB-04, KB-05, SEL-01, SEL-02, SEL-03, SEL-04]

must_haves:
  truths:
    - "A single standalone script exercises both MCPs and asserts all four phase success criteria with no Phase-4 orchestrator"
    - "The mock-backed demo runs green in CI; the live variant runs only under the sandbox marker once Selless/Voyage creds exist"
    - "HttpSellessClient against the live Selless gateway is verified by a human before being trusted"
  artifacts:
    - path: "tests/smoke/test_grounding_demo.py"
      provides: "MCP-client smoke proving cited semantic query + exact threshold + template + scoped/audited Selless read"
      contains: "semantic_search"
    - path: "README-grounding.md"
      provides: "How to run both MCPs + the ingest CLI + the smoke demo (mock and live)"
      min_lines: 20
  key_links:
    - from: "tests/smoke/test_grounding_demo.py"
      to: "src/knowledge_mcp/server.py + src/selless_mcp/server.py"
      via: "direct tool calls acting as an MCP client"
      pattern: "lookup_threshold"
    - from: "tests/smoke/test_grounding_demo.py"
      to: "audit.selless_audit"
      via: "assert a redacted audit row was written after a Selless read"
      pattern: "selless_audit"
---

<objective>
Final slice: a standalone MCP-client smoke demo proving all four Phase-3 success criteria end-to-end without the (future) Phase-4 orchestrator — mirroring the Phase-2 sandbox-demo pattern. It ingests a seed set, then issues real tool calls against both MCPs: a cited semantic query that flags the warranty conflict (CONTRA-01), an exact threshold lookup, a template fetch, and a scoped/audited Selless read that proves whitelist + audit + rate-limit. A blocking human-verify checkpoint confirms the live Selless gateway + Voyage embeddings before the live variant is trusted.

Purpose: This is the phase's demonstrable end-state (Claude's-discretion requirement in CONTEXT.md). It closes the goal-backward loop: every success criterion is asserted by an executable that any reviewer can run.
Output: `tests/smoke/test_grounding_demo.py` (GREEN mock; sandbox-gated live), the live `HttpSellessClient` wiring confirmed, and `README-grounding.md` run instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-VALIDATION.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-SELLESS-API.md

<interfaces>
<!-- Tool surfaces created by Plans 01-03. The smoke demo acts as an MCP client calling these. -->

Knowledge MCP (src/knowledge_mcp/server.py): semantic_search(query, top_k) -> SemanticSearchResult{citations, conflict, resolved_by_override}; lookup_threshold(threshold_id) -> ThresholdResult{value, source, conflict_id, ...}; lookup_code(code); get_template(code) -> TemplateResult.
Ingest (src/ingest/cli.py): python -m src.ingest.cli re-ingest (builds the store from Phase-1 snapshots).
Selless MCP (src/selless_mcp/server.py): get_order_status(order_id) -> OrderDetail (whitelisted); get_customer_info / get_purchase_history / get_ticket_mapping / resolve_order. Middleware writes audit.selless_audit per call.
src/selless_mcp/client.py: MockSellessClient (fixtures) + HttpSellessClient (live, gateway-trust). The demo defaults to Mock; live path behind @pytest.mark.sandbox.
audit.selless_audit (migration 0003): redacted rows.

Known demo anchors (03-RESEARCH.md §Standalone Demonstrable End-State):
- semantic_search("warranty window") -> >=1 Citation with source + authority_rank + conflict=True (CONTRA-01 is a known HIGH conflict — the demo case).
- lookup_threshold("THR-03") -> value "45 days from purchase" (D-10 exact).
- get_template("C1") -> a scaffold (D-11).
- get_order_status(order_id) on Mock -> whitelisted fields only + a redacted audit row + rate-limit kicks in past the bucket (SEL-04).

tests/conftest.py: db_pool, mock_selless_client, stub_embedder (mock path), clean_knowledge_db. The `sandbox` marker is skipped unless RUN_SANDBOX=1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standalone MCP-client smoke demo (mock-backed) + README</name>
  <read_first>
    - tests/test_e2e_sandbox.py (Phase-2 sandbox-demo pattern to mirror — structure + sandbox marker usage)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md (§Standalone Demonstrable End-State — the 4 assertions)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-VALIDATION.md (§Standalone Demonstrable End-State + §Manual-Only Verifications)
    - src/knowledge_mcp/server.py + src/selless_mcp/server.py (tool callables to invoke)
    - tests/conftest.py (db_pool, mock_selless_client, stub_embedder, clean_knowledge_db)
  </read_first>
  <action>
    Implement `tests/smoke/test_grounding_demo.py` (replacing the Plan-00 RED stub). Default mock-backed test (NOT sandbox-marked) that: seeds a minimal knowledge store (ingest a small fixture set or call ingest over a tiny subset; the warranty THR-03/THR-04 + CONTRA-01 rows + a C1 template + a couple prose chunks must be present, using stub_embedder so no Voyage call), then acting as an MCP client asserts the four criteria — (1) semantic_search over a warranty query returns >=1 Citation carrying source + authority_rank and conflict=True (criterion #1 + D-13); (2) lookup_threshold("THR-03") returns the exact "45 days" value (D-10); (3) get_template("C1") returns a scaffold (D-11); (4) get_order_status against mock_selless_client returns whitelisted fields only, a redacted audit.selless_audit row is written (criterion #4 SEL-04), and exceeding the rate-limit burst throttles. A second `@pytest.mark.sandbox` test runs the same flow against the LIVE HttpSellessClient + live Voyage (skipped unless RUN_SANDBOX=1).
    Create `README-grounding.md`: how to run `alembic upgrade head`, `python -m src.ingest.cli re-ingest`, start each MCP server, and run the smoke demo (mock: `pytest tests/smoke/test_grounding_demo.py`; live: `RUN_SANDBOX=1 pytest tests/smoke -m sandbox`). Document the D-05 composition note (ticket-do mapping from Selless -> ticket content from the Phase-2 Freshdesk client in Phase 4).
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && pytest tests/smoke/test_grounding_demo.py -x -q && pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/smoke/test_grounding_demo.py` mock test GREEN: all four assertions pass (semantic+conflict, exact threshold "45 days", template C1, scoped Selless read + redacted audit row + rate-limit).
    - The full suite `pytest tests/` is green (mock-backed); the live variant is sandbox-marked and skipped in CI (source assertion: `@pytest.mark.sandbox` present).
    - A redacted `audit.selless_audit` row is asserted after the Selless read (no raw email/phone — SEL-04/D-06).
    - `README-grounding.md` documents the ingest CLI, both MCP run commands, mock + live smoke commands, and the D-05 Freshdesk-composition note.
  </acceptance_criteria>
  <done>Standalone smoke demo proves all 4 success criteria mock-backed and green; live variant sandbox-gated; README written; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live gateway + Voyage verification</name>
  <read_first>
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-VALIDATION.md (§Manual-Only Verifications)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-SELLESS-API.md (§1 gateway-trust model, §5 Q1 D-05 ticket-content split)
    - src/selless_mcp/client.py (HttpSellessClient — confirm endpoint paths match live shapes)
  </read_first>
  <what-built>
    The mock-backed smoke demo is green and `HttpSellessClient` is wired to the confirmed `/public/tickets` endpoints (`/po/{id}`, `/customer/{id}`, `/po/search` for resolve_order, `/{id}/ticket-do`). Voyage embeddings use `voyage-3-large`. Neither has been exercised against the live gateway/API in CI (the Selless API is gateway-gated; Voyage costs credits) — both are behind `@pytest.mark.sandbox`.
  </what-built>
  <how-to-verify>
    1. Confirm network/gateway access to `https://api.selless.dev/admin/csm/order/public/tickets` from the run environment (the API has no token — access is gateway-gated). If a gateway header/VPN is required, set `SELLESS_API_GATEWAY_KEY` in `.env`.
    2. Set `VOYAGE_API_KEY` in `.env`.
    3. Run `RUN_SANDBOX=1 pytest tests/smoke/test_grounding_demo.py -m sandbox -x -q`.
    4. Confirm: a live `get_order_status` returns the expected whitelisted shape (no payment/cost/supplier leakage) and a live `semantic_search` returns cited passages. Confirm the live OrderDetail field shapes still match the mock fixtures (flag any drift).
  </how-to-verify>
  <resume-signal>Type "approved" once the live sandbox smoke passes (or "mock-only" to ship Phase 3 on the mock path and defer live wiring to deployment).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke demo -> live Selless gateway | first real cross of the gateway-trust boundary; field-shape drift could bypass the whitelist |
| smoke demo -> live Voyage | document text crosses to third-party embedding API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-DRIFT | Information Disclosure | HttpSellessClient live shapes | mitigate | Task 2 human-verify confirms live OrderDetail matches mock fixtures; whitelist is explicit-extraction so unknown new fields are dropped, not leaked |
| T-03-04-GATE | Spoofing/Access | live gateway access | mitigate | gateway-trust acknowledged; optional SELLESS_API_GATEWAY_KEY config hook; live path sandbox-gated, not in CI |
| T-03-04-ID | Information Disclosure | live smoke logs | mitigate | redact_text already applied in audit path; demo asserts redacted audit row even on the live run |
</threat_model>

<verification>
- `pytest tests/smoke/test_grounding_demo.py -x -q` green (mock); full `pytest tests/` green.
- All four success criteria asserted by the standalone demo.
- Live sandbox smoke gated behind human-verify (Task 2) and RUN_SANDBOX=1.
</verification>

<success_criteria>
- All four Phase-3 success criteria proven standalone (no Phase-4 orchestrator): cited semantic query + conflict flag, exact threshold, template fetch, scoped/audited/rate-limited Selless read.
- Live Selless gateway + Voyage path verified by a human before being trusted.
- README documents how to run the grounding layer.
</success_criteria>

<output>
Create `.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-04-SUMMARY.md` when done.
</output>
