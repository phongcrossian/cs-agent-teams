---
phase: 03-grounding-layer-selless-mcp-knowledge-rag-mcp
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/config.py
  - migrations/versions/0002_knowledge_schema.py
  - migrations/versions/0003_selless_audit.py
  - tests/conftest.py
  - tests/ingest/__init__.py
  - tests/knowledge_mcp/__init__.py
  - tests/selless_mcp/__init__.py
  - tests/smoke/__init__.py
autonomous: false
requirements: [KB-03, KB-04, KB-05, SEL-01, SEL-02, SEL-03, SEL-04]
user_setup:
  - service: voyage
    why: "voyage-3-large embeddings for the Knowledge RAG store (CLAUDE.md mandate). Build/unit tests use a stub embedder; the live embed path needs this key."
    env_vars:
      - name: VOYAGE_API_KEY
        source: "Voyage AI dashboard (dashboard.voyageai.com) -> API Keys"
  - service: selless
    why: "Live Selless reads. Build/test runs against MockSellessClient; live HttpSellessClient wiring needs gateway/network access (no token — gateway-trust model)."
    env_vars:
      - name: SELLESS_API_BASE_URL
        source: "Confirmed: https://api.selless.dev/admin/csm/order/public/tickets (gateway-gated)"

must_haves:
  truths:
    - "uv adds fastmcp, voyageai, pgvector to pyproject.toml and the env imports them"
    - "alembic upgrade head creates knowledge.* and audit.* schemas with pgvector + pg_trgm extensions"
    - "Settings exposes Selless + Voyage + rate-limit fields with new secrets redacted in __repr__"
    - "Every Phase-3 test target file exists (RED) so downstream plans run against real failing tests"
  artifacts:
    - path: "migrations/versions/0002_knowledge_schema.py"
      provides: "knowledge schema: kb_chunk(vector/FTS/trgm), policy_threshold, code_map, template_library, policy_resolution"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: "migrations/versions/0003_selless_audit.py"
      provides: "audit.selless_audit PII-redacted table"
      contains: "CREATE TABLE audit.selless_audit"
    - path: "src/config.py"
      provides: "Selless + Voyage + rate-limit settings, secrets redacted"
      contains: "voyage_api_key"
    - path: "tests/conftest.py"
      provides: "mock_selless_client, stub_embedder, clean_knowledge_db, pgvector codec registration"
      contains: "mock_selless_client"
  key_links:
    - from: "tests/conftest.py db_pool"
      to: "pgvector.asyncpg.register_vector"
      via: "pool init codec registration"
      pattern: "register_vector"
    - from: "migrations/0002"
      to: "migrations/0001"
      via: "down_revision chain"
      pattern: "down_revision = \"0001\""
---

<objective>
Wave 0 bootstrap for Phase 3: install the locked new dependencies (behind a supply-chain human-verify gate), create the two Alembic revisions (`knowledge.*` + `audit.*` schemas with pgvector/pg_trgm) in the SAME Postgres instance Phase 2 stood up (D-09 — no second datastore), extend `Settings` with Selless/Voyage/rate-limit config, and scaffold every Phase-3 test file as failing RED stubs so Plans 01–04 execute against real tests (Nyquist).

Purpose: Every downstream plan depends on the schemas, config fields, and test scaffolding created here. Dependency install is the only supply-chain attack surface in this phase, so it is gated by a blocking human-verify checkpoint per the package legitimacy gate.
Output: Updated `pyproject.toml`, two migration files applied via `alembic upgrade head`, extended `src/config.py`, scaffolded `tests/{ingest,knowledge_mcp,selless_mcp,smoke}/` failing test stubs, extended `tests/conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-PATTERNS.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-VALIDATION.md
@.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-SELLESS-API.md

<interfaces>
<!-- Existing contracts the executor builds against. Do NOT re-explore the codebase. -->

src/config.py — Settings(BaseSettings) singleton `settings`. Existing fields: database_url, freshdesk_domain, freshdesk_api_key, webhook_secret, send_mode, selless_sync_user_ids, retry_max_attempts, poller_interval_seconds, per_ticket_reply_throttle_*. __repr__ (~lines 101-109) redacts freshdesk_api_key + webhook_secret. model_config extra="ignore". EXTEND IN PLACE.

migrations/versions/0001_initial_queue_schema.py — Alembic pattern: module-level revision/down_revision/branch_labels/depends_on; upgrade()/downgrade() using op.execute("CREATE SCHEMA IF NOT EXISTS ..."); DROP ... IF EXISTS reverse-order in downgrade(). queue.* tables; alembic env already wired.

tests/conftest.py — fixtures: db_pool (function-scoped asyncpg pool, reads DATABASE_URL, replaces postgresql+asyncpg:// → postgresql://), clean_db (truncates queue.*), respx_mock (base_url https://testdomain.freshdesk.com). pytest_collection_modifyitems skips `sandbox` marker unless RUN_SANDBOX=1.

src/guards/pii.py — `redact_text(text: str) -> str` (Presidio; redact_text("") returns "" — safe on empty). Lazy `_get_engines()` singleton.

pyproject.toml [tool.pytest.ini_options]: asyncio_mode="auto", asyncio_default_fixture_loop_scope="function", markers=["sandbox: ..."], testpaths=["tests"].
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Supply-chain verify new dependencies before install</name>
  <read_first>
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md (§Package Legitimacy Audit — fastmcp/mcp/voyageai/pgvector all [ASSUMED]; §Standard Stack versions)
  </read_first>
  <what-built>
    Nothing automated yet — this is the package legitimacy gate (slopcheck was unavailable in research, so all four new packages are [ASSUMED]). Deps to add: `fastmcp` (~3.3.1), `voyageai` (~0.3.7), `pgvector` (asyncpg codec binding). `mcp` (~1.27.2) comes transitively with fastmcp.
  </what-built>
  <how-to-verify>
    1. https://pypi.org/project/fastmcp/ — confirm source repo github.com/jlowin/fastmcp (PrefectHQ), recent releases, healthy downloads.
    2. https://pypi.org/project/voyageai/ — confirm official github.com/voyage-ai/voyageai-python, latest 0.3.7 (2025-12-16).
    3. https://pypi.org/project/pgvector/ — confirm official github.com/pgvector/pgvector-python binding (dist name for the asyncpg `register_vector` codec).
    4. https://pypi.org/project/mcp/ — confirm official github.com/modelcontextprotocol/python-sdk, v1.x line (NOT v2 — CLAUDE.md forbids v2).
    Confirm none are typosquats and all map to the CLAUDE.md-expected source repos.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the install in Task 1, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install deps + extend Settings config</name>
  <read_first>
    - src/config.py (the Settings class being extended + __repr__ + model_config)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-PATTERNS.md (§src/config.py extend-in-place: exact new fields + __repr__ extension)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md (§Pitfall 1 voyageai Python 3.14 <3.15 cap)
  </read_first>
  <action>
    Run `uv add fastmcp voyageai pgvector` (only after Task 0 approval). If the voyageai resolver pins an old 0.2.x due to the `requires-python <3.15` cap on Python 3.14 (Pitfall 1), pin `voyageai==0.3.7`, confirm `python -c "import voyageai; voyageai.Client"` imports, and document the resolution in the SUMMARY. Run `uv sync`.
    Extend `src/config.py` Settings IN PLACE (do not create a new file) with these fields, in the existing `Field(default=..., description=...)` style: `selless_api_base_url` (default `https://api.selless.dev/admin/csm/order/public/tickets`, per D-01 confirmed gateway-trust model), `selless_api_gateway_key` (default "", NEVER log — reserved for a prod gateway/VPN header), `voyage_api_key` (default "", NEVER log), `voyage_model` (default `voyage-3-large`), `voyage_output_dimension` (default 1024), `selless_rate_limit_rps` (default 1.0, D-08 discretion), `selless_rate_limit_burst` (default 10, D-08). Extend `__repr__` to add `selless_api_base_url` (visible) and `<REDACTED>` for `selless_api_gateway_key` and `voyage_api_key`.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && python -c "import fastmcp, voyageai, mcp, pgvector; from src.config import settings; assert settings.voyage_model == 'voyage-3-large'; r = repr(settings); assert 'voyage_api_key=<REDACTED>' in r and 'selless_api_gateway_key=<REDACTED>' in r; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `import fastmcp, voyageai, mcp, pgvector` all succeed under Python 3.14 (behavior).
    - `settings.selless_api_base_url` equals the confirmed Selless `/public/tickets` URL (source assertion).
    - `repr(settings)` contains `voyage_api_key=<REDACTED>` and `selless_api_gateway_key=<REDACTED>`; no real key value appears (CLI assertion above).
    - resolved `mcp` version is on the v1.x line (NOT 2.x — CLAUDE.md forbids v2).
  </acceptance_criteria>
  <done>Deps installed and importable; Settings extended with all 7 new fields; both new secrets redacted in __repr__; verify command prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic revisions 0002 (knowledge) + 0003 (audit) and apply</name>
  <read_first>
    - migrations/versions/0001_initial_queue_schema.py (exact structure to mirror: revision chain, op.execute CREATE SCHEMA, downgrade reverse-order DROP IF EXISTS)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-PATTERNS.md (§migrations/0002 + §migrations/0003 — full table DDL incl. HNSW/FTS/trgm indexes and audit table)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-RESEARCH.md (§Pitfall 2 pgvector extension + codec)
  </read_first>
  <action>
    Create `migrations/versions/0002_knowledge_schema.py` (revision "0002", down_revision "0001"): `CREATE SCHEMA IF NOT EXISTS knowledge`, `CREATE EXTENSION IF NOT EXISTS vector`, `CREATE EXTENSION IF NOT EXISTS pg_trgm`. Tables per 03-PATTERNS.md §0002: `knowledge.kb_chunk` (id BIGSERIAL PK, content_hash TEXT, source, source_type, authority_rank INT [D-12], recency_flag TEXT [D-15], body TEXT, body_tsv TSVECTOR GENERATED ALWAYS AS to_tsvector('english', body) STORED, embedding VECTOR(1024), metadata JSONB, snapshot_version TEXT, created_at/updated_at TIMESTAMPTZ) with a UNIQUE index on content_hash (idempotent upsert, KB-04), an HNSW index `USING hnsw (embedding vector_cosine_ops) WITH (m=16, ef_construction=64)`, a GIN FTS index on body_tsv, and a GIN trgm index on body. `knowledge.policy_threshold` (threshold_id PK [D-10], label, value TEXT, source, authority_rank, conflict_id, snapshot_version). `knowledge.code_map` (code PK [D-10], action, template_code, source, snapshot_version). `knowledge.template_library` (code PK [D-11], scenario, subject_template, body_template, source, authority_rank DEFAULT 2, snapshot_version). `knowledge.policy_resolution` (conflict_id PK [D-14], winning_source, resolved_value, resolved_by, resolved_at, notes). downgrade() drops all in reverse order with IF EXISTS.
    Create `migrations/versions/0003_selless_audit.py` (revision "0003", down_revision "0002"): `CREATE SCHEMA IF NOT EXISTS audit`, `audit.selless_audit` (id BIGSERIAL PK, tool TEXT, input_key TEXT [PII-redacted per D-06], fields_returned TEXT [PII-redacted], latency_ms FLOAT, outcome TEXT, caller TEXT, created_at TIMESTAMPTZ) + index on (tool, created_at). downgrade() drops in reverse.
    Run `alembic upgrade head` [BLOCKING — must succeed before any integration verification in Plans 01–04].
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && alembic upgrade head && python -c "
import asyncio, asyncpg
async def m():
    c = await asyncpg.connect('postgresql://csbot:csbot@localhost:5432/csbot')
    for t in ['knowledge.kb_chunk','knowledge.policy_threshold','knowledge.code_map','knowledge.template_library','knowledge.policy_resolution','audit.selless_audit']:
        await c.fetch(f'SELECT 1 FROM {t} LIMIT 1')
    ext = await c.fetch(\"SELECT extname FROM pg_extension WHERE extname IN ('vector','pg_trgm')\")
    assert {r['extname'] for r in ext} == {'vector','pg_trgm'}
    await c.close(); print('OK')
asyncio.run(m())"</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` succeeds with chain 0001→0002→0003 (behavior).
    - All six tables are queryable (CLI assertion above).
    - Extensions `vector` and `pg_trgm` present in `pg_extension` (CLI assertion).
    - `knowledge.kb_chunk` has an HNSW index on `embedding` and a UNIQUE index on `content_hash` (source assertion in migration file).
    - Phase-2 `queue.*` tables untouched.
  </acceptance_criteria>
  <done>Both migrations created, chain 0001→0002→0003, `alembic upgrade head` applied, all six tables + both extensions verified by the CLI command printing OK.</done>
</task>

<task type="auto">
  <name>Task 3: Test scaffolding — failing RED stubs + conftest fixtures</name>
  <read_first>
    - tests/conftest.py (existing db_pool / clean_db / respx_mock + sandbox skip — extend in place)
    - tests/test_client.py (pytest-asyncio test-file pattern to mirror)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-PATTERNS.md (§tests/conftest.py additions: mock_selless_client, stub_embedder, clean_knowledge_db, selless_respx_mock)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-VALIDATION.md (§Per-Task Verification Map + §Wave 0 Requirements — exact test file names)
    - .planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-SELLESS-API.md (§2/§4 — fixture DTO shapes for MockSellessClient)
  </read_first>
  <action>
    Extend `tests/conftest.py` IN PLACE: register the pgvector codec inside `db_pool` (pass an `init=` setup callback to `asyncpg.create_pool` that calls `from pgvector.asyncpg import register_vector; await register_vector(conn)`, per Pitfall 2) so vector reads/writes round-trip; add `mock_selless_client` (returns `src.selless_mcp.client.MockSellessClient`), `stub_embedder` (monkeypatches `src.knowledge_mcp.embeddings.embed_query`/`embed_documents` to return 1024-dim zero vectors — avoids Voyage calls in unit tests), `clean_knowledge_db` (TRUNCATE knowledge.* + audit.* RESTART IDENTITY CASCADE), and `selless_respx_mock` (respx mock at base_url https://api.selless.dev). Reuse the single existing `sandbox` marker for live Voyage/Selless smoke — do NOT add new markers.
    Create package inits `tests/{ingest,knowledge_mcp,selless_mcp,smoke}/__init__.py`. Create FAILING RED stub test files (each imports the not-yet-existing target module so collection errors RED, mapped to 03-VALIDATION.md rows): `tests/ingest/test_pipeline.py`, `tests/ingest/test_idempotent.py`, `tests/knowledge_mcp/test_semantic.py`, `tests/knowledge_mcp/test_exact.py`, `tests/knowledge_mcp/test_conflict.py`, `tests/knowledge_mcp/test_override.py`, `tests/selless_mcp/test_tools.py`, `tests/selless_mcp/test_resolve_scope.py`, `tests/selless_mcp/test_whitelist.py`, `tests/selless_mcp/test_audit.py`, `tests/selless_mcp/test_rate_limit.py`, `tests/smoke/test_grounding_demo.py` (mark `@pytest.mark.sandbox`). Each stub's assertion text documents the requirement contract it will later satisfy. Do NOT create any `src/` Phase-3 modules here — that is Plans 01–03.
  </action>
  <verify>
    <automated>cd /Users/admin/work/phong/grok-agent-teams && for f in tests/ingest/test_pipeline.py tests/ingest/test_idempotent.py tests/knowledge_mcp/test_semantic.py tests/knowledge_mcp/test_exact.py tests/knowledge_mcp/test_conflict.py tests/knowledge_mcp/test_override.py tests/selless_mcp/test_tools.py tests/selless_mcp/test_resolve_scope.py tests/selless_mcp/test_whitelist.py tests/selless_mcp/test_audit.py tests/selless_mcp/test_rate_limit.py tests/smoke/test_grounding_demo.py; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done && grep -q "register_vector" tests/conftest.py && grep -q "def mock_selless_client" tests/conftest.py && pytest tests/test_client.py tests/test_queue.py -q 2>&1 | tail -3 && echo "SCAFFOLD-OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 12 new test files exist (CLI assertion above) and each is RED today (imports a not-yet-existing `src/` module).
    - `tests/conftest.py` contains `register_vector` (pgvector codec registered on db_pool) and `def mock_selless_client` (source assertions, grep above).
    - Existing Phase-2 tests `tests/test_client.py` + `tests/test_queue.py` still pass (no regression from conftest edits).
    - `tests/smoke/test_grounding_demo.py` carries `@pytest.mark.sandbox`.
    - No new `src/` Phase-3 module created in this plan.
  </acceptance_criteria>
  <done>conftest extended with pgvector codec + 4 new fixtures; 12 RED stubs + 4 package inits created; Phase-2 tests still green; verify command prints SCAFFOLD-OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip/uv registry → local env | New packages (fastmcp/voyageai/pgvector/mcp) cross an untrusted supply-chain boundary on install |
| env/.env → Settings | New secrets (voyage_api_key, selless_api_gateway_key) must never reach logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | uv add fastmcp/voyageai/pgvector | mitigate | Task 0 blocking-human verify on PyPI source repos (all [ASSUMED]); never auto-approve |
| T-03-00-ID | Information Disclosure | Settings.__repr__ | mitigate | voyage_api_key + selless_api_gateway_key redacted in __repr__ (verified by CLI assertion in Task 1) |
| T-03-00-T | Tampering | Alembic migration SQL | mitigate | DDL is static op.execute strings; no user input interpolated; chain 0001→0002→0003 verified |
</threat_model>

<verification>
- `alembic upgrade head` green; chain 0001→0002→0003.
- `python -c "import fastmcp, voyageai, mcp, pgvector"` succeeds.
- All six new tables + `vector`/`pg_trgm` extensions present.
- 12 RED test stubs exist; Phase-2 tests still pass.
</verification>

<success_criteria>
- New deps installed (gated by Task 0 human-verify) and importable.
- `knowledge.*` + `audit.*` schemas migrated and applied.
- `Settings` extended; both new secrets redacted.
- Phase-3 test scaffold (12 RED stubs + 4 fixtures) in place for Plans 01–04 Nyquist verification.
</success_criteria>

<output>
Create `.planning/phases/03-grounding-layer-selless-mcp-knowledge-rag-mcp/03-00-SUMMARY.md` when done.
</output>
