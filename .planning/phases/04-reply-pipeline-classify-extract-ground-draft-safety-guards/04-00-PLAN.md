---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/file_store/__init__.py
  - src/file_store/template_store.py
  - tests/test_file_store.py
  - pyproject.toml
autonomous: true
requirements: [REP-03]
must_haves:
  truths:
    - "The drafter can fetch a reply template body by code from the local snapshot files (no MCP, no DB, no network)"
    - "The drafter can resolve a customer_request sub-type to a candidate template code via the local CODE-MAP wiring"
    - "voyageai and the RAG use of pgvector are removed from pyproject.toml dependencies"
  artifacts:
    - path: "src/file_store/template_store.py"
      provides: "Local file-store loader: get_template_from_file(code) + subtype_to_code(sub_type) reading snapshots + CODE-MAP-templates.md"
      min_lines: 60
    - path: "tests/test_file_store.py"
      provides: "Unit tests proving get_template_from_file returns a real template body for a known code and a flow signal for an unknown code"
      min_lines: 40
  key_links:
    - from: "src/file_store/template_store.py"
      to: ".planning/phases/01-knowledge-survey-conflict-inventory/snapshots/*.md"
      via: "file read keyed on template heading"
      pattern: "snapshots"
    - from: "src/file_store/template_store.py"
      to: ".planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP-templates.md"
      via: "code → snapshot-file + heading lookup table"
      pattern: "CODE-MAP-templates"
---

<objective>
Build the local **Template + Workflow/CODE-MAP file-store** that replaces the semantic-RAG Knowledge MCP as the drafter's grounding surface (D-31), and strip the RAG dependencies (`voyageai`, RAG-use `pgvector`) from `pyproject.toml`.

Purpose: D-31 retires the Knowledge MCP from the runtime grounding path. The drafter must read the 26 local template snapshots + the CODE-MAP wiring directly from files — no `semantic_search`, no embeddings, no pgvector/Voyage. This plan creates the deterministic file-store module every downstream slice depends on.

Output: `src/file_store/template_store.py` (the keyed local lookup), its tests, and a cleaned `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.claude/CLAUDE.md

<interfaces>
<!-- The CODE-MAP-templates.md table is the wiring source. Each row maps a code to a snapshot file + verbatim template heading. -->
<!-- From .planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP-templates.md (columns): Code | Template Heading (verbatim) | Product Line | Snapshot File | Notes -->
<!-- Snapshot files live in: .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/*.md (26 markdown files). -->
<!-- Existing knowledge_mcp exact lookups to mirror behavior (NOT to import — the file-store is standalone): -->
<!-- src/knowledge_mcp/exact.py: fetch_template_row, lookup_code_row (these read the DB; the file-store reads files instead). -->
<!-- Sub-type → primary template code mapping already documented in .claude/skills/ground-and-draft/SKILL.md "Sub-type → template code mapping" table — reuse those code groupings. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the local template file-store loader</name>
  <files>src/file_store/__init__.py, src/file_store/template_store.py, tests/test_file_store.py</files>
  <read_first>
    - .planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP-templates.md (the code → snapshot-file + heading wiring table — source of truth for the lookup index)
    - .planning/phases/01-knowledge-survey-conflict-inventory/snapshots/product complaint-within guarantee-template2.md (a real snapshot to confirm heading format, e.g. `B7-All products-Cannot replace`)
    - .claude/skills/ground-and-draft/SKILL.md (the existing "Sub-type → template code mapping" table to reuse for subtype_to_code)
    - src/knowledge_mcp/exact.py (existing exact-lookup behavior to mirror — DO NOT import it; the file-store is standalone, file-based)
    - src/config.py (Settings singleton + repo-root path convention)
  </read_first>
  <behavior>
    - get_template_from_file("B7") returns a dict {code, heading, body, snapshot_file} where body is the verbatim template text read from the snapshot file section, not a DB row.
    - get_template_from_file("ZZZ") (unknown code) returns {code: "ZZZ", found: False, body: None} — never raises, never fabricates a body.
    - subtype_to_code("Return") returns a non-empty ordered list of candidate codes (e.g. ["B5","B6","B7","B3", ...]) per the CODE-MAP-templates wiring.
    - subtype_to_code("Review") returns [] (Phase-1 confirmed gap — no template exists).
    - Loader resolves snapshot paths relative to the Phase-1 snapshots dir; missing snapshot file → found: False (fail-soft, no fabrication).
  </behavior>
  <action>
    Create `src/file_store/` package with `template_store.py`. Build an index of `{code -> (snapshot_file, heading)}` parsed from `CODE-MAP-templates.md` rows (the wiring table). Expose `get_template_from_file(code: str) -> dict` that reads the named snapshot file under `.planning/phases/01-knowledge-survey-conflict-inventory/snapshots/`, extracts the section under the verbatim heading, and returns `{code, heading, body, snapshot_file, found}`. Expose `subtype_to_code(sub_type: str) -> list[str]` returning the ordered candidate codes per the SKILL.md sub-type→code groupings; `Review` returns `[]`. Resolve the snapshots directory via a module-level constant anchored to the repo root (mirror `src/config.py` path convention). NO network, NO DB, NO embeddings, NO import of `src.knowledge_mcp`. Fail-soft on any missing file/heading (return `found: False`, never fabricate). Add `tests/test_file_store.py` covering the five behaviors above.
  </action>
  <verify>
    <automated>uv run pytest tests/test_file_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_file_store.py -x` passes.
    - `uv run python -c "from src.file_store.template_store import get_template_from_file; r=get_template_from_file('B7'); assert r['found'] and r['body'], r"` exits 0 and prints nothing to stderr.
    - `grep -n "import" src/file_store/template_store.py | grep -i "knowledge_mcp\|voyage\|pgvector\|semantic"` returns nothing (no RAG/MCP imports).
    - `subtype_to_code("Review")` returns `[]` (asserted by a test).
  </acceptance_criteria>
  <done>The file-store loads real template bodies by code from snapshot files, resolves sub-types to candidate codes, fails soft on unknown codes, and imports no RAG/MCP/network code.</done>
</task>

<task type="auto">
  <name>Task 2: Strip RAG dependencies from pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current dependency list — lines around the `voyageai==0.3.7` and `pgvector>=0.4.2` entries)
    - CLAUDE.md (root — the "stack deltas" pivot banner listing voyageai/pgvector-as-RAG/Ragas as no-longer-used)
  </read_first>
  <action>
    Remove the `voyageai==0.3.7` dependency line and the RAG-use `pgvector>=0.4.2` dependency line from `pyproject.toml` per the D-29 stack delta. If `ragas` appears as a dependency, remove it too. Keep all non-RAG dependencies (anthropic, fastmcp, httpx, tenacity, presidio-*, asyncpg, pydantic-settings, pytest) untouched. Do NOT touch the `src/knowledge_mcp/` server code in this plan — it is archived-not-wired, handled by the settings.json change in 04-01; this task only removes the package manager deps so no embedding/vector library is installed for the PoC.
  </action>
  <verify>
    <automated>grep -v '^#' pyproject.toml | grep -ci "voyage\|ragas"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' pyproject.toml | grep -i "voyageai"` returns nothing.
    - `grep -v '^#' pyproject.toml | grep -i "ragas"` returns nothing.
    - `uv run python -c "import src.config"` still exits 0 (non-RAG deps intact).
    - Non-RAG deps (anthropic, fastmcp, presidio-analyzer) still present: `grep -c "anthropic\|fastmcp\|presidio" pyproject.toml` is ≥ 3.
  </acceptance_criteria>
  <done>voyageai and RAG-use pgvector/ragas are removed from pyproject.toml; the project still imports and the non-RAG stack is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → drafter | Template snapshot files are trusted developer-curated content (not attacker input); read-only |
| (none new) | This plan adds no new network or untrusted-input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | template_store reads snapshot files by path | mitigate | Resolve snapshot paths only from a fixed repo-anchored constant + CODE-MAP wiring index; never from runtime/ticket-supplied input — no path traversal from untrusted data |
| T-04-00-02 | Information Disclosure | template bodies contain no PII | accept | Snapshots are policy/template scaffolds, not customer data; no PII at rest in this module |
| T-04-00-SC | Tampering | dependency removal in pyproject.toml | mitigate | Only removes packages (voyageai/pgvector/ragas); adds no new package — no new supply-chain surface. No `[ASSUMED]`/`[SUS]` installs in this plan |
</threat_model>

<verification>
- File-store unit tests pass; a known code returns a real body; an unknown code fails soft.
- pyproject.toml has no voyageai/ragas; the project still imports.
- No RAG/MCP/network imports in the file-store module.
</verification>

<success_criteria>
The local Template + CODE-MAP file-store exists and is the standalone, deterministic grounding surface the drafter will use (D-31). RAG dependencies are gone from pyproject.toml.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-00-SUMMARY.md` when done.
</output>
