---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/reply_mcp/__init__.py
  - src/reply_mcp/server.py
  - .claude/settings.json
  - .claude/CLAUDE.md
  - CLAUDE.md
  - tests/fixtures/sample_tickets.py
  - tests/cs_team/__init__.py
  - tests/cs_team/test_hooks_red.py
  - tests/cs_team/test_team_kit_structure.py
  - tests/cs_team/test_settings_hook_bindings.py
  - pyproject.toml
autonomous: true
requirements: []
scaffolds_for: [REP-01, REP-02, REP-03, REP-04, SAFE-03, SAFE-04]

must_haves:
  truths:
    - "src/config.py exposes per-stage Claude model settings (Haiku classify/extract, Sonnet draft/critic/lead) and a DRY_RUN flag, all env-driven"
    - "Settings.__repr__ never prints the anthropic_api_key value"
    - "src/reply_mcp exposes a submit_reply(body, citations) tool — the ONLY path to emit a customer draft; in DRY_RUN it persists to queue.dry_run_log"
    - ".claude/settings.json registers three MCP servers (KnowledgeMCP, SellessMCP, ReplyMCP) and binds ALL FIVE hooks with the §4a events"
    - ".claude/CLAUDE.md holds the agent-team safety contract; root CLAUDE.md orchestration row updated to the agent-team runtime"
    - "Sample benign + high-risk + injection tickets exist as importable fixtures"
    - "A structural test asserts all five hooks are bound in settings.json with correct events; RED hook + team-kit stubs fail until later waves build them"
  artifacts:
    - path: "src/config.py"
      provides: "anthropic_api_key, claude_model_classify, claude_model_draft, claude_model_lead, dry_run"
      contains: "claude_model_lead"
    - path: "src/reply_mcp/server.py"
      provides: "FastMCP ReplyMCP with submit_reply(body, citations) — DRY_RUN persists to queue.dry_run_log"
      contains: "submit_reply"
    - path: ".claude/settings.json"
      provides: "3 MCP servers + all-five-hook bindings (PreToolUse submit_reply chain, UserPromptSubmit, PostToolUse/SubagentStop, PostToolUse pii) + DRY_RUN env"
      contains: "submit_reply"
    - path: ".claude/CLAUDE.md"
      provides: "agent-team safety contract (team-scoped, not root)"
    - path: "tests/cs_team/test_settings_hook_bindings.py"
      provides: "structural assertion that all five hooks are bound with correct events"
      contains: "submit_reply"
  key_links:
    - from: ".claude/settings.json"
      to: "src/knowledge_mcp/server.py + src/selless_mcp/server.py + src/reply_mcp/server.py"
      via: "uv run python -m src.<module>.server launch command"
      pattern: "src\\.reply_mcp\\.server"
    - from: ".claude/settings.json PreToolUse(submit_reply)"
      to: "grounding_check.py + pre_send_guard.py + final-risk check"
      via: "ordered hook chain that exits 2 to block → escalate"
      pattern: "submit_reply"
    - from: "src/reply_mcp/server.py"
      to: "queue.dry_run_log via src/work_queue/send.py _dry_run pattern"
      via: "DRY_RUN persistence with redact_text"
      pattern: "dry_run_log|redact_text"
---

<objective>
Wave-0 bootstrap cho cs-agent-team: dựng nền tảng + chokepoint an toàn để các wave sau xây 5 hooks và team agent mà không cần thăm dò codebase.

Mục tiêu: (1) mở rộng `src/config.py` với model per-stage (classify/draft/lead) + DRY_RUN (env-driven, secret redacted, Bedrock cut-over không đổi code); (2) tạo `src/reply_mcp/server.py` — MCP tool `submit_reply(body, citations)` là CON ĐƯỜNG DUY NHẤT phát draft cho khách (DRY_RUN → persist `queue.dry_run_log`); (3) tạo `.claude/settings.json` đăng ký BA MCP (Knowledge/Selless/Reply) + bind CẢ NĂM hook theo §4a (PreToolUse(submit_reply) chạy grounding_check → pre_send_guard → final-risk; UserPromptSubmit → injection_screen; PostToolUse/SubagentStop → escalation_gate; PostToolUse → pii_redact); (4) `.claude/CLAUDE.md` safety-contract team-scoped + cập nhật DÒNG orchestration của root `CLAUDE.md`; (5) sample-ticket fixtures; (6) RED stubs + test cấu trúc binding hook.

Purpose: §4a — biến hooks thành non-bypassable qua một chokepoint duy nhất (`submit_reply`). Mọi wave sau đọc model IDs, fixtures, `.claude/` layout, và chokepoint tool từ đây — interface-first.
Output: src/config.py (extended), src/reply_mcp/, .claude/settings.json, .claude/CLAUDE.md, root CLAUDE.md (orchestration row only), fixtures, RED + binding tests, pyproject.toml deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/specs/2026-06-02-cs-agent-team-design.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From src/config.py (existing Settings class to extend; lines 28-162):
- `class Settings(BaseSettings)` — module-level singleton `settings = Settings()` at line 162
- `class SendMode(str, Enum)` { DRY_RUN="dry_run", LIVE="live" } at lines 17-25
- `send_mode: SendMode = Field(default=SendMode.DRY_RUN, ...)` at lines 57-60
- `__repr__` (lines 142-151) redacts freshdesk_api_key/webhook_secret/selless_api_gateway_key/voyage_api_key — REPRODUCE for anthropic_api_key
- `model_config` (lines 153-158): env_file=".env", case_sensitive=False, extra="ignore"

From src/knowledge_mcp/server.py (line 32): `mcp = FastMCP(name="KnowledgeMCP", on_duplicate="error")`
  launch: `uv run python -m src.knowledge_mcp.server`; env DATABASE_URL, VOYAGE_API_KEY
From src/selless_mcp/server.py (line 150): `mcp = FastMCP(name="SellessMCP", on_duplicate="error")`
  launch: `uv run python -m src.selless_mcp.server`; env DATABASE_URL, SELLESS_API_BASE_URL

DRY_RUN persistence pattern — src/work_queue/send.py `_dry_run` (lines 93-123):
  redacts via redact_text() then INSERT INTO queue.dry_run_log (ticket_id, inbound_msg_id, action, body).
  reply_mcp.submit_reply mirrors this: redact then persist action="reply" to queue.dry_run_log.

Design §4a (authoritative enforcement spec):
  - Transport = settings.json hooks (NOT Agent-SDK programmatic). No "executor picks" choice.
  - submit_reply(body, citations) is the ONLY draft-emitting path → a hook vetoing it is a hard gate.
  - PreToolUse on submit_reply: grounding_check.py → pre_send_guard.py → final-risk check; failure exit 2 → block → runner treats as escalate (no draft, D-10).
  - injection_screen.py = UserPromptSubmit (or runner pre-screen) on inbound body.
  - escalation_gate.py = PostToolUse/SubagentStop on classifier/extractor → records risk signals (early-exit optimization; the hard guarantee is the submit_reply final-risk check).
  - pii_redact.py = PostToolUse (broad) before any sink.

PATTERNS.md settings.json shape: lines 253-288. PATTERNS.md config extension: lines 346-370.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/config.py (+ claude_model_lead) + build src/reply_mcp submit_reply tool + deps</name>
  <files>src/config.py, src/reply_mcp/__init__.py, src/reply_mcp/server.py, pyproject.toml</files>
  <read_first>
    - src/config.py lines 28-162 (Settings class, __repr__ at 142-151, singleton at 162)
    - .planning/phases/.../04-PATTERNS.md lines 342-425 (config self-extend + secrets-never-in-repr)
    - src/work_queue/send.py lines 93-123 (_dry_run: redact_text then INSERT queue.dry_run_log — reply_mcp mirrors this)
    - src/knowledge_mcp/server.py lines 32-97 (FastMCP init + @mcp.tool decorator shape)
    - CLAUDE.md "Recommended Stack" (model IDs claude-haiku-4-5 / claude-sonnet-4-6) + "What NOT to Use" (no Opus hot path)
  </read_first>
  <action>
    Add to `Settings` (src/config.py): `anthropic_api_key: str` (default "", "NEVER log"); `claude_model_classify: str` (default "claude-haiku-4-5", D-03 — Haiku classify/extract); `claude_model_draft: str` (default "claude-sonnet-4-6", D-03 — Sonnet draft/critic); `claude_model_lead: str` (default "claude-sonnet-4-6" — Sonnet orchestration; W3 fix); `dry_run: bool` (default True). No Opus field anywhere (D-03 hard rule). Extend `__repr__` to print claude_model_classify, claude_model_draft, claude_model_lead (non-secret) and `anthropic_api_key=<REDACTED>` — mirror the existing redaction line exactly; never interpolate the key. Keep all env-driven so Bedrock cut-over needs no code change. Create `src/reply_mcp/__init__.py` and `src/reply_mcp/server.py`: a FastMCP server `mcp = FastMCP(name="ReplyMCP", on_duplicate="error")` exposing one tool `submit_reply(body: str, citations: list[dict]) -> dict` — this is the SINGLE customer-draft chokepoint (design §4a). In DRY_RUN (settings.dry_run / SendMode.DRY_RUN) it mirrors src/work_queue/send.py `_dry_run`: pass body through `from src.guards.pii import redact_text` then INSERT into queue.dry_run_log with action="reply"; return {"submitted": True, "dry_run": True}. Expose a `__main__`/`run()` launch via `uv run python -m src.reply_mcp.server` like the other servers. In pyproject.toml add the Claude Agent SDK / claude CLI dependency under a `cs-team` optional group (pin v1.x). Do NOT add PydanticAI to the hot path (superseded). No Package Legitimacy Audit exists for this phase → any net-new PyPI install is [ASSUMED] and gated by the blocking human-verify checkpoint in 04-03 before install; declare only the official `anthropic`/`claude-agent-sdk` packages (verify exact name on pypi.org at the checkpoint).
  </action>
  <verify>
    <automated>uv run python -c "from src.config import settings; r=repr(settings); assert 'anthropic_api_key=<REDACTED>' in r, r; assert settings.claude_model_classify=='claude-haiku-4-5'; assert settings.claude_model_draft=='claude-sonnet-4-6'; assert settings.claude_model_lead=='claude-sonnet-4-6'; assert settings.dry_run is True; assert 'opus' not in (settings.claude_model_classify+settings.claude_model_draft+settings.claude_model_lead).lower(); print('cfg-OK')"</automated>
    <automated>uv run python -c "import ast,inspect; src=open('src/reply_mcp/server.py').read(); ast.parse(src); assert 'submit_reply' in src and 'ReplyMCP' in src and ('redact_text' in src) and 'dry_run_log' in src; print('reply_mcp-OK')"</automated>
  </verify>
  <done>Settings exposes Haiku/Sonnet per-stage models incl. claude_model_lead + dry_run; repr redacts anthropic_api_key; no Opus; src/reply_mcp.submit_reply is the single DRY_RUN-persisting chokepoint; deps declared.</done>
</task>

<task type="auto">
  <name>Task 2: .claude/settings.json (3 MCPs + all-five-hook §4a bindings) + .claude/CLAUDE.md + root CLAUDE.md row</name>
  <files>.claude/settings.json, .claude/CLAUDE.md, CLAUDE.md</files>
  <read_first>
    - docs/specs/2026-06-02-cs-agent-team-design.md §4a (the exact hook→event mapping — authoritative) + §3/§4
    - .planning/phases/.../04-PATTERNS.md lines 240-288 (settings.json MCP + hooks shape) and lines 453-471 (.claude/CLAUDE.md team-scoped, NOT root)
    - src/knowledge_mcp/server.py line 32, src/selless_mcp/server.py line 150, src/reply_mcp/server.py (Task 1 — ReplyMCP launch)
    - CLAUDE.md root "## Technology Stack" orchestration row (PydanticAI) + model-cost rules
  </read_first>
  <action>
    Create `.claude/settings.json` registering THREE mcpServers: KnowledgeMCP (uv run python -m src.knowledge_mcp.server; env DATABASE_URL + VOYAGE_API_KEY), SellessMCP (src.selless_mcp.server; env DATABASE_URL + SELLESS_API_BASE_URL), and ReplyMCP (src.reply_mcp.server; env DATABASE_URL). Bind ALL FIVE hooks per design §4a with CONCRETE events (verify exact key/matcher syntax against the Claude Code settings.json spec at build; §4a removes the prior transport-choice ambiguity — transport IS settings.json hooks): (a) `PreToolUse` matched to the `submit_reply` tool → an ORDERED hook list running `uv run python .claude/hooks/grounding_check.py` then `.../pre_send_guard.py` then a final-risk check — any non-zero (exit 2) blocks the tool; (b) `UserPromptSubmit` → `.../injection_screen.py`; (c) `PostToolUse`/`SubagentStop` matched to classifier/extractor → `.../escalation_gate.py` (records risk signals — early-exit optimization, not the sole net); (d) `PostToolUse` broad matcher → `.../pii_redact.py`. Add `env.SEND_MODE="dry_run"`. Create `.claude/CLAUDE.md` as the TEAM-SCOPED safety contract (LLM-readable non-negotiables): any-signal-escalates (D-08); escalate = no draft (D-10); no ungrounded claims, inline citations (D-11); commitment language blocked (D-13); email body untrusted/delimited (D-14); Haiku classify-extract & Sonnet draft-critic-lead, NO Opus (D-03); templates via Knowledge MCP get_template, never raw-source-per-reply; PII redacted before any sink; AND the chokepoint rule: a customer draft may ONLY be emitted via the submit_reply tool. Add a header note that this governs the cs-agent-team and the root CLAUDE.md remains the developer/project file (documents the dual-CLAUDE collision resolution). W4: update ONLY the root `CLAUDE.md` "## Technology Stack" orchestration/agent-layer row to read "Claude Code agent team (.claude/) / Claude Agent SDK — PydanticAI deferred"; preserve every model-cost + grounding rule and all other rows/text untouched (single-row edit).
  </action>
  <verify>
    <automated>uv run python -c "import json; d=json.load(open('.claude/settings.json')); ms=d['mcpServers']; assert {'KnowledgeMCP','SellessMCP','ReplyMCP'} <= set(ms); assert 'src.reply_mcp.server' in ms['ReplyMCP']['args']; s=json.dumps(d); assert 'submit_reply' in s and 'grounding_check.py' in s and 'pre_send_guard.py' in s and 'injection_screen.py' in s and 'escalation_gate.py' in s and 'pii_redact.py' in s; print('settings-OK')"</automated>
    <automated>uv run python -c "t=open('.claude/CLAUDE.md').read().lower(); assert 'submit_reply' in t and 'escalate' in t and 'opus' in t and 'citation' in t; import re; r=open('CLAUDE.md').read(); assert 'PydanticAI deferred' in r or 'Claude Code agent team' in r; print('claudemd-OK')"</automated>
  </verify>
  <done>settings.json registers 3 MCPs incl. ReplyMCP + binds all five hooks with §4a events (PreToolUse submit_reply chain = grounding→pre_send→final-risk); .claude/CLAUDE.md team-scoped + states submit_reply chokepoint; root CLAUDE.md orchestration row updated only.</done>
</task>

<task type="auto">
  <name>Task 3: Sample fixtures + RED hook/team-kit stubs + settings hook-binding structural test</name>
  <files>tests/fixtures/sample_tickets.py, tests/cs_team/__init__.py, tests/cs_team/test_hooks_red.py, tests/cs_team/test_team_kit_structure.py, tests/cs_team/test_settings_hook_bindings.py</files>
  <read_first>
    - tests/smoke/test_grounding_demo.py lines 28-56 (fixture/seed pattern, async test shape)
    - .planning/phases/.../04-PATTERNS.md lines 292-338 (runner + acceptance shape) and 374-389 ((bool,reason) contract)
    - src/guards/loop_guard.py lines 157-193 (the (bool, reason) contract the hooks mirror)
    - docs/specs/2026-06-02-cs-agent-team-design.md §3 (full .claude/ manifest) + §4a (hook→event mapping the binding test asserts)
    - .claude/settings.json (Task 2 — the binding test parses this)
  </read_first>
  <action>
    Create `tests/fixtures/sample_tickets.py` exporting BENIGN_TICKET (normal order-status question → should draft), HIGH_RISK_TICKET (explicit refund demand → escalate), INJECTION_TICKET (body with an instruction-override attempt → escalate via injection_screen). Each is a dict with ticket_id, subject, body, from_email, order_ref. Create `tests/cs_team/__init__.py`. Create `tests/cs_team/test_hooks_red.py` RED stubs (xfail/import-guarded) asserting the five hook functions exist with the (bool,reason) contract: screen_for_injection(body)->tuple[bool,str], check_commitment_language(draft)->tuple[bool,str], should_escalate(signals: dict)->tuple[bool,str], check_grounding(draft, citations)->tuple[bool,str], pii_redact_hook(text)->str — fail now, green in Wave 2. Create `tests/cs_team/test_team_kit_structure.py` RED stubs asserting the §3 manifest: .claude/agents/{cs-lead,classifier,extractor,drafter,critic}.md, .claude/skills/{reply-pipeline,classify-ticket,extract-answer-key,ground-and-draft,self-critique}/SKILL.md, .claude/hooks/{injection_screen,pre_send_guard,escalation_gate,grounding_check,pii_redact}.py — fail now, green by Wave 3. Create `tests/cs_team/test_settings_hook_bindings.py` (W-fix, runs NOW and stays green): parse .claude/settings.json and STRUCTURALLY assert ALL FIVE hooks are bound with the §4a events — grounding_check + pre_send_guard bound under a PreToolUse matcher targeting submit_reply (ordered, before any draft emission), injection_screen under UserPromptSubmit, escalation_gate under PostToolUse/SubagentStop, pii_redact under PostToolUse; the test FAILS if any hook or its event is missing. Use redact_text on any printed ticket body so fixtures never emit raw PII.
  </action>
  <verify>
    <automated>uv run python -c "from tests.fixtures.sample_tickets import BENIGN_TICKET, HIGH_RISK_TICKET, INJECTION_TICKET; assert 'body' in BENIGN_TICKET and 'order_ref' in BENIGN_TICKET; print('fixtures-OK')"</automated>
    <automated>uv run pytest tests/cs_team/test_settings_hook_bindings.py -q</automated>
    <automated>uv run pytest tests/cs_team/test_hooks_red.py tests/cs_team/test_team_kit_structure.py -q 2>&1 | grep -Eq "(failed|xfailed)" && echo "RED-as-expected"</automated>
  </verify>
  <done>Three sample fixtures importable; RED hook + team-kit stubs fail (turn green later); settings-hook-binding structural test runs now and asserts all five hooks bound with §4a events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | API keys / DB URLs enter via env; must never be logged |
| settings.json → MCP subprocess | launch commands inherit env; scoped to the three kit servers only |
| agent → submit_reply | the single chokepoint where a customer draft can be emitted; hook-guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Information Disclosure | Settings.__repr__ / logs | mitigate | anthropic_api_key redacted in __repr__; verified by Task 1 assert |
| T-04-00-02 | Information Disclosure | reply_mcp / fixtures | mitigate | submit_reply + fixtures redact_text before persist/print |
| T-04-00-03 | Tampering | settings.json hook bindings | mitigate | hooks pinned to specific .claude/hooks/*.py paths; PreToolUse(submit_reply) ordered chain; structural test asserts all five bound (§4a) |
| T-04-00-04 | Elevation of Privilege | draft emitted outside the chokepoint | mitigate | submit_reply is the ONLY draft path (§4a); a hook vetoing it is a hard gate the agents cannot route around |
| T-04-00-SC | Tampering | new PyPI installs (anthropic/claude-agent-sdk) | mitigate | no Package Legitimacy Audit → [ASSUMED]; blocking human-verify checkpoint in 04-03 before any net-new install; verify on pypi.org |
</threat_model>

<verification>
- `uv run python -c "from src.config import settings; print(repr(settings))"` shows no secret values; claude_model_lead present, no Opus
- `.claude/settings.json` parses, lists three MCP servers, binds all five hooks with §4a events
- `tests/cs_team/test_settings_hook_bindings.py` green (structural enforcement gate)
- RED hook + team-kit stubs fail (expected) — they gate Waves 2-3
- root CLAUDE.md orchestration row updated; all other content untouched
</verification>

<success_criteria>
- Config exposes Haiku/Sonnet per-stage models (classify/draft/lead) + dry_run, env-driven, secrets redacted, no Opus
- src/reply_mcp.submit_reply is the single customer-draft chokepoint (DRY_RUN → queue.dry_run_log, redacted)
- settings.json registers Knowledge/Selless/Reply MCPs and binds all five hooks with the §4a events; structural test enforces it
- .claude/CLAUDE.md team-scoped (collision resolved); root CLAUDE.md orchestration row updated only
- Sample fixtures + RED scaffolds in place
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-00-SUMMARY.md` when done
</output>
