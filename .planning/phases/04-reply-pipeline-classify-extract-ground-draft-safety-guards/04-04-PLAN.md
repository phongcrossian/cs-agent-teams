---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-05"]
files_modified:
  - tests/cs_team/conftest.py
  - tests/cs_team/test_authorized_offer.py
  - tests/cs_team/test_authorized_offer_red.py
  - tests/cs_team/test_pre_send_guard_authorized.py
  - tests/cs_team/test_escalation_gate_operational.py
  - tests/cs_team/test_classifier_subtype_contract.py
  - tests/cs_team/test_drafter_offer_contract.py
  - tests/cs_team/test_hooks.py
  - tests/cs_team/test_hooks_red.py
  - tests/cs_team/test_hooks_subprocess.py
  - tests/cs_team/test_e2e_dry_run.py
  - tests/cs_team/test_settings_hook_bindings.py
  - tests/cs_team/test_team_definitions.py
autonomous: true
requirements: [REP-01, REP-04, SAFE-03, SAFE-04]
must_haves:
  truths:
    - "pytest tests/cs_team -q is GREEN — no collection errors (no imports of the deleted hook modules) and no failures"
    - "No test in tests/cs_team asserts the retired contract (escalate=no-draft, mandatory [KB-N] citations, lookup_code, the D-26 authorized-offer gate, or the deleted guard hooks)"
    - "conftest.py registers only the two surviving hooks (injection_screen, pii_redact); it no longer maps pre_send_guard/escalation_gate/grounding_check"
    - "The surviving safety floor (injection_screen, pii_redact) and the always-draft structural contract still have passing tests"
  artifacts:
    - path: "tests/cs_team/conftest.py"
      provides: "Hook-module registration for ONLY injection_screen + pii_redact (deleted-hook entries removed)"
      contains: "injection_screen"
    - path: "tests/cs_team/test_settings_hook_bindings.py"
      provides: "Asserts ONLY injection_screen (UserPromptSubmit) + pii_redact (PostToolUse) are wired; no submit_reply PreToolUse chain; KnowledgeMCP removed"
      contains: "injection_screen"
    - path: "tests/cs_team/test_e2e_dry_run.py"
      provides: "Always-draft e2e: benign/high-risk/injection all yield action=draft (with advisory hint where applicable), never escalate=no-draft"
      contains: "draft"
  key_links:
    - from: "tests/cs_team/conftest.py"
      to: ".claude/hooks/injection_screen.py"
      via: "module registration for the surviving hooks only"
      pattern: "injection_screen"
    - from: "tests/cs_team/test_settings_hook_bindings.py"
      to: ".claude/settings.json"
      via: "asserts the slimmed two-hook wiring (04-01 result)"
      pattern: "pii_redact|injection_screen"
    - from: "tests/cs_team/test_team_definitions.py"
      to: ".claude/skills/self-critique/SKILL.md"
      via: "asserts the three REP-04 rubric dimensions survive the 04-05 rewrite"
      pattern: "faithfulness"
---

<objective>
Clean the `tests/cs_team/` suite to the always-draft contract so `pytest tests/cs_team -q` is GREEN after the pivot rework (04-00..04-03, 04-05). Today ~13 test files import the four deleted hook modules and/or assert the RETIRED contract (escalate=no-draft, mandatory `[KB-N]` citations, `lookup_code`, the D-26 authorized-offer gate, the `submit_reply` PreToolUse chain), so the suite errors on collection / goes red.

Purpose: The pivot deletes 4 guard hooks (04-01), flips the verdict to always-draft (04-02), removes KnowledgeMCP/semantic_search (04-02/04-05), and removes the D-26 gate. The test suite still encodes the old fail-closed contract — it must be cleaned in the same phase or CI is broken. This plan DELETEs the no-longer-applicable tests, REWRITEs the structural/e2e tests to the always-draft contract, and slims `conftest.py` to the two surviving hooks. It preserves coverage of the surviving safety floor (injection_screen, pii_redact) and the REP-01/REP-04 structural contract.

NOTE — `04-03` separately creates `tests/test_cs_team_demo_always_draft.py` and edits `tests/fixtures/sample_tickets.py` (different paths, no overlap with this plan's `tests/cs_team/` scope), so 04-04 and 04-03 run in the same wave without file conflict.

Output: a slimmed `conftest.py`, six deleted retired-contract test files, and three rewritten test files (e2e, settings-bindings, team-definitions), with `pytest tests/cs_team -q` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-VALIDATION.md
@.claude/CLAUDE.md

<interfaces>
<!-- Surviving hooks after 04-01: .claude/hooks/injection_screen.py (screen_for_injection / main: exit 1 on injection), .claude/hooks/pii_redact.py (never blocks, exit 0). -->
<!-- DELETED in 04-01: pre_send_guard.py, escalation_gate.py, grounding_check.py, authorized_offer.py. Any test importing/subprocessing these must be deleted or have those cases removed. -->
<!-- settings.json after 04-01/04-02: hooks = ONLY injection_screen (UserPromptSubmit) + pii_redact (PostToolUse); NO PreToolUse[submit_reply] chain, NO SubagentStop, NO CS_RUN_ID env. mcpServers = SellessMCP + ReplyMCP (KnowledgeMCP removed). -->
<!-- Always-draft verdict (D-33): {"action":"draft", "body":..., "citations":[...], "escalation_hint": {...}|null}. There is NO action=="escalate" with no body. -->
<!-- conftest.py _HOOK_MODULES currently maps 5 entries; must drop pre_send_guard/escalation_gate/grounding_check, keep injection_screen + pii_redact. It guards with file_path.exists() so missing files are skipped, but the dead entries must be removed for clarity + to stop registering retired modules. -->
<!-- test_team_definitions.py currently asserts drafter references get_template (KnowledgeMCP) and reply-pipeline encodes the "escalate verdict" — both contradict the rework; update to file-store grounding + always-draft. The three self-critique dimension asserts (faithfulness/policy-match/tone-completeness) STAY (REP-04, preserved by 04-05). -->
<!-- DELETE candidates (assert retired contract end-to-end, nothing to salvage): test_authorized_offer.py, test_authorized_offer_red.py, test_pre_send_guard_authorized.py, test_escalation_gate_operational.py, test_classifier_subtype_contract.py (asserts the hard-escalate sub-type contract), test_drafter_offer_contract.py (asserts the D-26 offer block). -->
<!-- test_team_kit_structure.py references the deleted hooks only as on-disk filename checks — update its expected hook set to the two survivors (do NOT assert the 4 deleted files exist). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the retired-contract test files + slim conftest to the two surviving hooks</name>
  <files>tests/cs_team/conftest.py, tests/cs_team/test_authorized_offer.py, tests/cs_team/test_authorized_offer_red.py, tests/cs_team/test_pre_send_guard_authorized.py, tests/cs_team/test_escalation_gate_operational.py, tests/cs_team/test_classifier_subtype_contract.py, tests/cs_team/test_drafter_offer_contract.py</files>
  <read_first>
    - tests/cs_team/conftest.py (the _HOOK_MODULES map registering all 5 hooks — drop the 3 deleted-guard entries)
    - tests/cs_team/test_authorized_offer.py (asserts the deleted authorized_offer.py decision module — DELETE)
    - tests/cs_team/test_pre_send_guard_authorized.py (asserts the deleted pre_send_guard D-26 block — DELETE)
    - tests/cs_team/test_escalation_gate_operational.py (asserts the deleted escalation_gate operational trigger — DELETE)
    - tests/cs_team/test_classifier_subtype_contract.py (asserts the hard-escalate sub-type contract — DELETE; REP-01 enum is re-covered by the always-draft team_definitions test in Task 2)
    - tests/cs_team/test_drafter_offer_contract.py (asserts the D-26 offer block on the drafter — DELETE)
    - .claude/hooks/ (confirm only injection_screen.py + pii_redact.py remain after 04-01)
  </read_first>
  <action>
    `git rm` the six retired-contract test files: `test_authorized_offer.py`, `test_authorized_offer_red.py`, `test_pre_send_guard_authorized.py`, `test_escalation_gate_operational.py`, `test_classifier_subtype_contract.py`, `test_drafter_offer_contract.py`. These assert the deleted hooks (authorized_offer/pre_send_guard/escalation_gate) and the retired D-26 offer/hard-escalate-sub-type contract end-to-end; there is nothing to salvage (REP-01 sub-type enum coverage is re-established structurally in Task 2). Then edit `tests/cs_team/conftest.py`: in the `_HOOK_MODULES` dict, REMOVE the `.claude.hooks.pre_send_guard`, `.claude.hooks.escalation_gate`, and `.claude.hooks.grounding_check` entries; KEEP only `.claude.hooks.injection_screen` and `.claude.hooks.pii_redact`. Update the module docstring to reflect the two surviving hooks. Do not change the registration mechanism.
  </action>
  <verify>
    <automated>test ! -f tests/cs_team/test_authorized_offer.py && test ! -f tests/cs_team/test_pre_send_guard_authorized.py && test ! -f tests/cs_team/test_escalation_gate_operational.py && test ! -f tests/cs_team/test_drafter_offer_contract.py && test ! -f tests/cs_team/test_classifier_subtype_contract.py && test ! -f tests/cs_team/test_authorized_offer_red.py && grep -c "pre_send_guard\|escalation_gate\|grounding_check" tests/cs_team/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - The six files are absent (the `test ! -f` chain in verify passes).
    - `grep -c "pre_send_guard\|escalation_gate\|grounding_check" tests/cs_team/conftest.py` returns 0.
    - `grep -c "injection_screen\|pii_redact" tests/cs_team/conftest.py` returns ≥ 2 (surviving hooks still registered).
    - `uv run python -c "import importlib.util,sys; sys.argv=['x']; spec=importlib.util.spec_from_file_location('cf','tests/cs_team/conftest.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)"` exits 0 (conftest imports cleanly with the deleted hooks gone).
  </acceptance_criteria>
  <done>The six retired-contract test files are deleted; conftest registers only injection_screen + pii_redact; conftest imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Strip deleted-hook cases from test_hooks*.py + test_team_kit_structure; rewrite e2e, settings-bindings, team-definitions to always-draft</name>
  <files>tests/cs_team/test_hooks.py, tests/cs_team/test_hooks_red.py, tests/cs_team/test_hooks_subprocess.py, tests/cs_team/test_e2e_dry_run.py, tests/cs_team/test_settings_hook_bindings.py, tests/cs_team/test_team_definitions.py, tests/cs_team/test_team_kit_structure.py</files>
  <read_first>
    - tests/cs_team/test_hooks.py (D-08/D-13/D-14 in-process cases — DELETE the pre_send_guard/escalation_gate/grounding_check cases, KEEP injection_screen + pii_redact cases)
    - tests/cs_team/test_hooks_red.py (relative-import RED cases for the deleted hooks — KEEP only injection_screen/pii_redact cases; remove the rest)
    - tests/cs_team/test_hooks_subprocess.py (subprocess exit-code proofs: pre_send_guard/grounding_check/escalation_gate sections + CR-01 escalation_gate outer-except + path-traversal CS_RUN_ID cases — DELETE; KEEP the injection_screen subprocess section + pii_redact if present)
    - tests/cs_team/test_e2e_dry_run.py (mock-chain e2e: high_risk→escalate / injection→escalate / uncited→grounding_check→escalate — REWRITE to always-draft + advisory hint)
    - tests/cs_team/test_settings_hook_bindings.py (asserts the 3-hook submit_reply PreToolUse chain + SubagentStop escalation_gate + 5 hook scripts — REWRITE to the two-hook wiring)
    - tests/cs_team/test_team_definitions.py (asserts drafter get_template + reply-pipeline "escalate verdict" — REWRITE to file-store + always-draft; KEEP the 3 self-critique dimension asserts)
    - tests/cs_team/test_team_kit_structure.py (on-disk hook filename checks — update expected hook set to the two survivors)
    - .claude/settings.json (the slimmed wiring from 04-01/04-02 — the assertion target)
    - .claude/skills/reply-pipeline/SKILL.md (the always-draft workflow from 04-02 — the assertion target)
    - .claude/agents/drafter.md (file-store grounding from 04-02 — the assertion target)
  </read_first>
  <action>
    (A) `test_hooks.py`, `test_hooks_red.py`, `test_hooks_subprocess.py`: remove every test case (and any helper/import) that targets the DELETED hooks — `pre_send_guard`, `escalation_gate` (incl. the stateful CS_RUN_ID veto, the CR-01 outer-except case, and the path-traversal cases), and `grounding_check`. KEEP and preserve the `injection_screen` cases (instruction-override → non-zero / escalate) and any `pii_redact` cases (never blocks → exit 0). If removing cases empties a file of all tests, `git rm` that file instead of leaving an empty module. Update each file's module docstring to list only the surviving hooks.
    (B) `test_e2e_dry_run.py`: REWRITE the mock-chain so the expected outcomes are always-draft: benign → `action=="draft"`, `escalation_hint is None`; high-risk (money/legal) → `action=="draft"` WITH a non-null `escalation_hint` (advisory) — NOT escalate-with-no-draft; injection → `action=="draft"` with an `escalation_hint` whose reason contains "injection" (injection_screen still flags via the surviving pre-screen, but the pipeline drafts). Remove the `should_escalate` / `pre_send_guard._has_commitment_term` / `grounding_check` mock wiring and the "first block wins → escalate" chain; keep `screen_for_injection` for the advisory pre-screen. Assert no raw PII in output and DRY_RUN throughout. No fixture yields `action=="escalate"` with no body.
    (C) `test_settings_hook_bindings.py`: REWRITE to the slimmed wiring — assert UserPromptSubmit binds `injection_screen.py`; PostToolUse binds `pii_redact.py`; there is NO PreToolUse binding with matcher `submit_reply`; NO SubagentStop binding; the only two hook scripts referenced are `injection_screen.py` + `pii_redact.py`; `mcpServers` contains `SellessMCP` + `ReplyMCP` and NOT `KnowledgeMCP`. Delete the three-hook-chain / order / "escalation_gate in two contexts" / "all five hook scripts" assertions.
    (D) `test_team_definitions.py`: KEEP the agent-exists / skill-exists / model-discipline (Haiku classify+extract, Sonnet draft+critic+lead, no Opus) / extractor-references-resolve_order / drafter-references-submit_reply / cs-lead-references-reply-pipeline / extract-answer-key-references-resolve_order asserts AND the three self-critique dimension asserts (faithfulness/policy-match/tone-completeness — REP-04). REPLACE `test_drafter_references_get_template` with an assert that drafter.md references the **file-store** (`get_template_from_file` / `subtype_to_code` / `file_store`) and NOT `get_template`/`KnowledgeMCP`. REPLACE `test_reply_pipeline_mentions_escalate_verdict` with an assert that reply-pipeline/SKILL.md encodes the **always-draft** verdict + an `escalation_hint` (and does NOT encode escalate=no-draft). Update `test_ground_and_draft_skill_references_get_template` to assert file-store grounding instead.
    (E) `test_team_kit_structure.py`: update the on-disk hook expectation to the two survivors (`injection_screen.py`, `pii_redact.py`); do NOT assert the four deleted files exist.
    After all edits, run the full cs_team subset and confirm GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/cs_team -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/cs_team -q` exits 0 — no collection errors, no failures (GREEN). This is the gating criterion for BLOCKER-1.
    - `grep -rln "pre_send_guard\|escalation_gate\|grounding_check\|authorized_offer" tests/cs_team/` returns nothing (no surviving reference to the deleted hooks).
    - `grep -rln "semantic_search\|KnowledgeMCP\|lookup_code\|\[KB-" tests/cs_team/` returns nothing (retired RAG/citation contract gone).
    - `grep -in "action.*draft\|escalation_hint" tests/cs_team/test_e2e_dry_run.py` returns ≥ 1 line; `grep -in "high.risk.*escalate\|injection.*escalate\|action.*escalate" tests/cs_team/test_e2e_dry_run.py` returns nothing (no escalate=no-draft expectation).
    - `grep -in "submit_reply" tests/cs_team/test_settings_hook_bindings.py | grep -i "PreToolUse\|matcher"` returns nothing (no submit_reply PreToolUse chain asserted); `grep -in "injection_screen\|pii_redact" tests/cs_team/test_settings_hook_bindings.py` returns ≥ 2 lines.
    - `grep -in "file_store\|get_template_from_file\|subtype_to_code" tests/cs_team/test_team_definitions.py` returns ≥ 1 line; `grep -in "faithfulness\|policy-match\|tone-completeness" tests/cs_team/test_team_definitions.py` returns ≥ 3 lines (REP-04 preserved).
  </acceptance_criteria>
  <done>All deleted-hook test cases are removed (or their files git-rm'd when emptied); injection_screen + pii_redact cases preserved; e2e/settings/team-definitions/kit-structure rewritten to the always-draft + two-hook + file-store contract; `pytest tests/cs_team -q` is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → CI gate | A red/erroring suite would let the pivot rework merge with a contradictory contract; this plan restores a meaningful gate |
| surviving hooks → tests | injection_screen + pii_redact remain the enforced safety floor; their tests must stay green so regressions are caught |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | deleting too much removes the injection_screen / pii_redact coverage, hiding a future safety-floor regression | mitigate | Task 2 explicitly KEEPS the injection_screen + pii_redact cases; acceptance asserts the e2e injection path still produces an advisory hint and settings still wires both hooks |
| T-04-04-02 | Repudiation | a test silently asserting the retired contract survives and masks a contradiction | mitigate | Grep gates assert ZERO references to the four deleted hooks and to semantic_search/KnowledgeMCP/[KB- across tests/cs_team/ |
| T-04-04-03 | Tampering | model-discipline regression (Opus on hot path) loses its test when team_definitions is rewritten | mitigate | Task 2 explicitly KEEPS the Haiku/Sonnet/no-Opus model-discipline asserts (D-03) and the REP-04 dimension asserts |
| T-04-04-SC | Tampering | n/a — no package installs in this plan | accept | Test-file edits/deletions only; no new dependencies |
</threat_model>

<verification>
- `pytest tests/cs_team -q` is GREEN (no collection errors, no failures).
- Zero references to the four deleted hooks or to semantic_search/KnowledgeMCP/[KB- in tests/cs_team/.
- injection_screen + pii_redact coverage preserved; e2e asserts always-draft + advisory hint; settings asserts the two-hook wiring + KnowledgeMCP removed.
- REP-04 rubric dimensions + D-03 model discipline still asserted in team_definitions.
</verification>

<success_criteria>
The cs_team test suite reflects the always-draft / two-hook / file-store contract and is GREEN, restoring CI as a meaningful gate for REP-01/REP-04/SAFE-03/SAFE-04 after the pivot rework.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-04-SUMMARY.md` when done.
</output>
