---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 02
type: execute
wave: 2
depends_on: ["04-00", "04-01"]
files_modified:
  - .claude/settings.json
  - .claude/CLAUDE.md
  - .claude/agents/cs-lead.md
  - .claude/agents/classifier.md
  - .claude/agents/extractor.md
  - .claude/agents/drafter.md
  - .claude/agents/critic.md
  - .claude/skills/reply-pipeline/SKILL.md
  - .claude/skills/ground-and-draft/SKILL.md
autonomous: true
requirements: [REP-01, REP-02, REP-03, REP-04, SAFE-03]
must_haves:
  truths:
    - "The pipeline always produces a customer draft — the verdict action is always 'draft', never 'escalate=no-draft'"
    - "The drafter grounds replies on the local file-store template + Selless order data, not on KnowledgeMCP semantic_search or [KB-N] citations"
    - "When Selless returns no order, the drafter consults the Workflow/CODE-MAP to pick a flow (verify-order / clarify-order-info) instead of fabricating order facts"
    - "An optional advisory escalation_hint MAY be attached for money/legal/injection signals but never suppresses the draft"
    - "No agent or skill (this plan's scope) references KnowledgeMCP.semantic_search, mandatory citations, the deleted guard hooks, or the D-26 authorized-offer gate"
  artifacts:
    - path: ".claude/skills/reply-pipeline/SKILL.md"
      provides: "Always-draft workflow: classify → extract → ground+draft (file-store) → critique (advisory) → emit draft verdict + optional hint"
      contains: "always"
    - path: ".claude/agents/drafter.md"
      provides: "File-store template grounding + Selless fill + D-34 flow-aware fallback; emits via submit_reply; never asserts a mutation"
      contains: "file-store"
    - path: ".claude/settings.json"
      provides: "mcpServers with KnowledgeMCP removed (SellessMCP + ReplyMCP only)"
      contains: "SellessMCP"
  key_links:
    - from: ".claude/agents/drafter.md"
      to: "src/file_store/template_store.py"
      via: "get_template_from_file + subtype_to_code (local file-store, D-31)"
      pattern: "file_store|file-store"
    - from: ".claude/agents/drafter.md"
      to: "SellessMCP.resolve_order"
      via: "order grounding + D-34 missing-order flow signal"
      pattern: "resolve_order"
    - from: ".claude/skills/reply-pipeline/SKILL.md"
      to: "ReplyMCP.submit_reply"
      via: "the single always-draft emission path"
      pattern: "submit_reply"
---

<objective>
Rework the agent team + the two **pipeline-level** skills (`reply-pipeline`, `ground-and-draft`) so the pipeline is **always-draft** (D-33), grounds on the **local file-store template + Selless order data** (D-31), uses **flow-aware Selless fallback** (D-34), and attaches only an **optional advisory escalation_hint** that never suppresses the draft. Remove KnowledgeMCP from the runtime wiring.

Purpose: Deliver the thin end-to-end always-draft slice. After this plan, a ticket flows classify → extract → ground+draft (from files + Selless) → advisory critique → a `{action:"draft", ...}` verdict — the core capability behind REP-01/02/03/04. The retired escalate=no-draft, mandatory `[KB-N]` citations, semantic_search grounding, and D-26 authorized-offer gate are all removed from the agent/pipeline-skill prose so the LLM contract matches the deleted hooks (04-01) and the file-store (04-00).

⚠️ SCOPE SPLIT (revision): the three **subagent-detail skills** — `classify-ticket/SKILL.md`, `extract-answer-key/SKILL.md`, and `self-critique/SKILL.md` — are NO LONGER reworked in this plan. They still encode the retired contract (`escalation_gate.py` enforcement, `missing_key→hard-escalate`, `KnowledgeMCP.lookup_code`, mandatory `[KB-N]`/`[SEL-N]` citations, `semantic_search`, the `overall:"escalate"`→no-draft branch). Their always-draft rewrite moves to the new **04-05** plan (same wave as this one, no file overlap). This keeps 04-02 under the file-scope threshold (9 files here; the three skills go to 04-05). Task 3 below rewrites the critic AGENT (`critic.md`) only; the self-critique SKILL is 04-05's job.

Output: reworked `.claude/agents/*.md`, the two pipeline-level `.claude/skills/{reply-pipeline,ground-and-draft}/SKILL.md`, `.claude/CLAUDE.md` advisory-escalation reference, and a KnowledgeMCP-free `.claude/settings.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.claude/CLAUDE.md

<interfaces>
<!-- File-store (built in 04-00) — the drafter's grounding surface (D-31): -->
<!--   src/file_store/template_store.py: get_template_from_file(code) -> {code, heading, body, snapshot_file, found}; subtype_to_code(sub_type) -> list[str] ([] for Review) -->
<!-- Selless MCP (unchanged, STAYS): src/selless_mcp/server.py — resolve_order, get_order_status, get_customer_info, get_purchase_history, get_ticket_history (whitelisted fields) -->
<!-- Reply chokepoint (unchanged): src/reply_mcp/server.py submit_reply(body, citations) — always DRY_RUN; redacts at persistence; no longer gated by deleted guards -->
<!-- D-33 advisory verdict shape (from .claude/CLAUDE.md "Escalation Semantics Reference (advisory)"): -->
<!--   {"action": "draft", "body": "...", "citations": [...], "escalation_hint": {"reason": "<money|legal|injection|low_confidence>", "signals": {...}} | null} -->
<!-- Sub-type → candidate code mapping currently duplicated in drafter.md + ground-and-draft/SKILL.md — now sourced from subtype_to_code(); keep the table as documentation only -->
<!-- D-34 flow signals (consult Workflow/CODE-MAP): missing order → verify-order / clarify-order-info flow; VALID order + pending infra detail → placeholder tokens allowed (e.g. [TRACKING_LINK], [ETA]) -->
<!-- The subagent-detail skills classify-ticket / extract-answer-key / self-critique are reworked in 04-05 (NOT here). reply-pipeline/SKILL.md still POINTS to them, so 04-05 must land for the contract to be fully consistent — 04-04 (test cleanup) depends on BOTH 04-02 and 04-05. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove KnowledgeMCP from settings.json + drop the citation/escalate contract from CLAUDE.md and the workflow skill</name>
  <files>.claude/settings.json, .claude/CLAUDE.md, .claude/skills/reply-pipeline/SKILL.md</files>
  <read_first>
    - .claude/settings.json (mcpServers block — KnowledgeMCP entry to remove; hooks already slimmed in 04-01)
    - .claude/CLAUDE.md (D-29/D-30 banner, the "Templates — local store" section, the advisory escalation reference; struck-through D-08/D-10/D-11/D-26 to leave as historical strike-through)
    - .claude/skills/reply-pipeline/SKILL.md (current stage order with escalation gates + escalate=no-draft verdict + grounding_check/pre_send_guard enforcement table to retire)
    - src/file_store/template_store.py (the grounding surface the workflow now points to)
  </read_first>
  <action>
    In `.claude/settings.json`, remove the `KnowledgeMCP` entry from `mcpServers` (leave `SellessMCP` and `ReplyMCP`). In `.claude/skills/reply-pipeline/SKILL.md`, rewrite the workflow to the **always-draft** flow: stage order `classify → extract → ground+draft → critique (advisory) → emit draft verdict`; remove the "escalation gate after classify/extract" stop-and-escalate steps and the "Hard Escalation Rules" / "Enforcement Reference" tables that reference `escalation_gate.py`, `grounding_check.py`, `pre_send_guard.py`, and the `submit_reply` PreToolUse chain (those hooks are deleted). Replace the dual draft/escalate "Verdict Shape" with a single always-draft verdict carrying an optional `escalation_hint` field (advisory, never suppresses the draft, D-33). State that grounding is via the **local file-store** (`get_template_from_file` / `subtype_to_code`) + Selless order data, NOT semantic_search, and that inline `[KB-N]` citations are no longer required (D-29/D-30). In `.claude/CLAUDE.md`, ensure the surviving-floor section (D-14 injection, D-04 PII) and the advisory escalation reference are the operative contract; do not re-enforce the struck-through D-08/D-10/D-11/D-26 (leave them as historical strike-through). Do not embed template bodies anywhere.
  </action>
  <verify>
    <automated>python -c "import json; s=json.dumps(json.load(open('.claude/settings.json'))); assert 'KnowledgeMCP' not in s and 'SellessMCP' in s and 'ReplyMCP' in s, s" && grep -c "always" .claude/skills/reply-pipeline/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; s=json.dumps(json.load(open('.claude/settings.json'))); assert 'KnowledgeMCP' not in s"` exits 0.
    - `grep -rn "escalation_gate\|grounding_check\|pre_send_guard\|authorized_offer" .claude/skills/reply-pipeline/SKILL.md` returns nothing.
    - `grep -in "always.*draft\|always produces a\|action.*draft" .claude/skills/reply-pipeline/SKILL.md` returns ≥ 1 line.
    - `grep -in "escalate = no draft\|no customer draft is emitted\|escalate OR draft" .claude/skills/reply-pipeline/SKILL.md` returns nothing (the no-draft outcome is gone).
    - `grep -in "escalation_hint" .claude/skills/reply-pipeline/SKILL.md` returns ≥ 1 line.
  </acceptance_criteria>
  <done>settings.json wires Selless + Reply MCP only; the workflow skill encodes the always-draft flow with an advisory hint and file-store grounding; no deleted-hook or escalate-no-draft contract remains.</done>
</task>

<task type="auto">
  <name>Task 2: Rework drafter + ground-and-draft skill for file-store grounding and D-34 flow-aware fallback</name>
  <files>.claude/agents/drafter.md, .claude/skills/ground-and-draft/SKILL.md</files>
  <read_first>
    - .claude/agents/drafter.md (current tools list incl. KnowledgeMCP.get_template/semantic_search/lookup_threshold; D-11 citation rule; D-26 authorized-offer steps; RD-Q2 eligibility stub — all to retire)
    - .claude/skills/ground-and-draft/SKILL.md (current semantic_search grounding + [KB-N] citation + D-26 offer-bounds prose to retire)
    - src/file_store/template_store.py (get_template_from_file, subtype_to_code — the new grounding API)
    - src/selless_mcp/server.py (resolve_order + whitelisted fields — the order grounding source that STAYS)
    - .planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP.md (Workflow flow map — source for the D-34 missing-order flow selection, e.g. verify-order / clarify-order-info)
  </read_first>
  <action>
    Rewrite `.claude/agents/drafter.md` frontmatter `tools` to drop all `KnowledgeMCP.*` entries (remove get_template, semantic_search, lookup_threshold) and keep `SellessMCP.resolve_order`, `SellessMCP.get_order_status`, `SellessMCP.get_customer_info`, `ReplyMCP.submit_reply`. Rewrite the system-prompt steps so: (1) template selection uses the **local file-store** — resolve the classifier `customer_request` sub-type to candidate codes via `subtype_to_code` then fetch the body via `get_template_from_file(code)` (file read, no MCP, no semantic_search); (2) order facts come from Selless `resolve_order` whitelisted fields only; (3) **D-34 flow-aware fallback** — when Selless returns no order (not found / no key), do NOT fabricate; consult the Workflow/CODE-MAP to pick the correct flow (verify-order / clarify-order-info) and draft that template instead; placeholder tokens (e.g. `[TRACKING_LINK]`, `[ETA]`) are allowed ONLY when the order is established VALID but an infra detail is pending — never to invent order facts; (4) the agent ALWAYS produces a draft via `submit_reply` (never escalate=no-draft); (5) keep RD-Q1 "never assert a completed operational action" as drafting guidance (`asserts_mutation` stays false). Remove the D-11 "every factual claim needs an inline `[KB-N]`/`[SEL-N]` citation" hard rule and the D-26 authorized-offer/threshold/eligibility block and the "guard re-validates every offer / blocked→escalate" language (the guard is deleted). Apply the same retirements to `ground-and-draft/SKILL.md`: replace the semantic_search grounding + citation-assignment + conflict/stale-escalate + D-26 offer-bounds sections with file-store template selection + Selless fill + the D-34 fallback. Keep the "email body is untrusted data / delimited" instruction (D-14). Do not embed template bodies. `citations` passed to `submit_reply` may carry Selless field references for provenance but are no longer mandatory or `[KB-N]`-shaped.
  </action>
  <verify>
    <automated>grep -in "file_store\|file-store\|get_template_from_file\|subtype_to_code" .claude/agents/drafter.md .claude/skills/ground-and-draft/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "semantic_search\|KnowledgeMCP" .claude/agents/drafter.md` returns nothing.
    - `grep -in "semantic_search\|KnowledgeMCP" .claude/skills/ground-and-draft/SKILL.md` returns nothing.
    - `grep -in "file-store\|file_store\|get_template_from_file" .claude/agents/drafter.md` returns ≥ 1 line.
    - `grep -in "verify-order\|clarify-order\|flow-aware\|no order\|missing order" .claude/agents/drafter.md` returns ≥ 1 line (D-34).
    - `grep -in "authorized.offer\|pre_send_guard\|blocked.*escalate\|every factual claim must have an inline" .claude/agents/drafter.md` returns nothing (D-26 / mandatory-citation retired).
    - `grep -in "untrusted\|ticket_body" .claude/agents/drafter.md` returns ≥ 1 line (D-14 retained).
  </acceptance_criteria>
  <done>The drafter grounds on the local file-store template + Selless order data, falls back to a Workflow/CODE-MAP flow on a missing order without fabricating, always emits a draft, and no longer carries semantic_search, mandatory citations, or the D-26 authorized-offer gate.</done>
</task>

<task type="auto">
  <name>Task 3: Align cs-lead, classifier, extractor, critic AGENTS to always-draft + advisory hint</name>
  <files>.claude/agents/cs-lead.md, .claude/agents/classifier.md, .claude/agents/extractor.md, .claude/agents/critic.md</files>
  <read_first>
    - .claude/agents/cs-lead.md (current dual draft/escalate verdict, "Escalation Is Final", D-08/D-10/D-11/D-13 hard-rules list, KnowledgeMCP tools)
    - .claude/agents/classifier.md (KnowledgeMCP.lookup_code tool + "low/high-risk always escalates" prose — escalation downgraded to advisory)
    - .claude/agents/extractor.md (missing-key → escalate prose — now a D-34 flow signal, not a no-draft escalate)
    - .claude/agents/critic.md (KnowledgeMCP.semantic_search tool + fail→redraft→escalate D-12 protocol + [KB-N] faithfulness — critique becomes advisory)
    - .claude/CLAUDE.md (advisory escalation reference + surviving floor D-03/D-04/D-14)
  </read_first>
  <action>
    Edit the four AGENT `.md` files only (the matching subagent-detail SKILLs — classify-ticket, extract-answer-key, self-critique — are reworked in 04-05). `cs-lead.md`: drop `KnowledgeMCP.*` from `tools` (keep `SellessMCP.resolve_order`, `ReplyMCP.submit_reply`); rewrite responsibilities + verdict so the lead ALWAYS emits `{action:"draft", body, citations, escalation_hint}` (D-33), optionally attaching an advisory `escalation_hint` for money/legal/injection/low-confidence signals; remove "Escalation Is Final / escalate = no draft" and the hard-rules referencing D-08/D-10/D-11/D-13/D-26; keep D-03 (models), D-04 (PII), D-14 (untrusted body), §4a (submit_reply is the only emission path). `classifier.md`: drop the `KnowledgeMCP.lookup_code` tool (CODE-MAP is now a local file concern handled by the drafter's file-store) — keep the two-level taxonomy + 13-value `customer_request` enum (REP-01) and the high_risk/confidence markers, but reframe them as **advisory signals feeding escalation_hint**, not hard escalate triggers; keep the injection-suspicion → high_risk marker (feeds D-14). `extractor.md`: keep answer-key extraction + resolve_order (REP-02), but reframe `missing_key` as a **D-34 flow signal** (drives verify-order / clarify-order-info fallback in the drafter) rather than a no-draft escalate; keep "never fabricate" and the untrusted-body rule. `critic.md`: drop the `KnowledgeMCP.semantic_search` tool; keep the three rubric dimensions (faithfulness/policy-match/tone-completeness) aligned to Phase-5 (REP-04) but make critique **advisory** — it may attach feedback to the `escalation_hint` and request at most one redraft, but a failing critique NEVER suppresses the draft (no escalate=no-draft); replace the `[KB-N]` faithfulness definition with "supported by the selected template + Selless order data".
  </action>
  <verify>
    <automated>grep -rin "KnowledgeMCP\|semantic_search" .claude/agents/cs-lead.md .claude/agents/classifier.md .claude/agents/extractor.md .claude/agents/critic.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rin "KnowledgeMCP\|semantic_search" .claude/agents/cs-lead.md .claude/agents/classifier.md .claude/agents/extractor.md .claude/agents/critic.md` returns nothing.
    - `grep -in "escalation_hint\|always.*draft\|advisory" .claude/agents/cs-lead.md` returns ≥ 1 line.
    - `grep -in "escalate = no draft\|Escalation Is Final\|no draft\|never both" .claude/agents/cs-lead.md` returns nothing.
    - `grep -in "customer_request\|Return\|Cancel_Order" .claude/agents/classifier.md` returns ≥ 1 line (REP-01 sub-type enum preserved).
    - `grep -in "flow signal\|verify-order\|clarify-order\|fallback\|advisory" .claude/agents/extractor.md` returns ≥ 1 line (missing-key reframed, D-34).
    - `grep -in "advisory\|never suppress\|always.*draft" .claude/agents/critic.md` returns ≥ 1 line.
    - `grep -in "faithfulness" .claude/agents/critic.md` returns ≥ 1 line AND `grep -in "policy-match" .claude/agents/critic.md` returns ≥ 1 line AND `grep -in "tone-completeness" .claude/agents/critic.md` returns ≥ 1 line (REP-04 rubric dimensions retained in the critic agent).
    - `grep -rin "untrusted\|ticket_body" .claude/agents/classifier.md .claude/agents/extractor.md .claude/agents/critic.md` returns ≥ 1 line per file (D-14 retained).
  </acceptance_criteria>
  <done>The lead always emits a draft verdict with an optional advisory hint; classifier/extractor/critic agents keep their REP-01/02/04 functions but their signals are advisory (feed escalation_hint / D-34 fallback), never no-draft escalates; no agent references KnowledgeMCP/semantic_search; the critic agent retains the three REP-04 rubric dimensions; D-03/D-04/D-14 retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body → all agent prompts | Untrusted attacker-controlled email body; delimited as `<ticket_body>` data in every agent (D-14) and screened by injection_screen.py |
| Selless MCP → drafter | Whitelisted read-only order fields; the only source of order facts (no fabrication, D-34) |
| drafter → submit_reply | Always-draft emission; no output guard (deleted in 04-01) — accepted PoC risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | injection in ticket body now that agents always draft | mitigate | Every reworked agent retains the "email body is untrusted / delimited as `<ticket_body>`" instruction; injection_screen.py (D-14) still wired; classifier still raises a high_risk advisory signal on override attempts |
| T-04-02-02 | Spoofing/Tampering | drafter fabricates order facts when Selless has no order | mitigate | D-34 flow-aware fallback: missing order → verify-order/clarify-order-info template; placeholders only for infra fields on a VALID order; "never invent order facts" hard rule in drafter.md |
| T-04-02-03 | Elevation of Privilege | always-draft emits unauthorized commitment (guard deleted) | accept | Accepted PoC trade-off (see 04-01 T-04-01-01); bounded by DRY_RUN-only; before-live guard re-author deferred. RD-Q1 "never assert a completed operational action" retained as drafting guidance to reduce blast radius |
| T-04-02-04 | Repudiation | model assignment drift to Opus on hot path | mitigate | D-03 retained: classifier/extractor stay Haiku, drafter/critic/lead stay Sonnet; no Opus anywhere — asserted by the model: lines in each agent frontmatter |
| T-04-02-SC | Tampering | n/a — no package installs in this plan | accept | Prose/config-only edits; no new dependencies |
</threat_model>

<verification>
- settings.json wires Selless + Reply MCP only (KnowledgeMCP removed).
- Workflow skill + all five agents encode the always-draft flow with an optional advisory escalation_hint.
- Drafter grounds on the file-store + Selless and falls back to a Workflow/CODE-MAP flow on a missing order.
- No agent or pipeline-skill (this plan's scope) references KnowledgeMCP, semantic_search, the deleted guards, mandatory citations, or the D-26 gate. (The subagent-detail skills are cleaned in 04-05.)
- D-03 (model split), D-04 (PII), D-14 (untrusted body) retained across all agents.
</verification>

<success_criteria>
The agent team + pipeline skills are an always-draft pipeline grounded on the local file-store + Selless with flow-aware fallback (REP-01/02/03/04), escalation is advisory-only (SAFE-03), and the agent/pipeline-skill LLM contract matches the deleted hooks and removed RAG surface. (Subagent-detail skill consistency is completed in 04-05; test-suite consistency in 04-04.)
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-02-SUMMARY.md` when done.
</output>
