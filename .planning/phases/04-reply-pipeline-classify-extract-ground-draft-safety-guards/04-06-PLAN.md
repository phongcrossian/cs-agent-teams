---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 06
type: execute
wave: 7
depends_on: ["04-01", "04-05"]
files_modified:
  - .claude/hooks/authorized_offer.py
  - tests/cs_team/test_authorized_offer.py
autonomous: true
requirements: [SAFE-04]
must_haves:
  truths:
    - "A pure-Python function decides AUTHORIZED vs UNAUTHORIZED for an offer given (sub_type, template_code, offered_thresholds, eligibility) without any LLM call"
    - "An offer is AUTHORIZED only when template matches the flow AND every offered threshold is within its cap AND eligibility passes AND it follows the documented flow"
    - "Every RULES §2 sub-type maps to an entry in the template/threshold registry or to a force-escalate sub-type"
    - "Eligibility is read from a typed stub surface (RD-Q2) whose call sites are clearly marked STUB so the real Selless check drops in without reshaping the function"
  artifacts:
    - path: ".claude/hooks/authorized_offer.py"
      provides: "Deterministic authorized-offer decision module + template/threshold registry + eligibility stub contract"
      contains: "def authorize_offer"
    - path: "tests/cs_team/test_authorized_offer.py"
      provides: "Unit proofs for every RULES §2 row decision"
      contains: "def test_"
  key_links:
    - from: ".claude/hooks/pre_send_guard.py"
      to: ".claude/hooks/authorized_offer.py"
      via: "import authorize_offer (wired in plan 04-09)"
      pattern: "from .* authorized_offer import|import authorized_offer"
---

<objective>
Build the deterministic core of the new D-26 guard as a standalone, importable, LLM-free module
`.claude/hooks/authorized_offer.py`. This module encodes the §0 authorized/unauthorized test, the
per-sub-type template registry, the threshold caps (THR-05/06/07/08), and the eligibility-stub
contract (RD-Q2). It is consumed by `pre_send_guard.py` (plan 04-09) and mirrored by the drafter
(plan 04-10). Building it first gives both downstream consumers a fixed contract — no scavenger hunt.

Purpose: The guard rework is the load-bearing change of the reopen. Isolating the decision logic in a
pure module makes it unit-testable per RULES §2 row, keeps `pre_send_guard.py` a thin stdin/exit-code
shim, and lets the deferred real-eligibility plan (04-11) swap one clearly-marked STUB function.
Output: `authorized_offer.py` + its exhaustive unit test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-AUTHORIZED-OFFER-RULES.md
@.planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md
@.planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP-templates.md
@.claude/CLAUDE.md

<interfaces>
<!-- Existing guard contract this module is built to fit. Extracted from codebase — use directly. -->

From .claude/hooks/pre_send_guard.py (the consumer; do NOT edit it in this plan):
- main() reads stdin JSON {"tool_name","tool_input":{"body","citations"}}; exit 2 = BLOCK, exit 0 = pass.
- Existing helper contract mirrors src/guards/loop_guard.should_suppress: (bool, reason: str).

From .claude/hooks/escalation_gate.py (sibling pattern to mirror):
- Module-level registries are plain Python literals; functions are pure; main() is the only I/O.

Threshold caps (POLICY-THRESHOLD-INDEX.md — exact IDs, do NOT invent):
- THR-05 = 40% discount + free shipping; THR-06 = ≤20% retention/courtesy; THR-07 = 50% refund;
  THR-08 = ≤50% late-ship compensation. THR-03 = 45 days from purchase; THR-04 = 14 days from delivery.

RULES §2 sub-types (the level-2 Customer_Request taxonomy — exact strings):
Return, Replace, Partial_Refund, Full_Refund, Review, Cancel_Order, Change_Shipping_Address,
Change_Product_Variant, Ask_About_Delivery_Status, Ask_About_Order, Ask_About_Policy,
Ask_About_Product, Ask_About_Promotion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: authorized_offer decision module + registries</name>
  <files>.claude/hooks/authorized_offer.py</files>
  <read_first>
    - .claude/hooks/authorized_offer.py (target — does not exist yet; create)
    - .planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-AUTHORIZED-OFFER-RULES.md (§0 guard core, §1 execution boundary, §2 per-category table — THE rule source)
    - .planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md (THR-05/06/07/08, THR-03/04 exact values)
    - .planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP-templates.md (which template codes belong to which flow: B7/B5/B6/B3 return, A4/A5/A9 full-refund, F1–F23 cancellation retention, E1–E13 change, G-codes shipping)
    - .claude/hooks/escalation_gate.py (sibling style: module-level literal registries, pure functions, (bool,reason))
  </read_first>
  <behavior>
    - authorize_offer(sub_type="Partial_Refund", template_code="B7", offered={"refund_pct":50,"discount_pct":40}, eligibility={"in_warranty":True,"prior_remediation":False}) -> (True, "authorized:B7")
    - authorize_offer(sub_type="Partial_Refund", template_code="B7", offered={"refund_pct":70}, ...) -> (False, "unauthorized:over_threshold:THR-07")
    - authorize_offer(sub_type="Return", template_code="C1", offered={...}, eligibility={"in_warranty":False}) -> (False, "unauthorized:ineligible:warranty")
    - authorize_offer(sub_type="Return", template_code="ZZ9", ...) -> (False, "unauthorized:out_of_template")
    - authorize_offer(sub_type="Return", template_code="B7", eligibility={"prior_remediation":True}) -> (False, "unauthorized:second_remediation")
    - authorize_offer(sub_type="Review", ...) -> (False, "unauthorized:force_escalate:no_flow")
    - authorize_offer(sub_type="Cancel_Order", template_code="F1", offered={"refund_pct":20}, ...) -> (True, "authorized:F1") for the ≤20% retention offer (THR-06)
    - authorize_offer(sub_type="Change_Shipping_Address", asserts_mutation=True, ...) -> (False, "unauthorized:operational_assertion") per RD-Q1
    - authorize_offer(sub_type="Ask_About_Order", offered={}, ...) -> (True, "authorized:no_offer") informational, no commitment
  </behavior>
  <action>
    Create `.claude/hooks/authorized_offer.py` as a pure, LLM-free, dependency-free (stdlib only) module.
    Define three module-level literal registries derived from the read_first sources (NO fenced code in this
    plan — author them in the file):
    (1) TEMPLATE_REGISTRY: mapping sub_type -> set of approved template codes for that flow. Return →
    {B3,B5,B6,B7,B8,B9,B10,B11,B12,B13,A4,A5,A6,A7,A8,A9}; Replace → A/B replacement codes (A1,A2,A3,B1,B2,
    + G11/G14 replacement); Partial_Refund → {B3,B7,A9}; Full_Refund → {A4,A5,A9} (evidence-gated set);
    Cancel_Order → F-codes (F1–F23); Change_Shipping_Address/Change_Product_Variant → E-codes; the four
    Ask_About_* inquiry sub-types → empty set (informational, no template-gated commitment required).
    (2) THRESHOLD_CAPS: refund_pct cap by flow (Return/Partial_Refund=THR-07 50; Cancel_Order retention=
    THR-06 20; late-ship comp=THR-08 50) and discount_pct cap (THR-05 40). Encode each cap with its THR-xx ID
    string so reasons can name the violated threshold.
    (3) FORCE_ESCALATE_SUBTYPES: {"Review"} (RULES §2A — no flow exists, Q2) plus any sub_type with
    asserts_mutation=True (RD-Q1 operational-action assertion).
    Implement `authorize_offer(sub_type, template_code=None, offered=None, eligibility=None,
    asserts_mutation=False) -> tuple[bool, str]` applying §0 in order: (a) if sub_type in
    FORCE_ESCALATE_SUBTYPES or asserts_mutation → (False, "unauthorized:force_escalate:no_flow" /
    "unauthorized:operational_assertion"); (b) inquiry sub-types with no offer → (True, "authorized:no_offer");
    (c) template_code must be in TEMPLATE_REGISTRY[sub_type] else (False, "unauthorized:out_of_template");
    (d) each offered pct must be ≤ its cap else (False, f"unauthorized:over_threshold:{THR_ID}"); (e)
    eligibility["in_warranty"] must be True else (False, "unauthorized:ineligible:warranty"); (f)
    eligibility["prior_remediation"] must be False else (False, "unauthorized:second_remediation"); else
    (True, f"authorized:{template_code}"). Mirror the loop_guard (bool, reason) signature exactly.
    Add a clearly-marked `# STUB (RD-Q2): real eligibility comes from Selless in plan 04-11` comment block
    at the single point where eligibility is consumed, and a helper `default_eligibility()` returning the
    PoC stub (in_warranty=True, prior_remediation=False, variant_in_stock=True) so the real check is a
    one-function swap. Add a `# STUB (RD-Q3): evidence treated as sufficient this phase` marker on the
    Full_Refund branch. Make all failure reasons deterministic strings (no f-string randomness).
  </action>
  <acceptance_criteria>
    - File `.claude/hooks/authorized_offer.py` exists and imports with stdlib only:
      `uv run python -c "import importlib.util,sys; spec=importlib.util.spec_from_file_location('ao','.claude/hooks/authorized_offer.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print(hasattr(m,'authorize_offer'))"` prints `True`.
    - `grep -n "def authorize_offer" .claude/hooks/authorized_offer.py` returns exactly one match.
    - All four threshold IDs present: `grep -oE "THR-0[5678]" .claude/hooks/authorized_offer.py | sort -u | tr '\n' ' '` outputs `THR-05 THR-06 THR-07 THR-08`.
    - Force-escalate set names Review: `grep -c "Review" .claude/hooks/authorized_offer.py` ≥ 1.
    - STUB markers present: `grep -cE "STUB \(RD-Q[23]\)" .claude/hooks/authorized_offer.py` returns ≥ 2.
    - No LLM/network imports: `grep -vE '^\s*#' .claude/hooks/authorized_offer.py | grep -cE "anthropic|httpx|requests|openai"` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import importlib.util; spec=importlib.util.spec_from_file_location('ao','.claude/hooks/authorized_offer.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.authorize_offer('Review')[0] is False; assert m.authorize_offer('Partial_Refund','B7',{'refund_pct':50,'discount_pct':40},{'in_warranty':True,'prior_remediation':False})[0] is True; assert m.authorize_offer('Partial_Refund','B7',{'refund_pct':70},{'in_warranty':True,'prior_remediation':False})[0] is False; print('ok')"</automated>
  </verify>
  <done>authorize_offer returns (True, "authorized:...") only for in-template + in-threshold + eligible + non-asserting offers; every other path returns (False, "unauthorized:<reason>"); STUB markers flag the RD-Q2/Q3 swap points.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaustive RULES §2 unit tests</name>
  <files>tests/cs_team/test_authorized_offer.py</files>
  <read_first>
    - tests/cs_team/test_authorized_offer.py (target — create)
    - .claude/hooks/authorized_offer.py (the module under test, from Task 1)
    - .planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-AUTHORIZED-OFFER-RULES.md (§2A/2B/2C rows — the answer key each test asserts)
    - tests/cs_team/test_hooks.py (analog: in-process pure-function assertions, no subprocess)
  </read_first>
  <behavior>
    - One test per RULES §2 row asserting the expected (bool, reason) for the AUTHORIZED happy path AND the dominant UNAUTHORIZED failure axis for that row.
    - 2A: Return AUTO* in-warranty B7 -> True; Return out-of-warranty -> False ineligible. Replace in-stock A-code -> True; Replace force-escalate when no template. Partial_Refund 50%+40% -> True; 70% -> over_threshold. Full_Refund A4 eligible -> True; Review -> always False force_escalate.
    - 2B: Cancel_Order F-code 20% retention -> True; Cancel_Order asserts_mutation -> operational_assertion False. Change_Shipping_Address asserts_mutation -> False; non-asserting acknowledgement (asserts_mutation=False, no over-threshold offer) -> True.
    - 2C: Ask_About_Order/Policy/Product/Promotion with no offer -> True authorized:no_offer; Ask_About_Delivery_Status with THR-08 comp ≤50 -> True, >50 -> over_threshold False.
  </behavior>
  <action>
    Create `tests/cs_team/test_authorized_offer.py` importing `authorize_offer` from
    `.claude/hooks/authorized_offer.py` (load via importlib so the dotted hooks path resolves, mirroring how
    tests/cs_team/test_hooks_subprocess.py locates hooks under _REPO_ROOT). Write one pytest function per
    RULES §2 row (13 sub-types) covering both the authorized happy path and the primary unauthorized axis,
    asserting BOTH the boolean and the reason-string prefix (e.g. assert reason.startswith("unauthorized:
    over_threshold")). Add an explicit test that the inquiry sub-types never require a template. No LLM, no
    network, no DB — pure function calls only. All tests must run in CI without skip markers.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/cs_team/test_authorized_offer.py -q` exits 0 with ≥ 13 passing tests.
    - Every RULES §2 sub-type appears as a test target: `grep -oE "Return|Replace|Partial_Refund|Full_Refund|Review|Cancel_Order|Change_Shipping_Address|Change_Product_Variant|Ask_About_Delivery_Status|Ask_About_Order|Ask_About_Policy|Ask_About_Product|Ask_About_Promotion" tests/cs_team/test_authorized_offer.py | sort -u | wc -l` returns `13`.
    - At least one test asserts a reason-string prefix: `grep -c "startswith(\"unauthorized" tests/cs_team/test_authorized_offer.py` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cs_team/test_authorized_offer.py -q</automated>
  </verify>
  <done>All 13 sub-type rows have passing authorized + unauthorized assertions; test file runs in CI with no skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafter (LLM) → authorize_offer | LLM-supplied sub_type/template_code/offered values are untrusted; the module must not trust them, only validate against fixed registries |
| eligibility stub → guard | RD-Q2 stub returns optimistic defaults; real Selless data replaces it in 04-11 — fail-closed posture must hold when fields are absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | LLM-supplied template_code claiming an unapproved code | mitigate | TEMPLATE_REGISTRY membership check; unknown code → out_of_template (False) |
| T-04-06-02 | Elevation | over-threshold offer slipping through | mitigate | THRESHOLD_CAPS numeric ≤ check per offered pct; reason names the THR-xx |
| T-04-06-03 | Spoofing | asserts_mutation false-claim of completed operational action | mitigate | RD-Q1 operational_assertion branch forces unauthorized |
| T-04-06-04 | Information disclosure | eligibility dict missing a field defaulting to permissive | accept | PoC stub is optimistic by RD-Q2 design; 04-11 replaces with fail-closed real check; documented STUB markers |
</threat_model>

<verification>
- `uv run pytest tests/cs_team/test_authorized_offer.py -q` green.
- Module imports with stdlib only (no anthropic/httpx).
- All four THR IDs and the Review force-escalate present.
</verification>

<success_criteria>
A pure deterministic `authorize_offer` exists, returns (bool, reason) per §0, covers every RULES §2 sub-type,
and exposes a single clearly-marked eligibility STUB swap point for plan 04-11.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-06-SUMMARY.md` when done
</output>
