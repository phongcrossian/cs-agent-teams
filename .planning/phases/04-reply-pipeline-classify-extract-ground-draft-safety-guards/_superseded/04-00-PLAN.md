---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/config.py
  - src/pipeline/__init__.py
  - src/pipeline/models.py
  - src/pipeline/errors.py
  - src/pipeline/tracing.py
  - src/pipeline/llm_client.py
  - tests/pipeline/__init__.py
  - tests/pipeline/conftest.py
  - tests/pipeline/test_models.py
  - tests/pipeline/test_config_redaction.py
  - tests/pipeline/test_guards.py
  - tests/pipeline/test_escalation.py
  - tests/pipeline/test_orchestrator.py
autonomous: false
requirements: [REP-01, REP-02, REP-03, REP-04, SAFE-03, SAFE-04]
user_setup:
  - service: anthropic
    why: "First LLM calls enter the codebase this phase (classify/extract/draft/critique)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> Settings -> API Keys"
  - service: langfuse
    why: "Trace sink for the new LLM calls (D-04); via OpenTelemetry"
    env_vars:
      - name: LANGFUSE_PUBLIC_KEY
        source: "Langfuse project settings -> API Keys"
      - name: LANGFUSE_SECRET_KEY
        source: "Langfuse project settings -> API Keys"

must_haves:
  truths:
    - "The pipeline package imports cleanly and exposes the verdict/stage Pydantic schemas the worker and Phase-5 harness will consume"
    - "Anthropic + Langfuse secrets never appear in Settings.__repr__ or logs"
    - "Per-stage model IDs resolve to claude-haiku-4-5 (classify/extract/risk) and claude-sonnet-4-6 (draft/critic) — no Opus on the hot path"
  artifacts:
    - path: "src/pipeline/models.py"
      provides: "PipelineResult / EscalationVerdict / DraftVerdict / ClassifyOutput / ExtractOutput / GroundOutput / CritiqueScores"
      contains: "class PipelineResult"
    - path: "src/pipeline/errors.py"
      provides: "PipelineFatalError / PipelineTransientError / PipelineEscalationError taxonomy"
      contains: "class PipelineEscalationError"
    - path: "src/pipeline/llm_client.py"
      provides: "injectable LLM client seam (set_llm_client / _get_llm_client) + model resolution per stage"
      contains: "def set_llm_client"
    - path: "src/pipeline/tracing.py"
      provides: "OTel->Langfuse tracer init + trace_stage context manager (PII-redacted attrs)"
      contains: "def trace_stage"
    - path: "src/config.py"
      provides: "Anthropic/model/Langfuse settings + extended __repr__ redaction"
      contains: "anthropic_api_key"
  key_links:
    - from: "src/pipeline/models.py"
      to: "src/knowledge_mcp/models.py"
      via: "import Citation"
      pattern: "from src.knowledge_mcp.models import"
    - from: "src/config.py __repr__"
      to: "anthropic_api_key / langfuse_secret_key"
      via: "REDACTED placeholder"
      pattern: "anthropic_api_key=<REDACTED>"
---

<objective>
Bootstrap Wave 0 cho reply pipeline: cài đặt các phụ thuộc LLM mới, mở rộng `Settings`
(Anthropic / model-per-stage / Langfuse, kèm redaction bí mật), định nghĩa schema kết quả
pipeline dùng chung (verdict + stage outputs), error taxonomy, seam LLM client có thể inject,
khung tracing Langfuse/OTel, và toàn bộ RED test stubs cho các plan sau.

Purpose: Đây là phase đầu tiên LLM đi vào codebase. Bootstrap thiết lập một code path
production duy nhất mà cả worker (D-02) và harness eval Phase-5 sẽ gọi qua, với cost-discipline
(Haiku/Sonnet split, no Opus) và PII/secret redaction được khóa ngay từ contract.
Output: package `src/pipeline/` với schema + errors + tracing + client seam; config mở rộng;
RED test scaffolds (fail cho tới khi plan 01/02/03 implement).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor MUST reuse. Extracted from codebase 2026-06-02. -->

From src/knowledge_mcp/models.py (import these — DO NOT redefine):
  class Citation(BaseModel):
      text: str; source: str; source_type: str; authority_rank: int
      recency_flag: Optional[str] = None   # D-15: "stale" if flagged, else None
      snapshot_version: str; score: float
      conflict_id: Optional[str] = None    # D-14: CONTRA-* if part of known conflict
  class SemanticSearchResult(BaseModel):
      citations: list[Citation]; conflict: bool; resolved_by_override: bool
  class ThresholdResult(BaseModel):
      threshold_id: str; value: str; source: str
      conflict_id: Optional[str] = None; override_resolution: Optional[str] = None
  class TemplateResult(BaseModel):
      code: str; scenario: str; subject_template: str; body_template: str
      source: str; authority_rank: int

From src/selless_mcp/server.py (the grounding tools stage code will call):
  async def _impl_get_order_status(order_id, client=None) -> OrderDetail
  async def _impl_get_customer_info(customer_id, client=None) -> CustomerInfo
  async def _impl_get_purchase_history(customer_id, client=None) -> PurchaseHistory
  async def _impl_get_ticket_history(order_id, client=None) -> TicketHistory
  async def _impl_resolve_order(param, client=None) -> ResolvedOrder
  def set_selless_client(client) -> None   # test seam

From src/config.py (extend this Settings class; mirror Phase 3 Field block ~lines 90-129):
  class SendMode(str, Enum): DRY_RUN / LIVE
  class Settings(BaseSettings): ... def __repr__(self) -> str  (lines ~142-151, redacts secrets)

From src/guards/pii.py:
  def redact_text(text: str) -> str   # call BEFORE any log/trace write of ticket content
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate (ASSUMED packages — no RESEARCH audit table exists)</name>
  <read_first>
    - CLAUDE.md §"Recommended Stack" / "What NOT to Use" (locks PydanticAI, anthropic SDK, Langfuse via OTel, Guardrails AI/NeMo; forbids MCP SDK v2, Opus hot path)
    - pyproject.toml (current dependency pins — Phase 3 block lines 24-28 is the template)
  </read_first>
  <what-built>
    No AI-SPEC/RESEARCH Package Legitimacy Audit exists for Phase 4, so the new pip packages are
    treated as [ASSUMED] and must be human-verified before install (planner protocol fallback).
    Packages to add (pin latest stable at verify time): pydantic-ai (1.x), anthropic (latest),
    langfuse (latest), opentelemetry-sdk + opentelemetry-exporter-otlp (latest), and one of
    guardrails-ai OR nemoguardrails (Claude's Discretion D — pick lightest; plain validators are
    also acceptable, in which case no guardrails framework is installed). MCP client deps already
    present via fastmcp==3.3.1.
  </what-built>
  <how-to-verify>
    1. Confirm each package on its registry page: pypi.org/project/pydantic-ai,
       pypi.org/project/anthropic, pypi.org/project/langfuse, pypi.org/project/opentelemetry-sdk,
       pypi.org/project/guardrails-ai (or pypi.org/project/nemoguardrails).
    2. Verify publisher + recent release date + download counts look legitimate (not typosquat).
    3. Confirm NO MCP SDK v2 and NO Opus model is being pulled in (CLAUDE.md "What NOT to Use").
  </how-to-verify>
  <resume-signal>Type "approved" with the confirmed package set + pins, or list packages to reject/swap</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add deps + extend Settings with Anthropic/model/Langfuse config + redaction</name>
  <files>pyproject.toml, src/config.py, src/pipeline/__init__.py</files>
  <read_first>
    - pyproject.toml lines 10-35 (dependencies + dev block + pytest config)
    - src/config.py lines 28-151 (Settings class, Phase 3 Field block 90-129, __repr__ 142-151) — 04-PATTERNS.md §src/config.py
    - 04-CONTEXT.md D-03 (model assignment), D-04 (Langfuse + prompt caching)
  </read_first>
  <action>
    Add the human-approved packages to pyproject.toml [project.dependencies] under a new
    "# Phase 4 additions — reply pipeline" comment block, with the exact pins approved in Task 0.
    Run `uv sync`. Create empty src/pipeline/__init__.py (mirror src/guards/__init__.py).
    Extend Settings with new Field()s (copy the Phase 3 Field pattern): anthropic_api_key (default ""),
    classify_model (default "claude-haiku-4-5", used for classify/extract/risk per D-03),
    draft_model (default "claude-sonnet-4-6", used for draft + self-critique per D-03),
    langfuse_public_key (default ""), langfuse_secret_key (default ""),
    langfuse_host (default "https://cloud.langfuse.com"), and prompt_cache_enabled (default True, D-04).
    Extend Settings.__repr__ to add anthropic_api_key=<REDACTED> and langfuse_secret_key=<REDACTED>
    to the existing redaction string. Do NOT add any model field defaulting to an Opus ID.
  </action>
  <verify>
    <automated>uv run python -c "from src.config import Settings; s=Settings(anthropic_api_key='sk-secret', langfuse_secret_key='lf-secret'); r=repr(s); assert 'sk-secret' not in r and 'lf-secret' not in r and '<REDACTED>' in r; assert s.classify_model=='claude-haiku-4-5' and s.draft_model=='claude-sonnet-4-6' and 'opus' not in (s.classify_model+s.draft_model).lower(); print('ok')"</automated>
  </verify>
  <done>uv sync succeeds with new pins; Settings exposes the 7 new fields; __repr__ redacts both secrets; no Opus model ID anywhere in config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define pipeline schemas + errors + LLM client seam + tracing skeleton</name>
  <files>src/pipeline/models.py, src/pipeline/errors.py, src/pipeline/llm_client.py, src/pipeline/tracing.py</files>
  <read_first>
    - 04-PATTERNS.md §src/pipeline/models.py (the exact Pydantic field shapes to define), §errors.py, §tracing.py, §"Injectable Singleton Client Pattern"
    - src/knowledge_mcp/models.py lines 24-86 (Citation/SemanticSearchResult/ThresholdResult/TemplateResult — import, don't redefine)
    - src/selless_mcp/server.py lines 116-143 (set_selless_client/_get_client seam to mirror)
    - src/freshdesk_io/errors.py lines 1-35 (three-class taxonomy to mirror)
    - src/observability.py (thin-wrapper style for tracing.py)
    - src/guards/pii.py line 56 (redact_text — tracing attrs must be redacted first)
  </read_first>
  <behavior>
    - Test: PipelineResult accepts verdict=EscalationVerdict(action="escalate", reason="conflict_flag", risk_signals=["conflict_flag"]) and verdict=DraftVerdict(action="draft", body_html="<p>x</p>", citations=[], critique_scores=...)
    - Test: ClassifyOutput rejects category not in the 4-category Literal; confidence Literal is high|med|low (D-06)
    - Test: ExtractOutput.requires_order_data is required; resolved_order_id defaults None (D-07)
    - Test: CritiqueScores has faithfulness/policy_match/tone_completeness/overall Literal["pass","fail"] + feedback (D-12)
    - Test: PipelineEscalationError(verdict).verdict is the carried EscalationVerdict
    - Test: set_llm_client(obj) then _get_llm_client() returns obj without constructing AsyncAnthropic
    - Test: model_for_stage("classify")=="claude-haiku-4-5", ("extract")=="claude-haiku-4-5", ("draft")=="claude-sonnet-4-6", ("critique")=="claude-sonnet-4-6"; never returns an Opus ID
  </behavior>
  <action>
    models.py: import Citation from src.knowledge_mcp.models. Define EscalationVerdict (action:
    Literal["escalate"], reason: str, risk_signals: list[str]), DraftVerdict (action:
    Literal["draft"], body_html: str, citations: list[Citation], critique_scores: CritiqueScores),
    PipelineResult (verdict: EscalationVerdict | DraftVerdict) with a convenience .action property
    delegating to verdict.action, ClassifyOutput (category Literal of order_tracking |
    returns_refunds_exchanges | quality_complaint | policy_product; high_risk: bool; confidence
    Literal high|med|low), ExtractOutput (order_code/customer_email/resolved_order_id Optional;
    issue_type: str; product_refs: list[str]=[]; requires_order_data: bool), GroundOutput (citations:
    list[Citation]; conflict: bool; resolved_by_override: bool; stale_only: bool; template:
    Optional[TemplateResult]; selless_facts: dict), CritiqueScores (4 Literal["pass","fail"] dims +
    feedback: str). errors.py: PipelineFatalError, PipelineTransientError (optional retry_after),
    PipelineEscalationError(verdict) carrying the EscalationVerdict — mirror freshdesk_io/errors.py.
    llm_client.py: module-level _llm_client singleton + set_llm_client(client)/_get_llm_client()
    (lazy AsyncAnthropic(api_key=settings.anthropic_api_key)); plus model_for_stage(stage:str)->str
    reading settings.classify_model / settings.draft_model (D-03). tracing.py: lazy get_tracer()
    (OTel) + trace_stage(stage_name, ticket_id, **attrs) context manager that calls redact_text() on
    any string attr before setting span attributes (CLAUDE.md PII rule). No fenced code in source
    beyond what PATTERNS specifies.
  </action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_models.py -x -q</automated>
  </verify>
  <done>All schema/errors/client/tracing tests in test_models.py pass; Citation imported (not redefined); model_for_stage never returns Opus; trace_stage redacts string attrs.</done>
</task>

<task type="auto">
  <name>Task 3: Create RED test scaffolds + pipeline test fixtures for plans 01/02/03</name>
  <files>tests/pipeline/__init__.py, tests/pipeline/conftest.py, tests/pipeline/test_config_redaction.py, tests/pipeline/test_guards.py, tests/pipeline/test_escalation.py, tests/pipeline/test_orchestrator.py</files>
  <read_first>
    - tests/conftest.py lines 1-180 (db_pool, mock_selless_client, fixture style, pytest_collection_modifyitems)
    - 04-PATTERNS.md §"Injectable Singleton Client Pattern" + §"Deterministic Guard Returns (bool,str)"
    - src/selless_mcp/server.py set_selless_client (the mock-injection seam tests reuse)
  </read_first>
  <action>
    Create tests/pipeline/conftest.py with a MockLLMClient fixture (records stage calls; returns
    canned structured outputs keyed by stage) and a mock_knowledge_mcp fixture (returns a
    SemanticSearchResult with toggleable conflict/stale/citations). Reuse mock_selless_client from
    the root conftest. Write RED stubs that import the not-yet-implemented modules and assert the
    Wave-1/2/3 behaviors so they FAIL now: test_guards.py (commitment-language blocks + injection
    screen), test_escalation.py (deterministic/conflict/stale checks return (bool,str)),
    test_orchestrator.py (early-exit escalation verdicts per signal + happy-path DraftVerdict),
    test_config_redaction.py (already-implemented redaction — should PASS). Each RED stub may use
    pytest.importorskip OR a direct import that raises ImportError until the module exists; prefer
    a real import wrapped so the failure is a clear "not implemented yet" assertion, not a collection
    error that blocks the whole suite.
  </action>
  <verify>
    <automated>uv run pytest tests/pipeline/ -q 2>&1 | grep -E "passed|failed" ; uv run pytest tests/pipeline/test_config_redaction.py -x -q</automated>
  </verify>
  <done>test_config_redaction.py passes; test_guards/test_escalation/test_orchestrator collect and FAIL with not-implemented assertions (RED, expected); suite does not error-out on collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codebase → Anthropic/Langfuse cloud | API keys leave the process; must never be logged |
| config → logs/traces | Settings repr / span attrs could leak secrets or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Settings.__repr__ leaking anthropic_api_key/langfuse_secret_key | mitigate | Task 1 extends __repr__ redaction list; test asserts secret strings absent (Task 1 verify) |
| T-04-02 | Information Disclosure | tracing.py span attributes leaking ticket PII to Langfuse | mitigate | trace_stage() calls redact_text() on string attrs before span set (Task 2) |
| T-04-03 | Elevation/Cost | accidentally configuring Opus on hot path | mitigate | model_for_stage reads Haiku/Sonnet fields only; test asserts no Opus (Task 2); config has no Opus default (Task 1) |
| T-04-SC | Tampering | pip installs of pydantic-ai/anthropic/langfuse/otel/guardrails | mitigate | blocking-human legitimacy checkpoint Task 0 (no RESEARCH audit table → all [ASSUMED]); verify on pypi.org |
</threat_model>

<verification>
- `uv sync` succeeds; `uv run python -c "import src.pipeline.models, src.pipeline.errors, src.pipeline.llm_client, src.pipeline.tracing"` imports clean.
- Secret + PII redaction asserted by tests; no Opus model ID present anywhere.
</verification>

<success_criteria>
Pipeline package contract (schemas/errors/client-seam/tracing) exists and imports; config exposes
per-stage Haiku/Sonnet models with secrets redacted; RED scaffolds in place for plans 01/02/03.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-00-SUMMARY.md` when done
</output>
