---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 11
type: execute
wave: 10
depends_on: ["04-06", "04-09", "04-10"]
files_modified:
  - .claude/hooks/authorized_offer.py
  - src/selless_mcp/server.py
  - src/selless_mcp/models.py
  - tests/cs_team/test_real_eligibility.py
autonomous: false
deferred: true
requirements: [SAFE-04]
must_haves:
  truths:
    - "authorize_offer reads real eligibility (warranty dates → THR-03/04 window, prior-remediation state, real variant stock) from Selless instead of the RD-Q2 stub"
    - "Full_Refund / evidence-gated paths validate real submitted evidence (photo + shipping label) instead of accept-as-sufficient (RD-Q3)"
    - "When a real eligibility or evidence field is unavailable, the AUTO* path degrades to escalate (fail-closed), not to an optimistic pass"
    - "The guard's structure is unchanged — only the eligibility/evidence DATA SOURCE is swapped at the marked STUB points"
  artifacts:
    - path: "src/selless_mcp/models.py"
      provides: "Eligibility fields (warranty dates, prior-remediation, variant stock) added to the whitelist"
      contains: "warranty"
    - path: ".claude/hooks/authorized_offer.py"
      provides: "Real eligibility/evidence consumption replacing default_eligibility() stub"
      contains: "in_warranty"
  key_links:
    - from: ".claude/hooks/authorized_offer.py"
      to: "src/selless_mcp/server.py"
      via: "real eligibility fields replacing default_eligibility() at the RD-Q2 STUB"
      pattern: "in_warranty|warranty"
---

<objective>
DEFERRED (RD-Q2 + RD-Q3). Replace the PoC stub eligibility/evidence surfaces with real data sources WITHOUT
reshaping the guard. Wire real Selless eligibility fields (warranty purchase/delivery dates → THR-03/04
window computation, prior-remediation state, real variant stock, scoped product-info API) into
`authorize_offer`, and add real evidence validation (photo + shipping label) for Full_Refund / evidence-gated
paths. The swap happens only at the STUB markers placed in plan 04-06; the §0 decision logic and exit-code
contract are untouched.

Purpose: RD-Q2 and RD-Q3 explicitly defer the real data wiring to a later plan so the local PoC can demo the
guard structure now. This plan closes that deferral once the Selless eligibility surface + evidence intake
exist. It is NON-AUTONOMOUS: it depends on confirmation that the Selless API exposes the eligibility fields
and on a decision about how evidence is received/verified (the two REMAINING clarifications in RULES §4.2/§4.3).

Output: real eligibility/evidence wiring + fail-closed degradation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-AUTHORIZED-OFFER-RULES.md
@.planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md
@.claude/CLAUDE.md

<interfaces>
<!-- Swap points + data source. Extracted from plan 04-06 + Selless models. -->
From .claude/hooks/authorized_offer.py (04-06): default_eligibility() stub + STUB(RD-Q2)/STUB(RD-Q3) markers.
From src/selless_mcp/models.py: OrderDetail{status, created, delivery_orders[]{status_date_delivered}},
  CustomerInfo, PurchaseHistory, TicketHistory{customer_request, rootcause}. The field whitelist (D-04)
  governs what may be exposed — adding eligibility fields requires whitelist + audit entries.
Thresholds: THR-03 = 45 days from purchase; THR-04 = 14 days from delivery (warranty window computation).
RULES §4.2: confirm Selless exposes warranty dates, prior-remediation, variant stock, product-info API.
RULES §4.3: confirm how evidence (photos/labels) is received + verified (human-in-loop vs automated).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Confirm real eligibility + evidence surface readiness</name>
  <decision>How does the real eligibility + evidence surface arrive, and is it ready to wire?</decision>
  <context>
    RD-Q2/RD-Q3 deferred the real wiring. RULES §4.2/§4.3 list the two open clarifications: (a) whether
    Selless exposes warranty purchase/delivery dates, prior-remediation state, real variant stock, and the
    scoped product-info API; (b) how Full_Refund evidence (photo + shipping label) is received and verified.
    This plan cannot wire real data until these are confirmed. The guard structure (04-06/04-09) is already
    built to accept the real fields at the STUB points.
  </context>
  <options>
    <option id="selless-fields-ready">
      <name>Selless eligibility fields + evidence intake are available</name>
      <pros>Proceed to wire real fields; remove the optimistic stub; enable real fail-closed degradation</pros>
      <cons>Requires the Selless API surface + evidence intake to actually exist and be whitelisted/audited</cons>
    </option>
    <option id="partial-ready">
      <name>Some fields ready (e.g. warranty dates) but not evidence intake</name>
      <pros>Wire what exists; keep RD-Q3 evidence as accept-as-sufficient until intake is built</pros>
      <cons>Mixed state; must keep the unwired STUB markers clearly scoped</cons>
    </option>
    <option id="not-ready">
      <name>Not ready — keep PoC stub, defer again</name>
      <pros>No risk of half-wiring; PoC demo unaffected</pros>
      <cons>AUTO* rows remain stub-optimistic; D-27 gate cannot be fully exercised on real eligibility</cons>
    </option>
  </options>
  <resume-signal>Select: selless-fields-ready, partial-ready, or not-ready (and name which fields/evidence intake are confirmed available)</resume-signal>
  <action>PAUSE for the developer. Present the three options and the two RULES §4.2/§4.3 open clarifications (Selless eligibility field availability; evidence intake/verification model). Record the selection and the list of confirmed-available fields/evidence intake into the plan execution notes; this gates which fields Task 1 may wire. Do not proceed to Task 1 until a selection is given.</action>
  <verify><human-check>Developer has selected one of selless-fields-ready / partial-ready / not-ready and named the confirmed-available eligibility fields and evidence intake model.</human-check></verify>
  <done>A readiness decision is recorded naming exactly which eligibility fields and (if any) evidence intake are confirmed available for wiring in Task 1.</done>
</task>

<task type="auto">
  <name>Task 1: Wire real eligibility + evidence (only the fields confirmed available)</name>
  <files>src/selless_mcp/models.py, src/selless_mcp/server.py, .claude/hooks/authorized_offer.py</files>
  <read_first>
    - .claude/hooks/authorized_offer.py (the STUB(RD-Q2)/STUB(RD-Q3) markers + default_eligibility() to replace)
    - src/selless_mcp/models.py (OrderDetail/CustomerInfo/whitelist — add eligibility fields here)
    - src/selless_mcp/server.py (the MCP tool + audit/whitelist wiring to expose new fields)
    - src/selless_mcp/whitelist.py (D-04 field whitelist governing exposure)
    - .planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md (THR-03 45d purchase / THR-04 14d delivery window math)
  </read_first>
  <action>
    For each field CONFIRMED available in the checkpoint decision: add it to the Selless model + D-04 field
    whitelist + audit (warranty purchase/delivery dates, prior-remediation state, real variant stock; and the
    scoped product-info field set). In `authorized_offer.py`, replace the consumption at the STUB(RD-Q2)
    marker so eligibility is computed from real Selless fields: in_warranty = (now ≤ purchase_date + THR-03
    45d) OR (now ≤ delivery_date + THR-04 14d); prior_remediation from prior-remediation state;
    variant_in_stock from real stock. CRITICAL: when a required real field is absent/None, the eligibility
    function MUST return a fail-closed value that makes authorize_offer return UNAUTHORIZED (degrade AUTO* →
    escalate), NOT the optimistic stub default. For RD-Q3 (if evidence intake confirmed): replace the
    accept-as-sufficient branch with the real photo + shipping-label validation; absent/invalid evidence →
    UNAUTHORIZED. Keep default_eligibility() only for any field still deferred (partial-ready), clearly
    re-scoping its STUB marker. Do NOT change authorize_offer's decision order, signature, or exit-code
    contract — only the data source behind the marked points.
  </action>
  <acceptance_criteria>
    - Real eligibility fields whitelisted: `grep -ciE "warranty|prior.remediation|variant.stock" src/selless_mcp/whitelist.py` ≥ 1 (for confirmed fields).
    - authorize_offer no longer hard-codes the optimistic pass for wired fields: the STUB(RD-Q2) marker for any wired field is removed: `grep -c "STUB (RD-Q2)" .claude/hooks/authorized_offer.py` is strictly less than its count after plan 04-06 (or 0 if fully wired).
    - Fail-closed degradation proven: a missing warranty field yields UNAUTHORIZED (see test in Task 2).
    - authorize_offer signature unchanged: `grep -c "def authorize_offer(sub_type" .claude/hooks/authorized_offer.py` returns `1`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cs_team/test_real_eligibility.py -q</automated>
  </verify>
  <done>Confirmed real eligibility/evidence fields replace the stub at the STUB markers; missing real fields degrade AUTO* to escalate (fail-closed); the guard's decision logic and contract are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed degradation tests for real eligibility</name>
  <files>tests/cs_team/test_real_eligibility.py</files>
  <read_first>
    - tests/cs_team/test_real_eligibility.py (target — create)
    - tests/cs_team/test_authorized_offer.py (analog from 04-06 — extend its style)
    - .claude/hooks/authorized_offer.py (the wired module, after Task 1)
  </read_first>
  <behavior>
    - Real in-warranty order (purchase within 45d) + no prior remediation -> authorize_offer authorized for B7.
    - Order past 45d purchase AND past 14d delivery -> UNAUTHORIZED ineligible:warranty.
    - Missing/None warranty field -> UNAUTHORIZED (fail-closed degrade, NOT optimistic pass).
    - prior_remediation True (real) -> UNAUTHORIZED second_remediation.
    - (If RD-Q3 wired) absent evidence on Full_Refund -> UNAUTHORIZED; valid evidence -> authorized.
  </behavior>
  <action>
    Create `tests/cs_team/test_real_eligibility.py` extending the unit style of
    tests/cs_team/test_authorized_offer.py. Assert the real-field computation for in/out-of-warranty cases,
    and — the load-bearing regression — that a missing real eligibility field yields UNAUTHORIZED (fail-closed)
    rather than the old optimistic stub pass. Cover the RD-Q3 evidence cases only for the branches confirmed
    wired in the checkpoint. No LLM/network/DB; use direct function calls with constructed field dicts.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/cs_team/test_real_eligibility.py -q` exits 0.
    - Fail-closed-on-missing-field test present: `grep -ciE "missing|None|fail.closed" tests/cs_team/test_real_eligibility.py` ≥ 1.
    - Out-of-warranty UNAUTHORIZED asserted: `grep -c "ineligible" tests/cs_team/test_real_eligibility.py` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cs_team/test_real_eligibility.py -q</automated>
  </verify>
  <done>Tests prove real warranty-window computation, second-remediation blocking, and fail-closed degradation when a real eligibility field is missing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selless real fields → authorize_offer | real eligibility data crosses into the guard; absence must fail closed |
| customer-submitted evidence → Full_Refund authorization | evidence is attacker-influenceable; validation must be real, not accept-as-sufficient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Elevation | missing real eligibility field silently passing as optimistic stub | mitigate | fail-closed: absent field → UNAUTHORIZED; regression test asserts it |
| T-04-11-02 | Spoofing | fabricated/insufficient evidence authorizing a Full_Refund | mitigate | RD-Q3 real photo + shipping-label validation; absent/invalid → UNAUTHORIZED |
| T-04-11-03 | Information disclosure | new eligibility fields leaking beyond the D-04 whitelist | mitigate | add fields to whitelist + audit explicitly; redaction unchanged |
</threat_model>

<verification>
- `uv run pytest tests/cs_team/test_real_eligibility.py -q` green.
- Missing real field → UNAUTHORIZED (fail-closed), not optimistic pass.
- authorize_offer signature + decision order unchanged.
</verification>

<success_criteria>
Real Selless eligibility (warranty window THR-03/04, prior-remediation, variant stock) and — where wired —
real evidence validation replace the RD-Q2/RD-Q3 stubs at the marked swap points, with fail-closed
degradation to escalate when a real field is unavailable, and no change to the guard's decision logic.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-11-SUMMARY.md` when done
</output>
