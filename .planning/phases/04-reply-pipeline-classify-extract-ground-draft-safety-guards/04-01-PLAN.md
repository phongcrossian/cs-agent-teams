---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/hooks/pre_send_guard.py
  - .claude/hooks/escalation_gate.py
  - .claude/hooks/grounding_check.py
  - .claude/hooks/authorized_offer.py
  - .claude/settings.json
  - tests/test_hook_deletion.py
autonomous: true
requirements: [SAFE-04, SAFE-03]
must_haves:
  truths:
    - "The four retired guard hooks (pre_send_guard, escalation_gate, grounding_check, authorized_offer) no longer exist on disk"
    - "settings.json no longer wires any of the four deleted hooks; the submit_reply PreToolUse chain is gone"
    - "injection_screen.py (D-14) and pii_redact.py (D-04) remain present and wired — the surviving safety floor"
    - "No code in .claude/ or scripts/ imports the deleted hook modules"
  artifacts:
    - path: ".claude/settings.json"
      provides: "MCP + hook wiring with ONLY injection_screen (PreToolUse) + pii_redact (PostToolUse); no deleted-hook references"
      contains: "injection_screen"
    - path: "tests/test_hook_deletion.py"
      provides: "Test asserting the 4 hook files are absent and settings.json references neither them nor the deleted hooks"
      min_lines: 30
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/injection_screen.py"
      via: "PreToolUse / UserPromptSubmit command binding (surviving)"
      pattern: "injection_screen"
    - from: ".claude/settings.json"
      to: ".claude/hooks/pii_redact.py"
      via: "PostToolUse command binding (surviving)"
      pattern: "pii_redact"
---

<objective>
DELETE the four retired guard hooks and all their settings.json wiring (D-32), keeping ONLY `injection_screen.py` (D-14) and `pii_redact.py` (D-04) active as the remaining safety floor.

Purpose: D-32 retires the fail-closed output guard for the always-draft PoC. The hooks `pre_send_guard.py`, `escalation_gate.py`, `grounding_check.py`, and `authorized_offer.py` must be removed entirely — both the code files and their `PreToolUse@submit_reply`, `PostToolUse`, and `SubagentStop` bindings in `.claude/settings.json`. The drafter's `submit_reply` chokepoint still exists but is no longer blocked by these guards.

⚠️ DEFERRED BEFORE-LIVE BLOCKER: deleting the guard lets the team draft (and, once live, auto-send) unauthorized refund/credit/legal commitments at 23k/week. This is an accepted PoC decision gated by **DRY_RUN-only**. A guard MUST be re-authored before any live (non-DRY_RUN) send — that re-authoring is OUT OF SCOPE for this phase (tracked in 04-CONTEXT `<deferred>`).

NOTE — `scripts/test_tickets_run.py` is an **untracked developer helper** that currently reads the `CS_RUN_ID` env and `importlib`-loads the deleted `authorized_offer.py`. It is NOT in this plan's `files_modified` (it is not tracked) and Task 1's straggler-grep across `scripts/` will flag it. Do NOT silently break it here: its always-draft cleanup is folded into **04-03 Task 1** (the demo-runner rework). In THIS plan, if the straggler grep surfaces it, leave the file as-is and record it in the SUMMARY as a deferred-to-04-03 untracked helper.

Output: a `.claude/hooks/` directory with only injection_screen + pii_redact, a slimmed settings.json, and a deletion-assertion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.claude/CLAUDE.md

<interfaces>
<!-- Current .claude/settings.json hook wiring (the thing being slimmed): -->
<!--   PreToolUse[matcher=submit_reply]: grounding_check.py, pre_send_guard.py, escalation_gate.py   ← DELETE all 3 -->
<!--   UserPromptSubmit[matcher=*]: injection_screen.py                                                ← KEEP -->
<!--   PostToolUse[matcher=*]: escalation_gate.py, pii_redact.py                                       ← DELETE escalation_gate, KEEP pii_redact -->
<!--   SubagentStop[matcher=*]: escalation_gate.py                                                     ← DELETE -->
<!--   env: SEND_MODE=dry_run, CS_RUN_ID=${CS_RUN_ID}                                                  ← CS_RUN_ID was escalation_gate's stateful veto; remove if only escalation_gate read it -->
<!-- mcpServers block (KnowledgeMCP/SellessMCP/ReplyMCP) is handled in 04-02 (KnowledgeMCP removal) — leave it alone here. -->
<!-- Surviving hook contracts (do not change): -->
<!--   injection_screen.py main(): reads stdin {"prompt": ...}, exit 1 + {"action":"escalate"} on injection, exit 0 clean -->
<!--   pii_redact.py main(): reads stdin, redacts body/draft fields, prints redacted JSON, always exit 0 (never blocks) -->
<!-- Untracked helper (do NOT edit here): scripts/test_tickets_run.py reads CS_RUN_ID + importlib-loads authorized_offer.py — cleanup folded into 04-03 Task 1. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the four retired guard hook files</name>
  <files>.claude/hooks/pre_send_guard.py, .claude/hooks/escalation_gate.py, .claude/hooks/grounding_check.py, .claude/hooks/authorized_offer.py</files>
  <read_first>
    - .claude/hooks/pre_send_guard.py (confirm it is the D-26 authorized-offer guard being retired)
    - .claude/hooks/escalation_gate.py (confirm the any-signal gate + operational_action trigger being retired)
    - .claude/hooks/grounding_check.py (confirm the [KB-N]/[SEL-N] citation enforcement being retired)
    - .claude/hooks/authorized_offer.py (confirm the §0 authorized/unauthorized decision module being retired)
    - .claude/CLAUDE.md (the D-32 banner confirming these four are deleted and injection_screen + pii_redact stay)
  </read_first>
  <action>
    Delete the four files `pre_send_guard.py`, `escalation_gate.py`, `grounding_check.py`, `authorized_offer.py` from `.claude/hooks/` (use `git rm`). Do NOT touch `.claude/hooks/injection_screen.py`, `.claude/hooks/pii_redact.py`, or `.claude/hooks/__init__.py`. After deletion, grep the `.claude/` tree and `scripts/` for any remaining `import` or string reference to the four deleted module names. Remove/repair stragglers in TRACKED code under `.claude/hooks/` (none should remain — the agent .md / skill references are reworked in 04-02/04-05). The ONE expected straggler is the untracked `scripts/test_tickets_run.py` (CS_RUN_ID + authorized_offer importlib) — DO NOT edit it here; record it in the SUMMARY as deferred to 04-03 Task 1. Also remove the `__pycache__` entries for the deleted modules so stale bytecode is not importable.
  </action>
  <verify>
    <automated>test ! -f .claude/hooks/pre_send_guard.py && test ! -f .claude/hooks/escalation_gate.py && test ! -f .claude/hooks/grounding_check.py && test ! -f .claude/hooks/authorized_offer.py && echo DELETED</automated>
  </verify>
  <acceptance_criteria>
    - All four files absent: the verify command prints `DELETED`.
    - `.claude/hooks/injection_screen.py` and `.claude/hooks/pii_redact.py` still exist (`test -f` both → 0).
    - `grep -rn "import pre_send_guard\|import escalation_gate\|import grounding_check\|import authorized_offer\|from .*pre_send_guard\|from .*escalation_gate\|from .*grounding_check\|from .*authorized_offer" .claude/hooks/ 2>/dev/null` returns nothing (TRACKED hook code clean; the untracked scripts/test_tickets_run.py straggler is deferred to 04-03 and noted in the SUMMARY).
  </acceptance_criteria>
  <done>The four retired guard hooks are removed; injection_screen and pii_redact remain; no tracked Python module under .claude/hooks/ imports the deleted hooks; the untracked scripts/test_tickets_run.py straggler is recorded for 04-03.</done>
</task>

<task type="auto">
  <name>Task 2: Remove deleted-hook wiring from settings.json + assert deletion</name>
  <files>.claude/settings.json, tests/test_hook_deletion.py</files>
  <read_first>
    - .claude/settings.json (current hook bindings — the PreToolUse[submit_reply] chain, PostToolUse escalation_gate, SubagentStop escalation_gate, and CS_RUN_ID env)
    - .claude/hooks/injection_screen.py (confirm its event binding — UserPromptSubmit — so the surviving wiring is correct)
    - .claude/hooks/pii_redact.py (confirm PostToolUse, never-blocks contract)
  </read_first>
  <action>
    Edit `.claude/settings.json`: remove the entire `PreToolUse` entry whose matcher is `submit_reply` (it bound grounding_check + pre_send_guard + escalation_gate). Remove the `SubagentStop` block (it bound only escalation_gate). In the `PostToolUse` block, remove the `escalation_gate.py` command but KEEP the `pii_redact.py` command. KEEP the `UserPromptSubmit` block binding `injection_screen.py` unchanged. Remove the `CS_RUN_ID` env key from the `env` block (it existed only as escalation_gate's stateful veto state-file pointer — confirm via grep that nothing surviving reads it before removing; keep `SEND_MODE=dry_run`). Leave the `mcpServers` block untouched (KnowledgeMCP removal is 04-02). The resulting hooks section must reference ONLY `injection_screen.py` and `pii_redact.py`. Then create `tests/test_hook_deletion.py` asserting (a) the four hook files do not exist, (b) `settings.json` parses as JSON and its serialized text contains neither the four deleted filenames nor `submit_reply` in any PreToolUse matcher, and (c) it still references `injection_screen.py` and `pii_redact.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_hook_deletion.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; d=json.load(open('.claude/settings.json')); s=json.dumps(d); assert 'pre_send_guard' not in s and 'escalation_gate' not in s and 'grounding_check' not in s and 'authorized_offer' not in s, s"` exits 0.
    - `python -c "import json; d=json.load(open('.claude/settings.json')); s=json.dumps(d); assert 'injection_screen' in s and 'pii_redact' in s"` exits 0.
    - `grep -c "CS_RUN_ID" .claude/settings.json` returns 0.
    - `uv run pytest tests/test_hook_deletion.py -x` passes.
  </acceptance_criteria>
  <done>settings.json wires only the surviving injection_screen + pii_redact hooks; the deleted-hook chain and CS_RUN_ID state are gone; a test asserts the deletion contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body → agent prompts | Untrusted attacker-controlled email body crosses into the LLM — STILL screened by injection_screen.py (D-14) |
| log/trace sink ← payloads | Customer PII must be redacted before any sink — STILL handled by pii_redact.py (D-04) |
| drafter → submit_reply chokepoint | The output guard that bounded commitments is REMOVED for the PoC — accepted high-severity risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Elevation of Privilege | drafter emits unauthorized refund/credit/legal commitment via submit_reply (guard deleted, D-32) | accept | Documented, accepted PoC trade-off. Bounded by DRY_RUN-only (submit_reply never posts to Freshdesk). Before-live blocker tracked in 04-CONTEXT `<deferred>`: re-author a guard before any non-DRY_RUN send. ⚠ HIGH severity, accepted only because no live send is possible in this phase |
| T-04-01-02 | Tampering | email body prompt-injection now that 3 PreToolUse guards are gone | mitigate | injection_screen.py (D-14) RETAINED and remains wired on UserPromptSubmit; ticket body still delimited as untrusted in agent prompts (enforced in 04-02) |
| T-04-01-03 | Information Disclosure | PII leaking to logs/traces after guard removal | mitigate | pii_redact.py (D-04) RETAINED and remains wired PostToolUse; submit_reply also redacts at its persistence boundary (src/reply_mcp/server.py) |
| T-04-01-SC | Tampering | settings.json hook wiring edited | mitigate | Deletion-assertion test (tests/test_hook_deletion.py) gates that only the two surviving hooks remain wired; JSON validity asserted |
</threat_model>

<verification>
- Four hook files deleted; injection_screen + pii_redact remain.
- settings.json references only the two surviving hooks; CS_RUN_ID env removed; JSON valid.
- No tracked Python import of the deleted modules under .claude/hooks/; the untracked scripts/test_tickets_run.py straggler is deferred to 04-03 and recorded in the SUMMARY.
- Deletion-assertion test passes.
</verification>

<success_criteria>
The retired guard hooks are gone (code + wiring) per D-32; the surviving safety floor (injection screening + PII redaction) is intact and still wired; the accepted before-live guard trade-off is documented as DRY_RUN-gated.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-01-SUMMARY.md` when done.
</output>
