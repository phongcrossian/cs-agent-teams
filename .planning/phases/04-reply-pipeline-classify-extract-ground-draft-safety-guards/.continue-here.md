---
context: phase
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
work_item: always-draft rework DONE + AI-vs-CS validation loop (real team, 30 PROD tickets)
status: in_progress
last_updated: 2026-06-05
---

# Continue Here — Phase 4 always-draft rework + validation loop

## State (2026-06-05)

**Rework DONE.** The always-draft pivot (D-29..D-34) is fully landed in code: 6 plans
04-00..04-05 executed, `pytest -q` = **308 passed / 9 skipped / 0 failed**. The `.claude/`
team is always-draft: 4 guard hooks deleted, KnowledgeMCP gone, file-store grounding (D-31),
`action=draft`+advisory hint (D-33), flow-aware Selless fallback (D-34). Only
`injection_screen` (D-14) + `pii_redact` (D-04) hooks remain.

**Validation harness works (D-35).** `scripts/test_tickets_run.py collect` drives the REAL
`.claude/` team via `claude --print` on 30 PROD tickets, read-only FD + Selless
(`api.selless.com`), **DRY_RUN only (D-39)** — never posts to Freshdesk. Now 2-pass:
PASS-1 Selless-aware classify → PASS-2 draft with the real template snapshot bodies injected
(`_load_templates_for_subtype`) + the allowed-code set. Robust JSON parse (fence/prose tolerant).
Writes `test-tickets.xlsx` (AI vs CS side-by-side, extended props D-37, per-ticket checker
reason+fix D-40).

**Convergence (AI vs CS, CS = absolute gold D-36; binding = template+reply match D-38):**

| iter | error | minor | ok | key change |
|------|-------|-------|----|-----------|
| 1 | 29 | 1 | 0 | baseline (template free-pick, no Selless branch, fit→Return) |
| 2 | 21 | 4 | 5 | constrain template family + Selless-first branch + fit→Replace rule |
| 3 | 17 | 5 | 8 | 2-pass: inject real template bodies → replies match CS closely |

## Remaining error backlog (catalogued per-ticket in test-tickets.xlsx "Checker" rows)

Prompt-fixable (no new data):
- **Wrong-store catalogue guard** (7731192, 7731509) — deflect (G1/G12) when the customer's
  product is outside the store catalogue instead of answering a random Selless order. Flagged
  all 3 iters; needs a catalogue list injected.
- **E-code do_status mapping** (change_request): NEW→E1, TA→E2, TO→E2 then E3; address-already-
  updated→E1. AI conflates TA/TO and over-uses E3.
- **E4 measurement-first gate** for Change_Product_Variant on sizing products.
- **F23 goodwill = next-purchase discount** not current-order refund (7731616).

Needs domain data not available at inference (legitimately hard / deferred):
- **Prior-remediation gate** (7731229) — needs DO-status CLOSED + ticket-history of a prior
  replacement (Selless eligibility wiring — deferred plan).
- **Sizing-table accuracy** (7731282, 7731348) — needs the authoritative size chart.
- **G6/G7 delay-lock** — checker keys on `cs_props.rootcause_type`, which is the CS LABEL (not
  available at inference); AI must infer delay from thread/Selless. Treat as judgment, not a
  cheap rule.
- **Review vs delivery classification + discount tier** (7731235) — subtle.

## How to run the next iteration
```
.venv/bin/python scripts/test_tickets_run.py collect --per-cat 10   # ~13 min, 60 claude calls
# rebuild .cs-compare/<cat>.json (see git log for the inline python), spawn 3 general-purpose
# checker agents (one per category) → .cs-compare/<cat>-findings.json, merge rec['checker'],
# then: .venv/bin/python scripts/test_tickets_run.py xlsx
```

## Notes / gotchas
- macOS has no `timeout`; don't use it in probes.
- The headless model returns ```json-fenced output → the robust `_parse_combined` handles it.
- Non-determinism: a couple tickets flip sub-type between runs (7731117/7731235) — expected.
- `draft()` standalone path is DEPRECATED for validation (D-35) — `collect()` is the source of truth.
- ⚠️ BEFORE any live (non-DRY_RUN) send: re-author an output guard (D-32 deleted them).

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Free-pick template | Loading templates by broad category glob + letting the LLM choose freely → invented/cross-family codes. | blocking | Deterministic sub-type→allowed-codes map injected into the prompt (PASS-2) + `_SUBTYPE_TEMPLATES`; the model MUST pick a verbatim code from the allowed set. Do NOT revert to category-glob free-pick. |
| Trust customer claim over order record | AI believed "wrong color" claim; Selless showed the customer ordered that variant. | advisory | PASS-1 classify is Selless-aware; verify claimed variant vs `selless_order.deliveries` before assigning Replace/defect. |
