---
context: phase
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
task: 8
total_tasks: 9
status: in_progress
last_updated: 2026-06-03T09:33:51.144Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual work this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Block-all commitment guard is wrong for this business** — Phase-4 `pre_send_guard.py`
  (D-13/SAFE-04) blocks ALL refund/credit/replace/charge language → escalate. But the real, correct CS
  flow for the highest-volume category (product complaint) IS to make **policy-bounded templated offers**
  (refund/discount/replacement). Verified live: ticket 7732073's real reply == template **B7** verbatim
  ("50% refund + 40% VIP discount"). Structural mitigation: the guard must become **template + threshold
  aware** (allow offers matching an approved template AND within the policy threshold for the verified
  order's eligibility; block only out-of-template / over-threshold / ineligible / fabricated). Do NOT
  resume by "tightening" the block-all guard — that is the anti-pattern.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Block-all commitment guard | D-13 blocks every refund/discount/replace word → escalates the entire complaint/cancellation/shipping volume; fails every correct templated reply | blocking | Redefine guard as template+threshold-aware; redefine Phase-5 hard gate to "0 UNAUTHORIZED commitments" (D-27), not "0 refund words" |
| Running tooling via bare `uv` in background bash | `uv` is not on PATH in the non-interactive background shell → fetch produced no output despite exit 0 | advisory | Use `.venv/bin/python scripts/...` (full path) instead of `uv run python ...` for background tasks |

<current_state>
Paused mid-investigation. Phase-5 discuss-phase is COMPLETE and committed (05-CONTEXT.md, commit f7dd671),
including the new decisions D-25..D-28 that arose from this investigation. We had just decided to **reopen
Phase 4** to make the commitment guard template/threshold-aware, and to derive the correct rules from the
two real Freshdesk exports. The reopen edits to ROADMAP/STATE were NOT yet made. The reply-fetch script
was written but its background run FAILED (uv not on PATH) — no data fetched yet.
</current_state>

<completed_work>

- Phase-5 discuss-phase: 05-CONTEXT.md + 05-DISCUSSION-LOG.md written & committed (f7dd671, 33e0a02). STATE recorded (065ccd1).
- Live Freshdesk verification (read-only, via .env.prd):
  - Agent team has NO property-update capability (classifier/extractor produce structured data internally; final verdict = {action, body, citations} / escalate; reply_mcp = submit_reply only).
  - Ticket 7732073's first public reply == template B7 verbatim → confirmed the "correct flow" uses templated offers.
- `scripts/fetch_golden_replies.py` written (fetches first public reply per ticket, joins with CSV properties).
</completed_work>

<remaining_work>

- Task 5: Reopen Phase 4 — ROADMAP.md `[x]`→`[ ]` for Phase 4 + progress-table row → "Reopened"; revise Phase-4 success criterion #4 (it currently says "block commitment language … regardless of category", which conflicts with templated offers); set STATE phase back to 4.
- Task 6: Re-run `scripts/fetch_golden_replies.py` with `.venv/bin/python` over BOTH exports (60000264798… and 60000264802…). Output → /tmp/cs_golden_analysis.jsonl.
- Task 7: Analyze all ~63 tickets — join CS-agent properties (Customer_Request/Feedback_Issue/Rootcause/Section_Flow/Order/Priority) with the first public reply; classify each reply to its template code (A1–A9 / B1–B13 / C1 / cancellation / shipping) and extract the offer (refund %, discount %, replacement, evidence-required) + eligibility signals (warranty window, "offered 50% before?").
- Task 8: Derive the authorized-offer RULE SET: case taxonomy → correct template → authorized offer/threshold → draft-vs-escalate boundary. Write to Phase 4 dir (e.g. 04-AUTHORIZED-OFFER-RULES.md).
- Task 9: Replan Phase 4 (template/threshold-aware pre_send_guard + drafter template selection grounded in Selless eligibility + POLICY-THRESHOLD-INDEX). Then redefine the Phase-5 gate per 05-CONTEXT D-27.
</remaining_work>

<decisions_made>

- Reply scoring is reference-aware but NOT binding similarity to past replies (Phase 5).
- Two-track eval: (A) classification vs CS-agent properties, (B) first public reply quality (Phase 5).
- Reopen Phase 4 to make the commitment guard template + threshold aware (D-26); redefine the hard gate to "0 UNAUTHORIZED commitments" (D-27).
</decisions_made>

<blockers>
- Background fetch failed: `uv` not on PATH in bg shell → use `.venv/bin/python scripts/fetch_golden_replies.py <csv>`.
- GSD has no native phase-reopen command → hand-edit ROADMAP/STATE (or insert phase 4.1). User asked to reopen Phase 4.
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-offline-evaluation-harness-the-gate/05-CONTEXT.md` — D-25..D-28 (authorized-offer guard + gate redefinition) — the spine of this work.
2. `.planning/phases/01-knowledge-survey-conflict-inventory/snapshots/product complaint-within guarantee-template1.md` and `…-template2.md` — the A/B template offers (incl. B7).
3. `.planning/phases/01-knowledge-survey-conflict-inventory/POLICY-THRESHOLD-INDEX.md` — THR-03/04 (warranty), THR-05/06/07/08 (discount/refund caps) — the bounds the guard must enforce.
4. `.planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP-templates.md` — code→template wiring.
5. `.claude/hooks/pre_send_guard.py` + `.claude/agents/drafter.md` — what gets reworked.

## Infrastructure State
- `.env.prd` present (Freshdesk shophelp.freshdesk.com API key + DB creds). Read-only API use only. NOT committed (secrets).
- Two export CSVs in Phase-1 snapshots/ (UNTRACKED, customer PII) — do NOT commit.
- /tmp/cs_golden_analysis.jsonl does NOT exist yet (fetch failed). Re-run fetch on resume.

## Pre-Execution Critique Required
- Before reworking the guard, derive the rule set from the data first (Task 7→8), then critique: does the
  proposed "authorized offer" boundary cover every template/threshold without opening an unauthorized-commitment hole?

<context>
This started as a Phase-5 discuss-phase and uncovered a Phase-4 design flaw. The whole point of automation
here is the high-volume templated complaint flow — which necessarily contains monetary offers. The fix is
to ground the guard in the approved templates + POLICY-THRESHOLD-INDEX and the verified order's eligibility,
not to block all money words. Use the real exports as the source of truth for what "correct" looks like.
</context>

<next_action>
DONE since pause: Phase 4 reopened (ROADMAP + STATE); replies fetched for all 3 categorized samples
(312 tickets); analysis run; **04-AUTHORIZED-OFFER-RULES.md written** (the data-derived rule set).
Start with: review 04-AUTHORIZED-OFFER-RULES.md §4 OPEN questions with the user, then replan Phase 4
(template/threshold-aware pre_send_guard + classifier sub-type emission + drafter template selection +
escalation_gate operational-action trigger). Then redefine the Phase-5 gate per 05-CONTEXT D-27.
Re-fetch note: `os.environ.setdefault` shadowed .env.prd → 404s; fixed to override. Use `.venv/bin/python`.
</next_action>
