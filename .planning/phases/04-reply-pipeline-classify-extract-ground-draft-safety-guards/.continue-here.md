---
context: phase
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
work_item: live-test harness (scripts/test_tickets_run.py) — AI-vs-CS on 30 real tickets
status: in_progress
last_updated: 2026-06-04T09:20:07.103Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Selless host is `api.selless.com`, NOT `api.selless.dev`.** The `.dev` host
      returns 200 with EMPTY data for every lookup; `.com` has the real orders. The runner is
      fixed, but `src/config.py:123` `selless_api_base_url` still defaults to `.dev` — so the
      Selless **MCP** path (used by the real agent-team) would also get empty data until reconciled.
      Mitigation: change the config default to `.com` before relying on the MCP grounding path.
- [ ] CONSTRAINT: **Resolve orders by the human ORDER CODE, not the FD-ticket→DO path.**
      `GET /{fd_ticket_id}/ticket-do` returns `{"do_ids": []}` for these tickets. The working path
      is `GET /po/search?param=<order_code>` → `id` → `GET /po/{id}`. Empty `/po/search` = a genuine
      "no order" signal (→ verify/clarify), not an error.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Free-pick template | Loading templates by broad category glob + letting the LLM choose freely → invented `Custom-` names and cross-family codes (F8 cancellation for a Change_Product_Variant). | blocking | `_SUBTYPE_TEMPLATES` deterministic map (sub-type → allowed codes + only the right snapshot files); prompt forces one REAL allowed code; runner validates + flags BAD-CODE. Do NOT revert to category-glob loading. |
| "Data provenance mismatch" misdiagnosis | Concluded the 30 tickets' orders simply don't exist in Selless. WRONG — they exist on `.com`. | advisory | Always test the actual order code via `/po/search` on `api.selless.com` before concluding "no data". |
| Over-offer / asserted action | Without live order data the drafter guessed order state, picked high-remediation codes, offered 100% refund, and claimed "we've changed/refunded…". | advisory | Grounding-discipline rules in the prompt + (TODO) a deterministic guard that strips assert-language/amounts and caps offers per template. |

<current_state>
Paused mid **iteration-4**: re-running all 30 tickets through the drafter WITH live Selless
grounding now wired (host fixed to `.com`, resolve-by-order-code). Background task `bstsjftm2`
was at 1/30 (selless=Y confirmed — real order status/tracking/variant now reaching the drafter).
Two commits landed: `572d361` (deterministic template selection + discipline) and `30dfc23`
(live Selless grounding). xlsx + .test-tickets-data.jsonl currently hold the iter-3 result.
</current_state>

<completed_work>
- Diagnosed why template was wrong (category-glob + free LLM pick).
- `572d361`: deterministic `_SUBTYPE_TEMPLATES` (sub-type→allowed codes+files), constrain to real code, validate; xlsx `template_valid` + Checker block.
- Grounding discipline in drafter prompt (no-assert, conservative-on-unknown, no-order→verify D-34, fit→B-codes).
- 3 iterations + 3 checker agents (per category) comparing AI vs CS: errors **11→7→4**, ok **5→13**, 30/30 valid template family.
- `30dfc23`: fixed Selless "no data" — wrong host (`.dev`→`.com`) + resolve by order code (`/po/search`→`/po/{id}`); live grounding verified (TA / DELIVERED + tracking + variant).
</completed_work>

<remaining_work>
- Let `bstsjftm2` (iter-4 drafter) finish (~10 min, all 30 with live grounding).
- Rebuild `.cs-compare/<cat>.json`, re-run the 3 checker agents, measure error delta vs iter-3 (4/13/13).
- Merge findings → rebuild `test-tickets.xlsx`.
- Decide/implement deterministic guard for template-inherent assertions/amounts (e.g. 7731646 G12).
- Reconcile `src/config.py` default to `.com`; final AI-vs-CS report.
</remaining_work>

<decisions_made>
- Deterministic sub-type→template shortlist, LLM picks one real code (user-chosen).
- Selless grounding by order code on `api.selless.com`.
- Drafter never asserts executed mutation/amount; conservative on unknown; no-order→verify.
</decisions_made>

<blockers>
- `src/config.py:123` still defaults Selless base to `.dev` (MCP path would get empty data). Runner unaffected (uses its own `.com` base). Reconcile before MCP grounding is used live.
</blockers>

## Required Reading (in order)
1. `scripts/test_tickets_run.py` — `_SUBTYPE_TEMPLATES`, `_build_draft_prompt` (discipline rules), `fetch_selless_order` (live grounding), `draft()`, `build_xlsx()`.
2. `.planning/phases/01-knowledge-survey-conflict-inventory/CODE-MAP-templates.md` — the code→template authority the checker uses.
3. `.cs-compare/*-findings.json` — latest per-ticket AI-vs-CS verdicts (iter-3; will refresh after iter-4).

## Infrastructure State
- Background task `bstsjftm2`: iter-4 drafter, ~10 min, writes `.test-tickets-data.jsonl`. Check its output file under the session tasks dir.
- Drafter invokes `claude --print --output-format json` as a subprocess per ticket.
- venv python: `.venv/bin/python` (has openpyxl, presidio, httpx; NOT `anthropic`). `uv` not on PATH.
- Selless reachable from this machine (200 OK on `.com`).

<context>
Classification (Customer_Request) already matches CS on all 30 — the entire effort is template
choice + offer/assert grounding. Iteration loop = edit prompt/map → re-run drafter (per category)
→ rebuild .cs-compare inputs → spawn 3 general-purpose checker agents → merge `rec['checker']` →
rebuild xlsx → read severity counts. The remaining residual errors before live grounding were
mostly "no live order data" (now fixed) + template-inherent assert-language (needs a guard).
</context>

<next_action>
Start with: read the tail of background task `bstsjftm2`'s output; once it prints "ALL DONE iter4"
with "0 bad-template-code", rebuild `.cs-compare/{change_request,complaint,inquiry}.json` from
`.test-tickets-data.jsonl`, then spawn the 3 checker agents (general-purpose) to re-score AI-vs-CS
WITH live grounding, merge findings into `rec['checker']`, rebuild `test-tickets.xlsx`, and report
the new error/minor/ok counts vs iter-3 (4/13/13).
</next_action>
