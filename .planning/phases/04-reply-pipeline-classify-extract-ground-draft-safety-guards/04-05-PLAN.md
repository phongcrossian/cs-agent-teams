---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 05
type: execute
wave: 6
depends_on: ["04-00", "04-01", "04-02", "04-03", "04-04"]
files_modified:
  - .claude/hooks/escalation_gate.py
  - .claude/settings.json
  - scripts/cs_team_demo.py
  - tests/cs_team/test_hooks_subprocess.py
autonomous: true
gap_closure: true
requirements: [SAFE-03, SAFE-04, REP-03]

must_haves:
  truths:
    - "escalation_gate at PreToolUse@submit_reply reads accumulated risk from an out-of-band per-run state file keyed by CS_RUN_ID, and BLOCKS (exit 2) if any signal is set"
    - "escalation_gate fails closed (exit 2) at submit_reply when no state file is found for the active run or CS_RUN_ID is unset"
    - "PostToolUse/SubagentStop escalation_gate invocations WRITE accumulated risk signals to the per-run state file (additive, OR-combined)"
    - "The ticket body is deterministically injection-screened on the REAL deployed path before any subagent sees it — a mandatory, non-bypassable runner pre-step (not just the demo convenience), and the screen is also bound on the subagent task-delivery event"
    - "SUBPROCESS tests prove each PreToolUse hook returns exit 2 on a violation payload and 0 on a clean payload, matching the deployed enforcement surface"
    - "The dead raw_body_for_check variable + misleading comment in cs_team_demo.py is removed"
  artifacts:
    - path: ".claude/hooks/escalation_gate.py"
      provides: "Stateful final-risk veto: reads per-run state file at submit_reply; writes accumulated signals at PostToolUse/SubagentStop; fail-closed default"
      contains: "CS_RUN_ID"
    - path: ".claude/settings.json"
      provides: "CS_RUN_ID env + injection screen bound on subagent task delivery; existing §4a bindings preserved"
      contains: "CS_RUN_ID"
    - path: "scripts/cs_team_demo.py"
      provides: "Runner that sets CS_RUN_ID per run and mandatorily pre-screens the body before any CLI/subagent invocation; dead variable removed"
      contains: "CS_RUN_ID"
    - path: "tests/cs_team/test_hooks_subprocess.py"
      provides: "Subprocess exit-code proofs for all PreToolUse hooks + escalation_gate state-file fail-closed"
      contains: "returncode"
  key_links:
    - from: ".claude/hooks/escalation_gate.py PreToolUse@submit_reply"
      to: "per-run state file keyed by CS_RUN_ID"
      via: "read accumulated signals; fail-closed if missing → exit 2"
      pattern: "CS_RUN_ID"
    - from: ".claude/hooks/escalation_gate.py PostToolUse/SubagentStop"
      to: "per-run state file keyed by CS_RUN_ID"
      via: "OR-merge derived signals into the file"
      pattern: "CS_RUN_ID"
    - from: "tests/cs_team/test_hooks_subprocess.py"
      to: ".claude/hooks/*.py via subprocess"
      via: "pipe violation/clean payload to python <hook>.py; assert returncode"
      pattern: "subprocess.*returncode"
---

<objective>
Đóng hai gap còn lại của Phase 4 và CHỨNG MINH enforcement đã deploy bằng subprocess test.

Nội dung:
- CR-02: làm `escalation_gate.py` "final-risk veto" có trạng thái — persist signal ra ngoài tiến trình. Hook stateless của Claude Code không chia sẻ bộ nhớ giữa các lần gọi; nên PostToolUse/SubagentStop GHI signal tích lũy vào 1 state file per-run (keyed bằng env `CS_RUN_ID`), còn PreToolUse@submit_reply ĐỌC file đó trước khi cho phép tool. Fail-closed: không có file (hoặc thiếu CS_RUN_ID) → escalate (exit 2).
- CR-04 (phần deploy): bắt buộc pre-screen injection trên ĐƯỜNG THẬT — một bước deterministic không-thể-bỏ-qua trong `scripts/cs_team_demo.py` trước MỌI lần gọi CLI/subagent, và thêm binding cho injection_screen trên sự kiện giao task cho subagent trong settings.json. Đồng thời dọn biến chết `raw_body_for_check` + comment gây hiểu nhầm (~dòng 469-471).
- TEST GAP: thêm `tests/cs_team/test_hooks_subprocess.py` chạy thật `uv run python .claude/hooks/<hook>.py` qua subprocess, assert `returncode == 2` trên payload vi phạm và `== 0` trên payload sạch — khớp mặt enforcement đã deploy (không phải in-process như Layer (b)).

Purpose: Khôi phục SAFE-03 (CR-02) và phần deploy của SAFE-04 (CR-04); khóa lại bằng test có thật để re-verify đạt 4/4.
Output: escalation_gate có state, settings.json wiring, runner pre-step bắt buộc + dọn dead-var, và bộ subprocess test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-VERIFICATION.md

<interfaces>
<!-- Exact contracts the executor edits/uses. Extracted from current code. -->

escalation_gate.py current (the file being made stateful):
  should_escalate(signals: dict) -> (bool, str)   # _SIGNAL_ORDER OR-gate, first match wins. KEEP.
  _SIGNAL_ORDER keys: low_confidence, high_risk_category, conflict, stale_only, missing_key.
  _derive_signals(payload) -> dict   # scans signals/risk_signals/escalation_signals + nested tool_result + top-level. KEEP for WRITE side.
  main() lines ~91-128: detects is_final_veto via tool_name=="submit_reply" or hook_event_name=="PreToolUse";
    exits sys.exit(2 if is_final_veto else 1). This exit logic is correct — extend it with state read/write.

State-file design (NEW — define here, keep consistent with src/config.py + DRY_RUN/local-PoC conventions):
  - Location: a per-run temp file, e.g. ${TMPDIR or /tmp}/cs_run_state/<CS_RUN_ID>.json  (gitignored; ephemeral).
    Use Python tempfile.gettempdir() — do NOT hardcode /tmp. Create parent dir if absent.
  - Key: env var CS_RUN_ID (string). The runner sets it once per ticket run; hooks read os.environ["CS_RUN_ID"].
  - Schema: {"signals": {"low_confidence": false, "high_risk_category": false, "conflict": false,
    "stale_only": false, "missing_key": false}, "updated_at": "<iso8601>"}.
  - Lifecycle: created/updated by PostToolUse + SubagentStop invocations (OR-merge True signals — additive, never clears a True per D-08); read by PreToolUse@submit_reply; the RUNNER deletes it at run end.
  - Concurrency: keyed per CS_RUN_ID so parallel ticket runs do not collide. Read-modify-write should tolerate a missing/partial file (treat missing as empty signals on the WRITE path).
  - Fail-closed READ rule at submit_reply: if CS_RUN_ID is unset OR the state file does not exist OR is unparseable → escalate (exit 2). This is the security default — no state means "we cannot prove the ticket is low-risk".

settings.json current bindings (preserve order; ADD only):
  PreToolUse@submit_reply: grounding_check -> pre_send_guard -> escalation_gate  (KEEP order)
  UserPromptSubmit: injection_screen  (KEEP)
  PostToolUse(*): escalation_gate, pii_redact  (KEEP — escalation_gate here is the WRITE side)
  SubagentStop(*): escalation_gate  (KEEP — WRITE side)
  env: current block is {"SEND_MODE":"dry_run"} → ADD the literal key "CS_RUN_ID": "${CS_RUN_ID}" alongside SEND_MODE.
    Claude Code hook subprocesses ONLY see env vars declared in settings.json `env` (or inherited from the launching
    shell); without this line the stateful veto is DEAD on the deployed path. The runner (Task 2) exports CS_RUN_ID;
    settings.json forwards it via ${CS_RUN_ID} so every bound hook subprocess receives it.
  Add an injection_screen binding on the subagent task-delivery event if Claude Code exposes one
  (e.g. a PreToolUse matcher on the Task/subagent tool, or SubagentStart). If no such event exists in the
  installed Claude Code version, the MANDATORY runner pre-screen (Task 2) is the enforced path; document
  which mechanism was used in the SUMMARY.

cs_team_demo.py current:
  _pre_screen_ticket(ticket) -> (bool, str)  already calls screen_for_injection(body) (lines ~199-206).
  run_ticket() already calls it (lines ~255-267) — make this NON-bypassable + set CS_RUN_ID + clean state at end.
  Dead code to remove: lines ~469-471 `raw_body_for_check = ...` + the misleading "(redact_text is called...)" comment.

Subprocess test pattern (NEW file) — use subprocess.run([... , "python", ".claude/hooks/<hook>.py"], input=json_bytes,
  capture_output=True); assert proc.returncode == 2 (block) or == 0 (pass). Run via `uv run python` to match deployed command,
  OR plain `sys.executable` with cwd=repo root + PYTHONPATH so `from src...` resolves. Set/unset CS_RUN_ID via env= for the
  escalation_gate state-file cases. This is the deployed-surface proof that Layer (b) in test_e2e_dry_run.py cannot give.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stateful final-risk veto — per-run state file in escalation_gate.py (CR-02)</name>
  <files>.claude/hooks/escalation_gate.py, .claude/settings.json</files>
  <read_first>
    - .claude/hooks/escalation_gate.py (FULL — should_escalate, _derive_signals, main() lines ~91-128)
    - .claude/settings.json (FULL — PreToolUse/PostToolUse/SubagentStop bindings + env block)
    - src/config.py lines 28-60, 113-117 (settings singleton, dry_run, SendMode — conventions to stay consistent with)
    - .claude/CLAUDE.md (D-08 any-signal-escalates fail-closed, §4a final accumulated risk check)
    - .planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-VERIFICATION.md (CR-02 evidence)
  </read_first>
  <action>
    Add out-of-band run-state to escalation_gate.py. Define a state-file path helper using `tempfile.gettempdir()` + `cs_run_state/<CS_RUN_ID>.json` (create the parent dir on first write; never hardcode /tmp). Add `_state_path() -> Path | None` returning None when CS_RUN_ID env var is unset. Add `_write_signals(signals: dict)` that OR-merges any True signal into the existing file's signals (read-modify-write; tolerate missing/partial file by starting from all-False; never flips a True back to False — additive per D-08; stamp updated_at ISO8601). Add `_read_signals() -> dict | None` returning the persisted signals dict, or None if path missing/file absent/unparseable.

    Rewire main(): determine context as today (is_final_veto when tool_name=="submit_reply" or hook_event_name=="PreToolUse").
    - WRITE context (PostToolUse / SubagentStop, i.e. NOT final veto): derive signals from the payload via the existing _derive_signals(), `_write_signals(derived)`, then exit 0 if no signal (so non-final stages don't hard-block here) OR exit 1 to surface an early escalation signal when a signal is present — preserve current non-final exit-1 behavior. Writing happens regardless of exit.
    - READ context (PreToolUse@submit_reply final veto): call `_read_signals()`. FAIL CLOSED — if CS_RUN_ID unset OR state file missing OR unparseable → print escalate verdict reason `escalate:no_run_state` and `sys.exit(2)`. If signals were read, run should_escalate(signals); if any signal True → print escalate verdict + `sys.exit(2)`; if all clean → `sys.exit(0)`. Keep the except/fail-closed branch escalating (exit 2 in final-veto context).
    Keep should_escalate() and _derive_signals() logic intact (reused for the WRITE side).

    settings.json: preserve all existing bindings and order. In the `env` block (currently `{"SEND_MODE":"dry_run"}`) ADD the literal key `"CS_RUN_ID": "${CS_RUN_ID}"` so Claude Code forwards the runner-exported CS_RUN_ID into every bound hook subprocess (without this the stateful veto is dead on the deployed path — hooks only see env declared here or inherited from the shell). Do NOT remove the PostToolUse/SubagentStop escalation_gate bindings — those are now the WRITE side.
  </action>
  <acceptance_criteria>
    - WRITE then READ blocks (high-risk signal persists across subprocess invocations) — `CS_RUN_ID` exported once so it reaches BOTH the write and read subprocesses:
      `export CS_RUN_ID=ci-test-1; rm -rf "${TMPDIR:-/tmp}/cs_run_state"; echo '{"hook_event_name":"PostToolUse","signals":{"high_risk_category":true}}' | uv run python .claude/hooks/escalation_gate.py; echo '{"tool_name":"submit_reply","tool_input":{"body":"x","citations":[]}}' | uv run python .claude/hooks/escalation_gate.py; test $? -eq 2; unset CS_RUN_ID`
    - Fail-closed when no state file for the run: `export CS_RUN_ID=ci-empty-$$; rm -f "${TMPDIR:-/tmp}/cs_run_state/$CS_RUN_ID.json"; echo '{"tool_name":"submit_reply","tool_input":{"body":"x","citations":[]}}' | uv run python .claude/hooks/escalation_gate.py; test $? -eq 2; unset CS_RUN_ID`
    - Fail-closed when CS_RUN_ID unset: `env -u CS_RUN_ID sh -c 'echo "{\"tool_name\":\"submit_reply\",\"tool_input\":{\"body\":\"x\",\"citations\":[]}}" | uv run python .claude/hooks/escalation_gate.py'; test $? -eq 2`
    - Clean run passes — `CS_RUN_ID` exported once so the all-False write is visible to the read: `export CS_RUN_ID=ci-clean-$$; rm -f "${TMPDIR:-/tmp}/cs_run_state/$CS_RUN_ID.json"; echo '{"hook_event_name":"PostToolUse","signals":{"high_risk_category":false,"low_confidence":false,"conflict":false,"stale_only":false,"missing_key":false}}' | uv run python .claude/hooks/escalation_gate.py; echo '{"tool_name":"submit_reply","tool_input":{"body":"x [KB-1]","citations":[{"id":"KB-1"}]}}' | uv run python .claude/hooks/escalation_gate.py; test $? -eq 0; unset CS_RUN_ID`
    - `grep -c 'CS_RUN_ID' .claude/hooks/escalation_gate.py` >= 2 (read + write paths).
    - `grep -c 'CS_RUN_ID' .claude/settings.json` >= 1 (the `"CS_RUN_ID": "${CS_RUN_ID}"` forwarding line is in the env block — without it hooks never receive the var on the deployed path).
    - `uv run python -c "import json; json.load(open('.claude/settings.json'))"` (valid JSON; bindings intact).
    - Existing structural binding test still green: `uv run pytest tests/cs_team/test_settings_hook_bindings.py -q`.
  </acceptance_criteria>
  <verify>
    <automated>export CS_RUN_ID=ci-verify-$$; rm -rf "${TMPDIR:-/tmp}/cs_run_state"; echo '{"hook_event_name":"PostToolUse","signals":{"high_risk_category":true}}' | uv run python .claude/hooks/escalation_gate.py; echo '{"tool_name":"submit_reply","tool_input":{"body":"x","citations":[]}}' | uv run python .claude/hooks/escalation_gate.py; test $? -eq 2 && echo STATE-VETO-OK; unset CS_RUN_ID</automated>
  </verify>
  <done>escalation_gate writes accumulated risk to a per-run CS_RUN_ID state file at PostToolUse/SubagentStop and reads it at submit_reply, blocking (exit 2) when any signal is set or when no state exists; settings.json bindings preserved and valid.</done>
</task>

<task type="auto">
  <name>Task 2: Mandatory runner pre-screen + CS_RUN_ID lifecycle + subagent injection binding + dead-var cleanup (CR-04 deploy)</name>
  <files>scripts/cs_team_demo.py, .claude/settings.json</files>
  <read_first>
    - scripts/cs_team_demo.py (FULL — _pre_screen_ticket ~199-206, run_ticket ~231-275, main ~422-496, dead var ~469-471)
    - .claude/settings.json (FULL — UserPromptSubmit binding + any subagent/Task event support)
    - .claude/hooks/injection_screen.py (screen_for_injection contract; fail-closed _extract_body from 04-04)
    - tests/fixtures/sample_tickets.py (confirmed constants: BENIGN_TICKET, HIGH_RISK_TICKET, INJECTION_TICKET — used by the Task 2 acceptance imports)
    - .claude/agents/cs-lead.md (how the lead delegates to subagents — confirms the body→subagent task path)
    - .claude/CLAUDE.md (D-14 body untrusted + screened before any agent sees it)
    - .planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-VERIFICATION.md (CR-04 evidence)
  </read_first>
  <action>
    cs_team_demo.py: make the injection pre-screen NON-bypassable on the real path. Move the `_pre_screen_ticket()` call so it runs unconditionally at the very start of run_ticket() BEFORE any branch (both the use_live_claude CLI path and the simulation path) — it already does, but add an explicit guard so an injection escalate returns immediately and the CLI is never invoked; add a module-level note that this deterministic pre-screen is the enforced D-14 entry gate for the runner (not a demo convenience). Generate and export `CS_RUN_ID` once per run_ticket() invocation (e.g. `os.environ["CS_RUN_ID"] = f"{ticket_id}-{uuid4().hex[:8]}"`) before invoking the CLI so the bound escalation_gate hooks share state; in a finally block, delete the per-run state file (best-effort) to honor the lifecycle defined in Task 1. Remove the dead `raw_body_for_check` variable and the misleading "(redact_text is called...)" comment at ~lines 469-471.

    settings.json: if the installed Claude Code exposes a subagent task-delivery event (e.g. a PreToolUse matcher on the Task tool, or SubagentStart), add an injection_screen binding there so the body is screened when delivered to a subagent. If no such event exists, do not invent one — rely on the mandatory runner pre-screen above and document the chosen mechanism in the SUMMARY. Preserve all existing bindings/order.
  </action>
  <acceptance_criteria>
    - Dead variable removed: `grep -c 'raw_body_for_check' scripts/cs_team_demo.py` returns 0.
    - CS_RUN_ID is set by the runner: `grep -c 'CS_RUN_ID' scripts/cs_team_demo.py` >= 1.
    - Injection ticket short-circuits before any CLI call (no live claude needed): `uv run python -c "import asyncio,sys; sys.path.insert(0,'.'); from scripts.cs_team_demo import run_ticket; from tests.fixtures.sample_tickets import INJECTION_TICKET; v=asyncio.run(run_ticket(INJECTION_TICKET)); assert v['action']=='escalate' and v['reason'].startswith('injection:'), v; print('PRESCREEN-OK')"`
    - Benign ticket still produces a draft in simulation: `uv run python -c "import asyncio,sys; sys.path.insert(0,'.'); from scripts.cs_team_demo import run_ticket; from tests.fixtures.sample_tickets import BENIGN_TICKET; v=asyncio.run(run_ticket(BENIGN_TICKET)); assert v['action']=='draft', v; print('BENIGN-OK')"`
    - State file cleaned after run: after the benign run above, `ls "${TMPDIR:-/tmp}/cs_run_state/" 2>/dev/null | wc -l` does not grow unbounded (best-effort cleanup present in a finally).
    - settings.json valid JSON: `uv run python -c "import json; json.load(open('.claude/settings.json'))"`.
    - Subagent-binding decision is provable (at least ONE branch must hold): EITHER a binding was added — `grep -c injection_screen .claude/settings.json` >= 2 — OR the runner-only path is documented — `grep -c 'D-14' .planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-05-SUMMARY.md` >= 1. The SUMMARY must record which mechanism was chosen.
    - `uv run pytest tests/cs_team/test_e2e_dry_run.py -q` still green (in-process layer unaffected).
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'raw_body_for_check' scripts/cs_team_demo.py | grep -qx 0 && uv run python -c "import asyncio,sys; sys.path.insert(0,'.'); from scripts.cs_team_demo import run_ticket; from tests.fixtures.sample_tickets import INJECTION_TICKET; v=asyncio.run(run_ticket(INJECTION_TICKET)); assert v['action']=='escalate' and v['reason'].startswith('injection:'); print('CR04-OK')"</automated>
  </verify>
  <done>The runner sets CS_RUN_ID per run, mandatorily injection-screens the body before any CLI/subagent invocation (returns escalate immediately on suspicion), cleans the state file at run end, and the dead variable/comment is removed; subagent injection binding added or the runner pre-screen documented as the enforced D-14 path.</done>
</task>

<task type="auto">
  <name>Task 3: Subprocess exit-code proof suite for all PreToolUse hooks (TEST GAP)</name>
  <files>tests/cs_team/test_hooks_subprocess.py</files>
  <read_first>
    - tests/cs_team/test_e2e_dry_run.py (the in-process Layer (b) that this complements — do not duplicate; this is the DEPLOYED-surface proof)
    - .claude/hooks/pre_send_guard.py, grounding_check.py, escalation_gate.py, injection_screen.py (the subprocess targets, post-04-04 + Task 1 here)
    - .claude/settings.json (the exact `uv run python .claude/hooks/<hook>.py` command form to mirror)
    - tests/cs_team/conftest.py (repo-root / sys.path conventions; PYTHONPATH for `from src...`)
    - .planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-VERIFICATION.md (TEST GAP section — required coverage list)
  </read_first>
  <action>
    Create tests/cs_team/test_hooks_subprocess.py. Each test pipes a JSON payload to a real hook subprocess (use subprocess.run with `sys.executable .claude/hooks/<hook>.py`, cwd=repo root, env carrying PYTHONPATH=repo root and CS_RUN_ID where relevant; or `uv run python` if available) and asserts the integer returncode. This is the deployed-surface contract Layer (b) cannot prove. Cover:
    - pre_send_guard: commitment-language body → returncode == 2; clean cited body → returncode == 0.
    - grounding_check: ungrounded (citations present, no marker) → 2; empty-citation no-marker → 2 (CR-03); properly cited → 0.
    - escalation_gate: WRITE a high_risk signal (PostToolUse payload, fixed CS_RUN_ID, temp state dir) THEN submit_reply payload with same CS_RUN_ID → 2; submit_reply with a CS_RUN_ID that has NO state file → 2 (fail-closed); CS_RUN_ID unset → 2 (fail-closed); clean all-False signals written then submit_reply → 0. Use a unique CS_RUN_ID per test and clean its state file in teardown.
    - injection_screen: an instruction-override body → non-zero (escalate); missing body field → non-zero (fail-closed, CR-04); clean body → 0.
    Add a helper `_run_hook(name, payload, env=None) -> CompletedProcess`. Keep payloads small and inline. No real LLM, no network, no DB. Mark nothing as skip — these must run in CI.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/cs_team/test_hooks_subprocess.py -q` passes with all cases above present.
    - The file actually invokes a subprocess (not in-process function calls): `grep -c 'subprocess' tests/cs_team/test_hooks_subprocess.py` >= 1 AND `grep -c 'returncode' tests/cs_team/test_hooks_subprocess.py` >= 6.
    - At least one test asserts returncode == 2 for pre_send_guard, grounding_check (both ungrounded and empty-citation), and escalation_gate (state veto + two fail-closed cases): `grep -c 'returncode == 2' tests/cs_team/test_hooks_subprocess.py` >= 5.
    - At least one clean-pass case per gate asserts returncode == 0: `grep -c 'returncode == 0' tests/cs_team/test_hooks_subprocess.py` >= 3.
    - Full regression green: `uv run pytest tests/cs_team/ -q`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cs_team/test_hooks_subprocess.py -q && grep -c 'returncode == 2' tests/cs_team/test_hooks_subprocess.py | awk '{exit !($1>=5)}' && echo SUBPROC-SUITE-OK</automated>
  </verify>
  <done>A subprocess-based suite proves every PreToolUse hook emits exit 2 on violation and 0 on clean input — matching deployed enforcement; escalation_gate's state-file veto and both fail-closed defaults are covered; full tests/cs_team/ suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accumulated risk signals → submit_reply | stateless hook subprocesses must share risk via an out-of-band per-run file; missing state must fail closed |
| inbound ticket body → subagent task input | untrusted body must be injection-screened on the real deployed path before any subagent sees it |
| deployed hook exit codes → enforcement | the test surface must exercise the real subprocess exit-code contract, not in-process functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Elevation of Privilege | escalation_gate final veto reads nothing | mitigate | per-run CS_RUN_ID state file written at PostToolUse/SubagentStop, read at submit_reply; exit 2 on any signal (D-08/SAFE-03) |
| T-04-05-02 | Tampering | missing run state silently passes | mitigate | fail-closed: no CS_RUN_ID / no file / unparseable → escalate (exit 2) |
| T-04-05-03 | Spoofing | injection body reaches subagents unscreened | mitigate | mandatory non-bypassable runner pre-screen before any CLI/subagent call + subagent-event binding when available (D-14/SAFE-04) |
| T-04-05-04 | Repudiation | green tests mask open gate | mitigate | subprocess tests assert deployed exit-code contract (returncode==2/0) for every PreToolUse hook |
| T-04-05-05 | Information Disclosure | run-state file leaks PII | mitigate | state file stores only boolean signal flags + timestamp — no ticket body/PII; lives in ephemeral tempdir, deleted at run end |
| T-04-05-SC | Tampering | no net-new package installs | accept | edits existing files + new test using stdlib subprocess; no package-manager installs |
</threat_model>

<verification>
- escalation_gate state-veto subprocess proofs pass (write→veto, both fail-closed cases, clean pass)
- Runner injection pre-screen short-circuits the injection ticket before any CLI call; CS_RUN_ID set; dead var removed
- `uv run pytest tests/cs_team/test_hooks_subprocess.py -q` green; `grep returncode == 2` >= 5
- `uv run pytest tests/cs_team/ -q` green (full regression incl. structural + in-process layers)
- settings.json remains valid JSON with all §4a bindings intact
</verification>

<success_criteria>
- High-risk signal accumulated upstream blocks submit_reply via the per-run state file; no-state fails closed (CR-02 closed → SAFE-03 met)
- Ticket body deterministically injection-screened on the real deployed path before subagents (CR-04 deploy closed → SAFE-04 met)
- Subprocess tests prove the deployed exit-code enforcement for every PreToolUse hook (test gap closed)
- Re-verification can reach 4/4 must-haves
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-05-SUMMARY.md` when done
</output>
