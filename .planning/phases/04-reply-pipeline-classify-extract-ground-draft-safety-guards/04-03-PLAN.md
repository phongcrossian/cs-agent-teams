---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 03
type: execute
wave: 4
depends_on: ["04-00", "04-01", "04-02"]
files_modified:
  - scripts/cs_team_demo.py
  - tests/cs_team/test_e2e_dry_run.py
autonomous: false
requirements: [REP-01, REP-02, REP-03, REP-04, SAFE-03, SAFE-04]

must_haves:
  truths:
    - "A local runner feeds a sample ticket to cs-lead and prints the resulting verdict (design section 7)"
    - "Benign ticket -> action=draft with >=1 citation and NO commitment language"
    - "High-risk ticket (refund demand) -> action=escalate with the triggering signal and NO draft"
    - "Injection ticket -> action=escalate via injection_screen with NO draft"
    - "PII never appears in the runner logs/traces (redacted before any sink)"
    - "Everything runs in DRY_RUN — nothing is posted to Freshdesk"
  artifacts:
    - path: "scripts/cs_team_demo.py"
      provides: "local PoC runner: feed benign/high-risk/injection ticket to cs-lead, print verdict"
      contains: "dry_run"
    - path: "tests/cs_team/test_e2e_dry_run.py"
      provides: "automated assertions on the produced verdicts"
      contains: "escalate"
  key_links:
    - from: "scripts/cs_team_demo.py"
      to: ".claude/agents/cs-lead.md (via claude CLI / Agent SDK)"
      via: "headless invocation of the team lead"
      pattern: "cs-lead|claude"
    - from: "scripts/cs_team_demo.py"
      to: "src/guards/pii.py redact_text"
      via: "redact before any print/log"
      pattern: "redact_text"
---

<objective>
Chứng minh team chạy được LOCAL trong DRY_RUN — đây là tiêu chí nghiệm thu §7 của design.

Mục tiêu: runner mỏng `scripts/cs_team_demo.py` nạp 3 sample ticket (benign / high-risk refund / injection) vào cs-lead (qua `claude` CLI headless hoặc Agent SDK — chọn cái đơn giản giữ hooks enforce được), parse verdict, in kết quả đã redact PII, và `tests/cs_team/test_e2e_dry_run.py` assert: benign → draft + ≥1 citation + không commitment; high-risk → escalate + signal + không draft; injection → escalate + không draft; PII không lọt log. Tất cả DRY_RUN — không post Freshdesk.

Purpose: Cổng nghiệm thu local PoC; verdict schema giữ ổn định cho Phase-5 + worker (live deferred).
Output: scripts/cs_team_demo.py + tests/cs_team/test_e2e_dry_run.py + 1 checkpoint human-verify (đăng nhập claude/đặt env trước net-new install; chạy demo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/specs/2026-06-02-cs-agent-team-design.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-PATTERNS.md
@.claude/settings.json
@.claude/CLAUDE.md

<interfaces>
<!-- Runner analog + DRY_RUN pattern. Extracted from codebase + PATTERNS.md. -->

Runner analog tests/smoke/test_grounding_demo.py:
  - module-level client injection (lines 36-51): import MCP impl + set_selless_client
  - async main() (lines 183-193): feed fixture, invoke, parse, assert
  - acceptance-print shape (PATTERNS.md lines 332-338): [PASS] benign -> draft; [PASS] high-risk -> escalate; etc.

DRY_RUN pattern (src/work_queue/send.py lines 105-121): log would-be action, never call Freshdesk, return {"dry_run": True}
Config singleton: from src.config import settings (settings.dry_run is True; claude_model_classify/draft)
PII: from src.guards.pii import redact_text — call before ANY print/log of ticket body or draft

Sample tickets: tests/fixtures/sample_tickets.py — BENIGN_TICKET, HIGH_RISK_TICKET, INJECTION_TICKET
Verdict shape (from 04-02): action in {draft, escalate}; draft -> {body, citations}; escalate -> {reason, risk_signals}

Open questions to resolve at build (design section 9): (1) local Agent SDK auth = Claude subscription vs API key;
(2) hook transport = settings.json shell hooks vs Agent SDK programmatic hooks — pick the one the runner uses.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify agent-team SDK package legitimacy + local auth/env before install & run</name>
  <action>Blocking human gate (no auto-approve): verify the agent-team runtime PyPI package is the official Anthropic package, set local Claude auth, bring up MCP env + Docker pgvector, confirm DRY_RUN. See how-to-verify.</action>
  <what-built>
    Wave 0 declared the agent-team runtime dependency (anthropic / claude-agent-sdk) but there is NO
    Package Legitimacy Audit for this phase, so any net-new PyPI package is treated [ASSUMED] and must be
    human-verified BEFORE install, and the local team run needs the developer's Claude auth + env.
  </what-built>
  <how-to-verify>
    1. Confirm the agent-team SDK/CLI package name + that it is the official Anthropic package:
       verify on https://pypi.org/project/anthropic and the official Claude Agent SDK / claude CLI page.
       Reject any typosquat; approve the exact official package name + version (v1.x).
    2. Authenticate the local runtime: run `claude login` (Claude subscription) OR set ANTHROPIC_API_KEY
       in .env (per design section 5 / open question 9.1). Confirm which auth path you want for the PoC.
    3. Export the MCP env the servers need (DATABASE_URL, VOYAGE_API_KEY, SELLESS_API_BASE_URL) and confirm
       the Docker pgvector stack is up (per MEMORY: colima + docker-compose) so the Knowledge/Selless MCPs launch.
    4. Confirm SEND_MODE=dry_run / settings.dry_run is True (no live Freshdesk posting in this phase).
  </how-to-verify>
  <resume-signal>Type "approved" once the package is verified-legit, auth is set, MCP env+DB are up, and DRY_RUN is confirmed — or name the corrections.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: scripts/cs_team_demo.py — local runner feeding sample tickets to cs-lead</name>
  <files>scripts/cs_team_demo.py</files>
  <read_first>
    - tests/smoke/test_grounding_demo.py lines 28-51, 183-193, 483+ (runner structure, client injection, print shape)
    - .planning/phases/.../04-PATTERNS.md lines 292-338 (runner design + acceptance-print + DRY_RUN output)
    - src/work_queue/send.py lines 105-121 (DRY_RUN log-only pattern)
    - tests/fixtures/sample_tickets.py (the three sample tickets)
    - .claude/settings.json + .claude/CLAUDE.md (how the team is wired + the safety contract)
  </read_first>
  <action>
    Create scripts/cs_team_demo.py: an async main() that, for each of BENIGN_TICKET, HIGH_RISK_TICKET, INJECTION_TICKET, invokes the cs-lead team (headless — via the `claude` CLI subprocess against .claude/, or the Claude Agent SDK; pick the simpler one that keeps the settings.json hooks enforceable, resolving design open questions 9.3/9.4), parses the returned verdict JSON (action draft|escalate), and PRINTS a one-line [PASS]/[FAIL] per the PATTERNS.md acceptance shape. Before printing or logging ANY ticket body / draft body, pass it through `from src.guards.pii import redact_text` (never emit raw PII). Honor DRY_RUN: read `from src.config import settings`, assert settings.dry_run is True, and never call any Freshdesk send path (mirror src/work_queue/send.py dry-run log-only). Provide a `--ticket {benign|high_risk|injection|all}` CLI arg (default all). Keep main() pure/importable so the e2e test (Task 2) and the Phase-5 harness can reuse it. Print a final summary line counting passes.
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('scripts/cs_team_demo.py').read()); src=open('scripts/cs_team_demo.py').read(); assert 'redact_text' in src; assert 'dry_run' in src.lower(); assert 'from src.config import settings' in src; print('runner-OK')"</automated>
    <human-check>Run `uv run python scripts/cs_team_demo.py --ticket all` after the checkpoint; observe [PASS] lines: benign->draft+citation, high-risk->escalate, injection->escalate; no raw PII in output.</human-check>
  </verify>
  <done>Runner feeds the three sample tickets to cs-lead, prints redacted [PASS]/[FAIL] verdicts, stays DRY_RUN, and exposes an importable main().</done>
</task>

<task type="auto">
  <name>Task 2: tests/cs_team/test_e2e_dry_run.py — automated acceptance assertions</name>
  <files>tests/cs_team/test_e2e_dry_run.py</files>
  <read_first>
    - scripts/cs_team_demo.py (Task 1 — importable main()/per-ticket entry to assert against)
    - tests/smoke/test_grounding_demo.py (async pytest patterns, fixtures, sandbox-gating convention)
    - .claude/hooks/*.py (the deterministic verdict producers the test can assert through if a live team run is unavailable in CI)
    - docs/specs/2026-06-02-cs-agent-team-design.md section 7 (acceptance criteria)
  </read_first>
  <action>
    Create tests/cs_team/test_e2e_dry_run.py asserting the design section-7 acceptance. Because a live `claude`/SDK run requires the developer's auth (not available in CI), structure the test in two layers: (a) a DETERMINISTIC layer that always runs in CI — drive the three sample tickets through the Wave-1 hooks directly (injection_screen on INJECTION_TICKET.body -> escalate; pre_send_guard on a refund-bearing draft -> escalate; grounding_check on a benign grounded draft -> pass; should_escalate on a high_risk_category signal -> escalate) and assert each verdict matches the expected action + that NO raw PII (email/phone from the fixtures) appears in any captured output; (b) a LIVE layer gated behind an env flag (e.g. RUN_CS_TEAM=1, mirroring the RUN_SANDBOX convention in test_grounding_demo.py) that actually invokes scripts.cs_team_demo.main() and asserts benign->draft+>=1 citation+no commitment, high-risk->escalate+no draft, injection->escalate+no draft. The CI/deterministic layer must be green without auth; the live layer is human-attested (the Task-1 human-check). Assert settings.dry_run is True so no live send can occur.
  </action>
  <verify>
    <automated>uv run pytest tests/cs_team/test_e2e_dry_run.py -q</automated>
  </verify>
  <done>Deterministic CI layer green without auth (hooks produce the expected escalate/draft verdicts + no PII leak); live layer present but env-gated; DRY_RUN asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sample ticket → cs-lead | the full team is exercised on attacker-shaped inputs (injection/refund) |
| verdict → demo output/log | draft/body text crosses to stdout/log; must be redacted |
| runner → Freshdesk | MUST remain closed in this phase (DRY_RUN only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | demo stdout/logs | mitigate | redact_text on every body/draft before print/log; e2e test asserts no raw PII |
| T-04-03-02 | Elevation of Privilege | accidental live send | mitigate | settings.dry_run asserted True; no Freshdesk send path called (mirrors send.py dry-run) |
| T-04-03-03 | Tampering | net-new PyPI install (no audit) | mitigate | blocking-human checkpoint verifies official package on pypi.org before install ([ASSUMED] policy) |
| T-04-03-04 | Spoofing | injection ticket bypassing team | mitigate | injection_screen hook escalates INJECTION_TICKET; asserted in deterministic e2e layer (D-14/SAFE-04) |
| T-04-03-05 | Repudiation | high-risk auto-answered | mitigate | high-risk/refund ticket -> escalate+no draft asserted (D-08/D-10/SAFE-03/SAFE-04) |
| T-04-03-SC | Tampering | install supply chain | mitigate | T-04-00-SC closed here via blocking checkpoint; never auto-approvable |
</threat_model>

<verification>
- Checkpoint approved: package verified-legit, auth set, MCP env+DB up, DRY_RUN confirmed
- `uv run pytest tests/cs_team/test_e2e_dry_run.py -q` green (deterministic layer, no auth needed)
- `uv run python scripts/cs_team_demo.py --ticket all` (human-check): benign->draft+citation, high-risk->escalate, injection->escalate, no raw PII
- nothing posted to Freshdesk (DRY_RUN)
</verification>

<success_criteria>
- Design section-7 acceptance demonstrated locally: benign->draft+>=1 citation+no commitment; high-risk->escalate+no draft; injection->escalate+no draft; PII never in logs
- Runner reusable (importable main) for the Phase-5 harness
- Everything DRY_RUN; live Freshdesk wiring remains deferred
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-03-SUMMARY.md` when done
</output>
