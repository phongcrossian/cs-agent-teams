---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - scripts/cs_team_demo.py
  - tests/fixtures/sample_tickets.py
  - tests/test_cs_team_demo_always_draft.py
autonomous: true
requirements: [SAFE-03, SAFE-04, REP-03]
must_haves:
  truths:
    - "Running the demo on a benign ticket yields action=draft with a grounded body in DRY_RUN (no Freshdesk post)"
    - "Running the demo on a high-risk (refund/legal) ticket yields action=draft WITH an advisory escalation_hint — never action=escalate-with-no-draft"
    - "Running the demo on an injection ticket is still screened by injection_screen (D-14) and recorded as an advisory hint, but the pipeline still drafts"
    - "A missing-order ticket yields a draft built from a verify-order / clarify-order-info flow, not fabricated order facts (D-34)"
    - "The demo never emits an escalate=no-draft verdict and never references the deleted guard hooks"
  artifacts:
    - path: "scripts/cs_team_demo.py"
      provides: "Always-draft DRY_RUN runner: classify→extract→file-store draft→advisory hint; verdict parser accepts the always-draft shape"
      contains: "draft"
    - path: "tests/test_cs_team_demo_always_draft.py"
      provides: "Test asserting every sample ticket yields action=draft (with hint where applicable) and never escalate=no-draft"
      min_lines: 40
  key_links:
    - from: "scripts/cs_team_demo.py"
      to: "src/file_store/template_store.py"
      via: "simulated draft grounded on the local file-store (D-31)"
      pattern: "file_store|get_template_from_file"
    - from: "scripts/cs_team_demo.py"
      to: ".claude/hooks/injection_screen.py"
      via: "surviving injection pre-screen feeding an advisory hint (D-14)"
      pattern: "injection_screen"
    - from: "scripts/cs_team_demo.py"
      to: "src/reply_mcp/server.py"
      via: "DRY_RUN submit_reply persistence (no Freshdesk post)"
      pattern: "submit_reply|dry_run"
---

<objective>
Rework `scripts/cs_team_demo.py` from the fail-closed escalate-emitting runner to the **always-draft** flow (D-33), grounded on the local file-store (D-31) with **flow-aware fallback** (D-34), proving end-to-end DRY_RUN behavior across the sample tickets.

Purpose: The current demo still emits `escalate` verdicts, imports the deleted guard hooks, and high-risk/injection tickets short-circuit to no-draft. Under D-30 the pipeline always drafts. This slice makes the runner and its verdict parser match the always-draft contract: every ticket produces a draft, with an advisory `escalation_hint` attached for money/legal/injection/low-confidence cases, and a missing order produces a verify-order/clarify-order draft instead of fabricated facts. This is the independently runnable end-to-end proof of the rework.

Output: a reworked `cs_team_demo.py`, refreshed sample-ticket fixtures (expected verdicts updated to always-draft), and a test asserting the always-draft contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.claude/CLAUDE.md

<interfaces>
<!-- File-store (04-00): src/file_store/template_store.py get_template_from_file(code) / subtype_to_code(sub_type) -->
<!-- Surviving hooks: injection_screen.py (exit 1 + {"action":"escalate"} on injection — in always-draft we capture this as an advisory hint, NOT a no-draft); pii_redact.py (never blocks) -->
<!-- Reply chokepoint: src/reply_mcp/server.py submit_reply(body, citations) — DRY_RUN persists to queue.dry_run_log, no Freshdesk post -->
<!-- Current demo internals to rework: _parse_verdict (fail-closed → escalate), _simulate_verdict (high-risk category → escalate; benign → draft), _post_screen_draft (commitment tripwire → escalate), _ESCALATE_ACTION constant, _load_hook importing deleted guard modules -->
<!-- Current fixtures: tests/fixtures/sample_tickets.py BENIGN_TICKET (→draft), HIGH_RISK_TICKET (→escalate), INJECTION_TICKET (→escalate) — expected verdicts updated to always-draft -->
<!-- D-33 verdict shape: {"action":"draft", "body":..., "citations":[...], "escalation_hint": {...}|null} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the demo runner to always-draft + advisory hint + D-34 fallback</name>
  <files>scripts/cs_team_demo.py</files>
  <read_first>
    - scripts/cs_team_demo.py (the whole runner: _load_hook, _parse_verdict, _build_prompt, _pre_screen_ticket, _post_screen_draft, _simulate_verdict, run_ticket, _run_via_claude_cli, main)
    - src/file_store/template_store.py (file-store API for the simulated draft grounding)
    - .claude/hooks/injection_screen.py (the surviving pre-screen contract — now feeds an advisory hint, not a no-draft)
    - .claude/hooks/pii_redact.py (surviving; never blocks)
    - src/reply_mcp/server.py (submit_reply DRY_RUN persistence)
    - .claude/skills/reply-pipeline/SKILL.md (the always-draft workflow + escalation_hint shape, reworked in 04-02)
  </read_first>
  <action>
    Rework `cs_team_demo.py` to the always-draft contract: (1) Remove `_load_hook`/`importlib` loading of the deleted guard modules (`pre_send_guard`, `escalation_gate`, `grounding_check`, `authorized_offer`); keep only the `injection_screen` import for the surviving pre-screen. (2) `_pre_screen_ticket` / injection handling: when injection_screen flags the body, do NOT return a no-draft escalate — instead record an advisory `escalation_hint={"reason":"injection:...","signals":{...}}` and continue to draft. (3) `_simulate_verdict`: always return `{"action":"draft","body":...,"citations":[...],"escalation_hint": hint|None}`; ground the simulated body via the file-store (`subtype_to_code` + `get_template_from_file`) using the ticket category/sub-type; for a high-risk category (refund/legal/etc.) attach an advisory hint but STILL draft; for a missing/unresolvable order, build the body from a **verify-order / clarify-order-info** flow per D-34 rather than fabricating order numbers. (4) Remove `_post_screen_draft`'s commitment-language → escalate behavior (the guard is gone); if you keep any post-screen, it may only annotate an advisory hint, never change action away from draft. (5) `_parse_verdict`: accept the always-draft shape; the fail-closed fallback returns `{"action":"draft","escalation_hint":{"reason":"parse_error"}}` (still a draft, never escalate=no-draft). (6) Persist the would-be reply through `submit_reply` (DRY_RUN) so the demo proves no Freshdesk post. Keep PII redaction on any logged body (D-04). Update the module docstring's expected-outcome comments (BENIGN/HIGH_RISK/INJECTION) to the always-draft expectations. Delete the `_ESCALATE_ACTION` no-draft verdict construction paths.
  </action>
  <verify>
    <automated>uv run python scripts/cs_team_demo.py --help >/dev/null 2>&1; grep -in "file_store\|get_template_from_file\|escalation_hint" scripts/cs_team_demo.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "pre_send_guard\|escalation_gate\|grounding_check\|authorized_offer" scripts/cs_team_demo.py` returns nothing.
    - `grep -in "file_store\|get_template_from_file" scripts/cs_team_demo.py` returns ≥ 1 line (D-31 grounding).
    - `grep -in "escalation_hint" scripts/cs_team_demo.py` returns ≥ 1 line (D-33 advisory).
    - `grep -in "verify-order\|clarify-order\|missing order\|no order" scripts/cs_team_demo.py` returns ≥ 1 line (D-34 fallback).
    - `grep -in "injection_screen" scripts/cs_team_demo.py` returns ≥ 1 line (D-14 surviving pre-screen).
    - `uv run python -c "import ast; ast.parse(open('scripts/cs_team_demo.py').read())"` exits 0 (valid Python).
  </acceptance_criteria>
  <done>The demo runner always produces a draft (no escalate=no-draft path), grounds on the file-store, falls back to a verify/clarify-order flow on a missing order, captures injection/high-risk as advisory hints, persists via DRY_RUN submit_reply, and imports none of the deleted guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update sample-ticket fixtures + assert the always-draft contract end-to-end</name>
  <files>tests/fixtures/sample_tickets.py, tests/test_cs_team_demo_always_draft.py</files>
  <read_first>
    - tests/fixtures/sample_tickets.py (BENIGN_TICKET / HIGH_RISK_TICKET / INJECTION_TICKET dicts + their expected-verdict comments — to update to always-draft)
    - scripts/cs_team_demo.py (the reworked run_ticket / _simulate_verdict from Task 1 — the function under test)
    - src/file_store/template_store.py (so a MISSING_ORDER_TICKET maps to a flow with no fabricated facts)
  </read_first>
  <behavior>
    - run_ticket(BENIGN_TICKET) → action == "draft", body non-empty, escalation_hint is None.
    - run_ticket(HIGH_RISK_TICKET) → action == "draft", escalation_hint.reason contains a money/legal label (e.g. "high_risk" or "money").
    - run_ticket(INJECTION_TICKET) → action == "draft", escalation_hint.reason contains "injection" (injection_screen still flags, but the pipeline drafts).
    - run_ticket(MISSING_ORDER_TICKET) → action == "draft", body references a verify-order / clarify-order flow and contains no fabricated order number (D-34).
    - No sample ticket ever yields action == "escalate" with no body.
  </behavior>
  <action>
    In `tests/fixtures/sample_tickets.py`, update the expected-verdict comments to the always-draft outcomes and ADD a `MISSING_ORDER_TICKET` dict (order-related question with an unresolvable/absent order_ref) for the D-34 fallback case. Create `tests/test_cs_team_demo_always_draft.py` that drives `run_ticket` (in the local-simulate path, no live `claude` CLI) for each of the four fixtures and asserts the five behaviors above — most importantly that NO fixture produces an escalate=no-draft verdict and that the missing-order draft contains no fabricated order number. Use the DRY_RUN path; do not require a live Anthropic key or the `claude` CLI.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cs_team_demo_always_draft.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cs_team_demo_always_draft.py -x` passes.
    - The test asserts `verdict["action"] == "draft"` for all four fixtures (no escalate=no-draft anywhere).
    - The test asserts HIGH_RISK and INJECTION fixtures carry a non-null `escalation_hint`, and BENIGN carries `escalation_hint is None`.
    - The test asserts the MISSING_ORDER draft body contains no fabricated order number and references a verify/clarify-order flow.
    - `grep -c "escalate" tests/fixtures/sample_tickets.py` does not appear as an expected *action* (comments may mention the word historically, but no fixture's expected action is escalate-no-draft).
  </acceptance_criteria>
  <done>All sample tickets — benign, high-risk, injection, and missing-order — yield an always-draft verdict in DRY_RUN, with advisory hints where applicable and no fabricated order facts; a test enforces the always-draft contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body → demo runner | Untrusted email body; injection_screen pre-screen runs (D-14) before the simulated/CLI draft |
| demo runner → submit_reply | DRY_RUN only — persists to queue.dry_run_log, never posts to Freshdesk |
| log output ← ticket body | PII redacted before logging (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | injection in demo ticket body now that it always drafts | mitigate | injection_screen.py pre-screen retained; on flag the runner attaches an advisory escalation_hint and proceeds — the screen still runs (D-14), it just no longer suppresses the draft |
| T-04-03-02 | Spoofing | demo fabricates order facts on a missing order | mitigate | D-34 fallback asserted by the MISSING_ORDER_TICKET test: draft must use verify/clarify-order flow and contain no fabricated order number |
| T-04-03-03 | Elevation of Privilege | demo emits/persists an unauthorized commitment (guard deleted) | accept | Accepted PoC trade-off; bounded by DRY_RUN submit_reply (no Freshdesk post). Before-live guard re-author deferred (04-CONTEXT) |
| T-04-03-04 | Information Disclosure | customer PII in demo logs | mitigate | redact_text retained on logged bodies (D-04); submit_reply redacts at persistence |
| T-04-03-SC | Tampering | n/a — no package installs | accept | Script/test/fixture edits only; no new dependencies |
</threat_model>

<verification>
- `uv run pytest tests/test_cs_team_demo_always_draft.py -x` passes.
- Demo imports no deleted guard hooks; grounds on file-store; uses injection_screen as an advisory pre-screen.
- All four fixtures yield action=draft; high-risk/injection carry advisory hints; missing-order uses a flow with no fabricated facts.
- submit_reply runs in DRY_RUN (no Freshdesk post).
</verification>

<success_criteria>
The end-to-end demo proves the always-draft pipeline (D-33) grounded on the file-store (D-31) with flow-aware fallback (D-34) and surviving D-14/D-04 floor, in DRY_RUN only — the reworked runner matches the deleted guards (04-01) and reworked agents (04-02). SAFE-04's removed guard is demonstrated as DRY_RUN-gated; SAFE-03 escalation is advisory.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-03-SUMMARY.md` when done.
</output>
