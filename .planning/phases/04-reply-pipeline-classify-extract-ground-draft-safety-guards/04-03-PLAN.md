---
phase: 04-reply-pipeline-classify-extract-ground-draft-safety-guards
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/work_queue/worker.py
  - tests/pipeline/test_worker_integration.py
autonomous: false

must_haves:
  truths:
    - "The Phase-2 worker replaces its canned_body placeholder with run_pipeline and routes the result through the existing DRY_RUN send seam to dry_run_log"
    - "An escalation verdict marks the row escalated and sends NO reply (D-10); a draft verdict flows its citation-grounded body through send_reply in DRY_RUN"
    - "A real (PII-redacted) draft body reaches dry_run_log proving the end-to-end classify->...->draft path works through the worker"
    - "LLM fatal errors dead-letter and transient errors retry, reusing the existing worker error taxonomy"
  artifacts:
    - path: "src/work_queue/worker.py"
      provides: "run_pipeline wired into process_queue_row replacing canned_body (D-02)"
      contains: "run_pipeline"
  key_links:
    - from: "src/work_queue/worker.py"
      to: "src/pipeline/orchestrator.run_pipeline"
      via: "Step 6 replacement of canned_body"
      pattern: "run_pipeline"
    - from: "src/work_queue/worker.py"
      to: "src/work_queue/send.send_reply"
      via: "DraftVerdict.body_html -> send_reply (DRY_RUN)"
      pattern: "send_reply"
---

<objective>
Nối orchestrator vào seam DRY_RUN của worker Phase-2 (D-02): thay `canned_body` ở
`src/work_queue/worker.py:201-219` bằng `run_pipeline(...)`. Verdict escalate -> đánh dấu row
"escalated", KHÔNG gửi (D-10); verdict draft -> đẩy body_html qua `send_reply` ở chế độ DRY_RUN
vào `dry_run_log` (đã redact PII tại persistence boundary). Map lỗi LLM fatal/transient vào
dead-letter/retry sẵn có. Checkpoint human-verify chứng minh end-to-end qua worker.

Purpose: Hoàn tất slice cuối — chứng minh code path production duy nhất (D-02) chạy từ inbound
ticket -> draft/escalate -> dry_run_log, không gửi live (Phase 4 chỉ DRY_RUN). Đây là điều
Phase-5 harness sẽ tái dùng và Phase-6 routing gate sẽ tiêu thụ verdict.
Output: worker.py wired + integration test xanh + 1 human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-CONTEXT.md
@.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-PATTERNS.md
@CLAUDE.md

<interfaces>
From src/pipeline/orchestrator.py (04-02):
  async def run_pipeline(ticket_id, conversations, settings) -> PipelineResult   # .verdict.action == "escalate"|"draft"
From src/pipeline/models.py: PipelineResult, EscalationVerdict, DraftVerdict
From src/pipeline/errors.py: PipelineFatalError, PipelineTransientError, PipelineEscalationError

From src/work_queue/worker.py (the seam + surrounding structure to preserve):
  async def process_queue_row(pool, client, row, settings, dead_letter_sink) -> None
  # Step 5 PII note (193-199), Step 6 canned_body SEAM (201-207), Step 7 send_reply (209-219),
  # Step 8 finalize_done (221-227); error handlers FreshdeskFatalError/RateLimit (229-...)
  async def _mark_status(conn, row_id, status) -> None   # line 344 — reuse for "escalated"
From src/work_queue/send.py:
  async def send_reply(client, conn, ticket_id, inbound_msg_id, body, mode, row_id, claim_token)
  # DRY_RUN -> _dry_run() redacts body at persistence boundary (no caller redaction needed for dry_run_log)
From src/observability.py: increment(metric, **labels), emit_alert(event, **context)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace canned_body seam with run_pipeline + verdict routing (D-02 / D-10)</name>
  <files>src/work_queue/worker.py, tests/pipeline/test_worker_integration.py</files>
  <read_first>
    - 04-PATTERNS.md §"src/work_queue/worker.py — DRY_RUN seam replacement (D-02)" (exact before/after for lines 201-219, preserving Steps 5 and 7-8)
    - src/work_queue/worker.py lines 80-228 (process_queue_row full flow), line 344 (_mark_status), lines 229-260 (error taxonomy handlers)
    - src/work_queue/send.py lines 57-121 (send_reply + _dry_run redaction boundary)
    - src/pipeline/orchestrator.py (run_pipeline + PipelineResult.action) and src/pipeline/errors.py
    - 04-CONTEXT.md D-02 (single production path, wire into worker), D-10 (escalate = no send), D-05 Phase-2 (DRY_RUN default)
  </read_first>
  <behavior>
    - Test: process_queue_row with a draft-verdict pipeline (run_pipeline patched) calls send_reply with body == DraftVerdict.body_html and mode == settings.send_mode (DRY_RUN), then finalize_done; dry_run_log row written
    - Test: process_queue_row with an escalate-verdict pipeline calls _mark_status(conn,row_id,"escalated"), increments escalated_total, and does NOT call send_reply
    - Test: run_pipeline raising PipelineFatalError routes the row to the dead-letter sink (reuse existing fatal handler path); PipelineTransientError schedules retry (finalize_retry/backoff path)
    - Test: SKIP-IF-SENT (sent_at not null) still short-circuits BEFORE run_pipeline is called (no wasted LLM tokens on already-sent rows)
    - Test: raw ticket body never appears in logs (redaction holds); dry_run_log body is redacted by the send boundary
  </behavior>
  <action>
    In process_queue_row replace Step 6 (canned_body, lines 201-207): import run_pipeline from
    src.pipeline.orchestrator and call pipeline_result = await run_pipeline(ticket_id=ticket_id,
    conversations=conversations, settings=settings). Branch on pipeline_result.verdict.action: if
    "escalate" -> logger.info("worker_escalated", extra={row_id,ticket_id,reason}) (reason from
    verdict.reason; never log raw body), await _mark_status(conn,row_id,"escalated"),
    increment("escalated_total"), return (NO send — D-10). If "draft" -> reply_body =
    pipeline_result.verdict.body_html and fall through to existing Step 7 send_reply(..., body=reply_body,
    mode=settings.send_mode, ...) and Step 8 finalize_done UNCHANGED. Extend the except chain: add
    `except PipelineFatalError` -> route to dead_letter_sink (same as FreshdeskFatalError handler),
    and `except PipelineTransientError` -> retry/backoff (same shape as FreshdeskRateLimitError/transient
    handler). Keep Step 1 SKIP-IF-SENT and Step 2-5 (conversations fetch, loop-guard, throttle, PII note)
    intact and BEFORE the pipeline call. Add tests/pipeline/test_worker_integration.py with run_pipeline
    monkeypatched (no real LLM/network) covering the behaviors above using the existing db_pool/clean_db
    + respx fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/pipeline/test_worker_integration.py -x -q</automated>
  </verify>
  <done>Worker calls run_pipeline at the seam; draft routes through send_reply DRY_RUN to dry_run_log; escalate marks "escalated" and sends nothing; LLM fatal->dead-letter, transient->retry; SKIP-IF-SENT precedes the pipeline; no raw-body logging.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression + end-to-end DRY_RUN smoke (mock LLM, no network)</name>
  <files>tests/pipeline/test_worker_integration.py</files>
  <read_first>
    - tests/conftest.py (db_pool, clean_db, mock_selless_client) + tests/pipeline/conftest.py (MockLLMClient, mock_knowledge_mcp from 04-00)
    - src/work_queue/send.py _dry_run (dry_run_log insert + redact_text boundary)
    - tests/test_queue.py / tests/test_e2e_sandbox.py (existing worker test style to mirror)
  </read_first>
  <action>
    Add an end-to-end DRY_RUN smoke test: enqueue a synthetic ticket, run process_queue_row with
    set_llm_client(MockLLMClient) + set_selless_client(MockSellessClient) + mock Knowledge MCP wired so
    run_pipeline returns a real DraftVerdict, and assert a redacted body lands in queue.dry_run_log and
    status finalizes done. Add a second smoke asserting an injection/high-risk synthetic ticket ends
    "escalated" with no dry_run_log reply row. Then run the WHOLE suite to confirm no Phase-1/2/3
    regression from the seam change.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>End-to-end DRY_RUN smoke proves a real draft reaches dry_run_log (redacted) and an unsafe ticket escalates with no reply; full test suite green (no regression).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify end-to-end pipeline through the worker (DRY_RUN, real Anthropic)</name>
  <what-built>
    The orchestrator is wired into the worker DRY_RUN seam. With real ANTHROPIC_API_KEY +
    Langfuse keys set, a queued ticket now runs classify(Haiku)->extract(Haiku)+resolve_order->
    ground(MCP)->draft(Sonnet)->commitment-guard->critique(Sonnet) and the result flows to
    dry_run_log (draft) or marks the row escalated (no send). Nothing is ever sent live (Phase 4 = DRY_RUN).
  </what-built>
  <how-to-verify>
    1. Ensure .env has ANTHROPIC_API_KEY + LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY and send_mode=DRY_RUN.
    2. Run the documented DRY_RUN harness/CLI against 2-3 representative tickets:
       (a) a benign order-tracking ticket -> expect a DraftVerdict with inline citations in dry_run_log;
       (b) a refund-demand / legal-threat ticket -> expect "escalated", NO dry_run_log reply row;
       (c) a ticket whose grounding hits a known CONFLICT (e.g. dual-warranty CONTRA-01) -> expect "escalated".
    3. Confirm in dry_run_log the stored body is PII-redacted; confirm in Langfuse the traces show
       Haiku on classify/extract and Sonnet on draft/critique (NO Opus), with prompt caching active on
       the system prompt.
    4. Confirm no Freshdesk reply was POSTed (DRY_RUN — check no live send attempted).
  </how-to-verify>
  <resume-signal>Type "approved" if (a) drafts with citations, (b)/(c) escalate with no send, redaction + Haiku/Sonnet split confirmed; otherwise describe the failing case</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline verdict → worker send path | escalation must NOT send; draft must go DRY_RUN only |
| draft body → dry_run_log | must be PII-redacted at the persistence boundary |
| LLM errors → worker | must map to dead-letter/retry, never silently drop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Elevation (escalated ticket auto-sent) | worker verdict routing (D-10) | mitigate | escalate branch marks "escalated" + returns before send_reply (Task 1); test asserts send_reply not called |
| T-04-15 | Information Disclosure (PII to dry_run_log) | send._dry_run boundary | mitigate | redact_text at persistence boundary (existing); Task 1/2 assert redacted body |
| T-04-16 | Denial (silent drop on LLM error) | worker except chain | mitigate | PipelineFatalError->dead-letter, PipelineTransientError->retry (Task 1) |
| T-04-17 | Elevation (live send in Phase 4) | send_reply mode | mitigate | settings.send_mode stays DRY_RUN (Phase-2 default); checkpoint confirms no live POST (Task 3) |
</threat_model>

<verification>
- `uv run pytest -q` full suite green (no Phase-1/2/3 regression).
- Human-verify checkpoint confirms draft-with-citations, escalation-without-send, redaction, and Haiku/Sonnet (no Opus) split via Langfuse traces, all in DRY_RUN.
</verification>

<success_criteria>
The worker runs the orchestrator through one production code path: benign tickets produce a
citation-grounded draft in dry_run_log, unsafe/conflicting tickets escalate with no send, LLM errors
dead-letter/retry — all DRY_RUN, proving the end-to-end Phase-4 pipeline.
</success_criteria>

<output>
Create `.planning/phases/04-reply-pipeline-classify-extract-ground-draft-safety-guards/04-03-SUMMARY.md` when done
</output>
